# Changelog

All notable changes to this package will be documented in this file.
The format follows [Keep a Changelog](https://keepachangelog.com/en/1.1.0/) and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

_Last updated: 2026-05-09 (M4.X cluster close — post-`v0.9.0-alpha` hardening, no tag yet — folds into `v1.0.0-rc` after M3c UX audit). Highlights: unified audit tool `Tools → Kitforge → UI Kit → Audit` (Path-A clean replacing legacy `Editor/QA/` + `Editor/QA/Catalog/`, 7 pluggable `IUIKitCheck` impls + new `ThemedFieldsWiredCheck` locking symmetric backing-field rule); Stage 2 text-theming sweep B/C/D/E (~36 callsites, +51 themed counts, `CatalogGroupBuilderShared.CreateThemedText` helper + 9 named `ThemeBuilderSlots` tuples); `WarningText` callsite migrations (DailyLogin already-claimed countdown + HUDEnergy regen countdown via existing `ThemeColorSlot.WarningColor`, no new slot — Decision Protocol §4 Contrarian win); HUDGems audit dedup (catalog count 16→17, filename-disambiguated labels for ≥2 prefabs sharing one component type); runtime theme swap API (`SetTheme` on `UIManager`/`PopupManager`/`ToastManager` re-Initializing cached instances) + GroupE_BootDemo theme dropdown (E2E manual swap parity with ThemePresetsDemo); one-click validation flow (`Regenerate + Audit` toolbar button orchestrating A→B→C→D→M4.1→E rebuild + Run Everything; M4.1 included after sample re-import GUID lifecycle bug surfaced); `Documentation~/QAReports/` package-side release evidence ritual (`_README.md` + baseline `_Summary.md` 23/23 pass, separate from buyer-facing `MirrorReportsToAssets` toggle which writes to consumer's `Assets/Editor/QAReports/`)._

## [Unreleased]

## [1.0.1] — 2026-05-11

### Fixed

- **Input System package compatibility** — `KitforgeRoot.prefab` shipped with a legacy `StandaloneInputModule` on its `EventSystem`, which threw `InvalidOperationException: You are trying to read Input using the UnityEngine.Input class` under buyers with `Active Input Handling = Input System Package` (legacy disabled). New `KitforgeInputBootstrap` runtime static class (`Runtime/Bootstrap/KitforgeInputBootstrap.cs`, gated by `ENABLE_INPUT_SYSTEM && !ENABLE_LEGACY_INPUT_MANAGER`) hooks `SceneManager.sceneLoaded` via `[RuntimeInitializeOnLoadMethod(BeforeSceneLoad)]` and reflectively swaps `StandaloneInputModule` for `UnityEngine.InputSystem.UI.InputSystemUIInputModule` on every `EventSystem` in the scene. No asmdef changes (reflection-based — works whether the Input System package is installed or not). No prefab modification (zero binary delta). Zero buyer action required after upgrade.

## [1.0.0] — 2026-05-10

> First stable release of Kitforge Mobile UI Kit. Promotion from `v1.0.0-rc.2` with no code, API, or sample changes — release-candidate cycle (rc.1 → rc.2 → 1.0.0) closed without further hotfixes. Promotion gate: 24/24 UIKit Audit (17 prefabs + 7 scenes) PASS + 303 EditMode tests green. Public API frozen.

### Notes

- No `Added`, `Changed`, `Fixed`, or `BREAKING` entries — content identical to `[1.0.0-rc.2]`. The release-candidate cycle closed clean.
- Buyers upgrading from `v1.0.0-rc.2`: zero migration steps. Pin install URL `#v1.0.0` (or omit the qualifier to track `main`).
- Buyers upgrading from `v0.X.0-alpha`: see migration blocks in earlier version entries below — most recent BREAKING tags are `[0.6.0-alpha]` (Group C — `IEconomyService` v2 + `HUDCurrency` parameterized), `[0.7.0-alpha]` (Group D — `IPlayerDataService` rewritten + new `IUILocalizationService`), `[0.8.0-alpha]` (Group E — Screens + 8 new theme slots + OnUpdate infra dispatch).
- Release evidence (audit baseline + per-version snapshot): `Documentation~/QAReports/_Summary.md` (latest) + `Documentation~/QAReports/v1.0.0/_Summary.md` (versioned).

## [1.0.0-rc.2] — 2026-05-10

> Post-rc.1 hotfix release closing 4 buyer-facing bugs surfaced via in-Editor "fresh install" smoke test (Unity MCP) + user-driven Regenerate+Audit: Bug 2 (`UIAnimPreset` shipped + 3 themes pre-wired so out-of-the-box popups animate without requiring Bootstrap Defaults), Bug 4 (`Catalog_All_Demo.ShowDailyLogin` no longer leaves `RewardEntries = null`), Bug 5 (Catalog tab gains group-filter chips + search box + universal drag-drop — workshop-drawer test pass for the M3c UX audit's findability gap on the Catalog browser), Bug 6 (`CatalogAllDemoBuilder` now wires `UIManager._screenRoot` — pre-existing builder gap latent since rc.1 that broke `PushLoadingScreen` + `PushMainMenuScreen` AllDemo buttons). No public API changes; samples additive only. **Triple gate VERDE post-Bug 6 fix**: 303/303 EditMode tests + UIKit Audit 24/24 (17 prefabs + 7 scenes incl. AllDemo) PASS + AllDemo Play smoke validated.

### Added

- **Catalog tab — group-filter chips + search box + universal drag-drop (Bug 5, post-rc.1 in-Editor smoke test).** Workshop-drawer test failure surfaced when the user opened the rc.1 Catalog tab and could neither narrow the 17-entry grid (no chips, no search) nor drag any non-HUD card to the scene (4/17 entries draggable — Popups/Screens/Toasts gated to "snippet only"). **Fix**: new `kfh-catalog-filter-bar` at the top of the left panel (60% width column) with (a) chip row of `All + A + B + C + D + E` (groups enumerated dynamically from `KitforgeCatalogRegistry` so future Group F adds zero code) — `kfh-catalog-filter-chip--active` highlights the current selection in Hub blue (`rgb(60, 100, 160)` matching the active sidebar tab color), (b) `TextField` search filtering by `DisplayName.IndexOf(query, OrdinalIgnoreCase)` — combined with chip filter via AND. Empty-results state renders a `kfh-catalog-empty-results` italic hint. Filter state persisted in `KitforgeHubState._catalogGroupFilter` + `_catalogSearchQuery` (new `[SerializeField]` strings + setter persistence — survives Hub close/reopen). Drag-drop universalized: `RegisterDragCallbacks` now fires for ALL `KitforgeSpawnPattern` (was `HUD` only). For Popup/Screen/Toast entries the prefab drops as a static preview (no DTO wiring, no manager driving) — detail panel `BuildSpawnInstruction` clarifies semantics: "Drag-to-scene drops a static preview prefab (no DTO, no manager driving) for visual composition; delete it before Play. Use the spawn snippet for production." When the relevant Group sample isn't built the resolver returns null and the drag emits a `Debug.LogWarning` naming the exact `Tools → Kitforge → UI Kit → Build Group X Sample` menu (or Package Manager → Samples import). HUD instruction (auto-bind to services on enable) preserved unchanged. New USS classes: `kfh-catalog-left` (column wrapper, 60% width — replaces direct `width: 60%` on `kfh-catalog-grid-scroll` which now uses `flex-grow: 1` inside the column), `kfh-catalog-filter-bar`, `kfh-catalog-filter-chip-row`, `kfh-catalog-filter-chip` + `--active` modifier, `kfh-catalog-filter-search`, `kfh-catalog-empty-results`. Empirical verification (Unity MCP `execute_code` after compile-clean): filter=B → 5 cells (Reward + Shop + NotEnoughCurrency + HUDCoins + HUDGems); filter=C + search=Daily → 1 cell (Daily Login); filter=All + search=HUD → 4 cells (HUDCoins + HUDGems + HUD Energy + HUD Timer); reset → 17 cells (matches D3-locked catalog count). Bug 5 closes the M3c UX audit's "workshop-drawer findability" gap on the Catalog browser specifically — game designer can now narrow the 17-entry grid to the 1-3 elements relevant to the current week's prototype, and visualize composition by dragging a Popup preview onto a mock screen before writing the spawn host code.

- **`UIAnimPreset_Playful.asset` shipped inside the package + pre-wired into 3 ship themes (Bug 2 Path C fix, post-rc.1 hotfix).** New SO at `Packages/com.kitforgelabs.mobile-ui-kit/Runtime/Animation/Presets/UIAnimPreset_Playful.asset` (stable GUID `7adb85a485d243e09fb65e077ad33e67`); `Theme_Default.asset` + `Theme_Casual.asset` + `Theme_Premium.asset` `_defaultAnimPreset` field cabled to it via that GUID (mirrors the stable-GUID pattern already used by the 3 theme presets themselves). **Why**: rc.1's `### Known issues` documented that out-of-the-box themes had `_defaultAnimPreset = null`, causing every popup spawn to log `[XPopup] No animation preset resolved → popups appear without animation`. Bootstrap Defaults dialog asked the buyer to drag a preset manually — workshop-drawer test failed (extra step the buyer should never see). Path C ships a default preset *inside* the package so OOTB experience is animation-on, while Bootstrap Defaults keeps its role of generating buyer-side customizable presets at `Assets/Settings/UIAnimPresets/`. Asset values mirror the canonical "Playful" preset that `DefaultUIAnimPresetsCreator.CreatePresets` produces (showDuration `0.40` / OutBack ease / hideDuration `0.20` / InQuad ease / scaleOvershoot `1.10`); other 11 fields use the class defaults defined in `UIAnimPreset.cs`. Buyer who wants a different baseline still has the 10-preset library in `Assets/Settings/UIAnimPresets/` and can swap by dragging a different SO onto `Theme._defaultAnimPreset`. **Note about UPM read-only contract**: package-internal `.asset` edits revert on package update — but in this case the asset SHIPS inside the package's `Runtime/Animation/Presets/` and the wiring is on package-internal theme assets that ALSO ship with the package, so there's no buyer-side mutation involved. Buyer customization path (duplicate theme to `Assets/` first, then re-wire) remains unchanged.

### Fixed

- **`Catalog_All_Demo` builder — `UIManager._screenRoot` left null (Bug 6, surfaced by user-run `Regenerate + Audit` post-Bug 5 commit).** `CatalogAllDemoBuilder.WireManagerArrays` cabled `_themeConfig`, `_services`, `_screenPrefabs` (UIManager) and the corresponding root transforms for PopupManager (`_popupRoot`) + ToastManager (`_toastRoot`) — but had no symmetric step for UIManager's `_screenRoot`, leaving the field at `null` in every regenerated `AllDemo.unity`. Latent since rc.1 (sample first shipped 2026-05-10) — never empirically surfaced because the rc.1 smoke test cycle clicked only the 10 popup + 1 toast spawn buttons, never `PushLoadingScreen` or `PushMainMenuScreen`. Real impact at runtime: `UIManager.Push<T>(data)` calls `Instantiate(prefab, _screenRoot)` (Runtime/Core/UIManager.cs:89) — with `_screenRoot = null`, screens parent to the **scene root** instead of `UICanvas/ScreenRoot`, breaking screen layout (no Canvas ancestor → no Graphic raycaster → no UI rendering). `UIKitManagerCheck` fail-fast caught it the first time the AllDemo scene was actually run through the audit (which only happens via the `Regenerate + Audit` toolbar button — the headless `Tools/Kitforge/UI Kit/Audit (Run Headless)` menu doesn't include the `Scenes` dimension by default, which is why the rc.1 smoke + rc.2 first-cut both reported 17/17 PASS without flagging this). **Fix**: `BuildDemoScene` now creates `screenRoot = CreateStretchChild(canvasGO.transform, "ScreenRoot")` analogous to the existing PopupRoot + ToastRoot calls; `BuildManagers` + `WireManagerArrays` accept `screenRoot` as the first transform parameter and call `uiSO.FindProperty("_screenRoot").objectReferenceValue = screenRoot`. Verified empirically post-fix: `_Summary.md` 24/24 pass · 0 fail (was 23/24 · 1 fail with AllDemo failing on `field '_screenRoot' is null`).

- **Test tab — empty-state UX hotfix (post-rc.1 smoke test).** Smoke-testing rc.1 in a fresh empty scene revealed the Test tab rendered all Spawn buttons as enabled-on-Play-mode regardless of whether `KitforgeRoot` existed in the active scene — clicks dumped a rich console error (`[KitforgeTestLauncher] PopupManager not found in active scene → Run Setup → Step 2…`) but the user-facing toast was just `Spawn blocked: no PopupManager`. Workshop-drawer test failed: the buyer in Play mode with empty scene + green "Play mode active — click Spawn on any element below" banner had zero in-Hub guidance to recover. **Fix**: `KitforgeTestLauncher.DetectManagers` polls `FindAnyObjectByType<PopupManager>/UIManager/ToastManager>` on every Refresh; banner now renders 3 states: (a) `--warn` (red) "No KitforgeRoot in active scene" + 3 inline CTA buttons (Add Scene Root via reused `KitforgeSetupWizard.AddSceneRootToActiveScene` static, Open Catalog_All_Demo if `Assets/Catalog_All_Demo/AllDemo.unity` exists on disk, Open Setup tab via new public `KitforgeHubWindow.SwitchToTab`); (b) Edit + KitforgeRoot present → "KitforgeRoot detected — enter Play mode to spawn"; (c) Play + KitforgeRoot present → green "Play mode active" (current). Spawn buttons now `SetEnabled(IsSpawnEnabled(pattern))` checking the per-pattern manager (Popup/Screen/Toast each gate independently — no false-positive when only one of three is present). Refresh subscribes to `EditorApplication.hierarchyChanged` + `EditorSceneManager.sceneOpened` (in addition to existing `playModeStateChanged`) so dropping `KitforgeRoot.prefab` manually in the Hierarchy or opening a different scene updates the banner + button state immediately without forcing a tab switch. New USS classes `kfh-test-banner--warn` + `kfh-test-banner-column` + `kfh-test-banner-actions` + `kfh-test-banner-action` + `kfh-test-banner-hint`. `KitforgeSetupWizard.AddSceneRootToActiveScene` + `TryPingExistingBinder` + `InstantiateNewKitforgeRoot` extracted as `public static` (was instance) to share the path with the Test tab's "Add Scene Root" CTA; `RunStep2` delegates. `KitforgeHubWindow.SwitchToTab(HubTab)` exposed as public delegating to existing private `SwitchTab` so the Test launcher can navigate users to Setup without coupling to internals.

- **`Catalog_All_Demo` — `ShowDailyLogin` host method left `RewardEntries = null` (post-rc.1 in-Editor smoke test).** Sibling bug to the Test tab Force scenario fix below: `CatalogAllDemoHost.ShowDailyLogin` (the buyer-facing AllDemo sample's spawn method, wired to the Sidebar "Daily Login" button) constructed `DailyLoginPopupData` with only `Title` + `CurrentDay = 3` set, leaving `RewardEntries = null`. `DailyLoginPopup.ValidateBindOrAbort` rejected the spawn → popup auto-deactivated → buyer who clicks the sample button sees nothing visually + a `DailyLoginPopup: RewardEntries is null or empty. Bind a non-empty calendar before showing.` console warning. Workshop-drawer test failed: AllDemo IS the kit's "wow moment" (per M3c UX audit + per CHANGELOG `[1.0.0-rc.1]` § Catalog_All_Demo). **Fix**: `ShowDailyLogin` now builds a 7-entry calendar inline (Day N = N×100 coins via `RewardKind.Coins`, Day 7 marked `IsBigReward=true` + `AllowDouble=true`) before passing the DTO to `PopupManager.Show<DailyLoginPopup>`. Same pattern as the `KitforgeMockDtoFactory.ApplyTypeSpecificDefaults` fix below — both paths (Hub Test launcher reflection-spawn AND AllDemo host explicit-spawn) now populate the contract. Both files updated (Samples~/ source + Assets/Samples/.../1.0.0-rc.1/ imported copy per UPM samples blast-radius rule). Verified empirically: post-fix Play mode + `ShowDailyLogin` → popup `activeInHierarchy=true`, console clean, full 7-day calendar renders. Surfaced via Unity MCP smoke test 2026-05-10.

- **Test tab — Force scenario `DailyLogin · Day 1 first claim` aborted with `RewardEntries is null or empty` (post-rc.1 smoke test).** `DailyLoginPopup.ValidateBindOrAbort` (Runtime/Catalog/Popups/DailyLogin/DailyLoginPopup.cs:132) hard-rejects empty `_data.RewardEntries` — and both the regular catalog Spawn (via `KitforgeMockDtoFactory.CreateForComponent`) and the Force scenario `BuildDailyLoginDay1` (via `KitforgeForceScenariosRegistry.ConfigureDailyLoginDay1`) left `RewardEntries = null`. Force scenario only set `Title` / `CurrentDay` / `AlreadyClaimedToday` / `LastClaimUtc`, assuming the factory had pre-populated the calendar; the factory's `ApplySmartDefaults` only handled `Title` / `Subtitle` / `Message` string fields generically. **Fix**: `KitforgeMockDtoFactory.ApplyTypeSpecificDefaults` now dispatches on DTO type — populates `DailyLoginPopupData.RewardEntries` with a 7-day calendar (Day N = N×100 coins, Day 7 marked `IsBigReward=true` + `AllowDouble=true`) and `TutorialPopupData.Steps` with 3 mock steps (preventive — Tutorial doesn't hard-error on empty Steps but renders blank without). Force scenario `ConfigureDailyLoginDay1` keeps its narrow override (Title + CurrentDay=1 + AlreadyClaimedToday=false), the factory's calendar populates underneath. Both regular Spawn AND Force scenario paths fixed by the single factory change.

### Documentation

- **rc.1 Known Issues — RESOLVED via Path C above.** rc.1's `### Known issues` block claimed running `Tools → Kitforge → UI Kit → Bootstrap Defaults` "auto-wires `Theme_Default.DefaultAnimPreset`" — empirically false; `DefaultUIAnimPresetsCreator.EnsureDefaultTheme` only creates `Theme_Default.asset` if missing, never assigns `_defaultAnimPreset`. The v1.0.0 fix path (ship a `UIAnimPreset.asset` inside the package + pre-wire 3 ship themes) is now landed — see the `### Added` entry above. Bootstrap Defaults dialog wording (M5.1 entry of `[1.0.0-rc.1]`) remains valid for buyer-side customization (10-preset library generation in `Assets/Settings/UIAnimPresets/`).

## [1.0.0-rc.1] — 2026-05-10

> First release candidate of v1.0. Closes the M5/RC milestone (M5.1-M5.7 Hub Editor window 5/5 panes shipped + R-U-C3 HUDSimple variants + R-U-C5/C7 phase exit closure post-discovery + Catalog_All_Demo master demo + Quickstart §Manual prefab registration). Public API frozen — only patch fixes between rc.N and final v1.0.0.

### Known issues (rc.1, fix tracked for v1.0.0)

- **Pre-Bootstrap-Defaults state surfaces `[XPopup] No animation preset resolved` warnings on every popup OnShow.** Cause: `UIThemeConfig.DefaultAnimPreset` is `{fileID: 0}` on the 3 themes that ship in the package (`Theme_Default` + `Theme_Casual` + `Theme_Premium`) because the 10 `UIAnimPreset` ScriptableObjects live under `Assets/Settings/UIAnimPresets/` (buyer-side, not `Packages/`) and the package cannot pre-wire references across the boundary. **Workaround**: run `Tools → Kitforge → UI Kit → Bootstrap Defaults` once on first install — the menu item generates the 10 presets + auto-wires `Theme_Default.DefaultAnimPreset`; the 3 warnings per popup spawn disappear and popups animate normally. **Why we ship rc.1 with the warning still firing**: the kit's "wire it in five minutes" contract assumes Bootstrap Defaults as Step 1 of the Quickstart (`README.md § Quickstart` step 1) — making the warning unconditional surfaces the missing-step diagnostic instead of silently shipping animation-less popups in production. Buyers reading the warning know exactly what command to run. **Fix tracked for v1.0.0 final**: wire `DefaultAnimPreset` deterministically across the package/Assets boundary (probably via an `[InitializeOnLoad]` editor hook that lazy-creates the 10 presets at first Hub auto-open, or a `KitforgeRoot` prefab field that points at a package-local fallback preset).

### Added

- **Catalog_All_Demo — single-import master demo (9th sample).** New `Samples~/Catalog_All_Demo/` ships as the workshop-drawer demo for the Monday-morning game designer: ONE scene, ONE builder click, every catalog element wired with buttons-per-element + Theme dropdown. Files: `KitforgeLabs.MobileUIKit.Samples.CatalogAllDemo.asmdef` (refs main runtime + Catalog asmdef + Unity.TextMeshPro), `CatalogAllDemoHost.cs` (~250 LOC MonoBehaviour with 16 buttons + theme dropdown wiring + click handlers calling the canonical `_popupManager.Show<T>(dto)` / `_uiManager.Push<T>(dto)` / `_toastManager.Show<T>(dto)` API), `README.md` (prerequisites + build steps + contract validation script). Editor builder `Editor/Generators/CatalogAllDemoBuilder.cs` validates 13 prefab dependencies + 3 theme presets, builds Canvas + EventSystem + 3 managers (UIManager / PopupManager / ToastManager) wired with `_screenPrefabs[]` (LoadingScreen, MainMenuScreen) + `_popupPrefabs[]` (10 popups: Confirm, Pause, Tutorial, Reward, Shop, NotEnough, DailyLogin, LevelComplete, GameOver, Settings) + `_toastPrefabs[]` (NotificationToast), UIServices with 7 in-memory stubs auto-attached via reflection (Economy + PlayerData + Progression + ShopData + Ads + Time + Localization). Output: `Assets/Catalog_All_Demo/AllDemo.unity` + sidebar of 16 buttons grouped Group A→E + HUD test. Buyer journey: import 5 catalog samples + M4.1 + this sample (Package Manager) → run their builders → run Build Catalog_All_Demo → open scene → Play → click any button → popup spawns + theme dropdown re-skins live across Default/Casual/Premium. Workshop-drawer test passes — the kit is "findable, predecible, reutilizable" in <5 minutes from fresh install. Sample is ADDITIVE; the 8 previous samples remain granular for buyers who want opt-in groups.

- **Quickstart README — §Manual prefab registration added (closes phantom `RegisterPrefab` API hallucination definitively).** New section in `Samples~/Quickstart/README.md` explicitly states there is NO `PopupManager.RegisterPrefab(Type, GameObject)` runtime API + walks the buyer through the canonical Inspector path: drag prefab into `_popupPrefabs[]` / `_screenPrefabs[]` / `_toastPrefabs[]` on the manager component, then call `Show<T>(data)` / `Push<T>(data)`. Step-by-step for "add my own popup" + symmetry note for screens + toasts + validation behavior (silent null return when `T` not in array). Closes the loop on the 4th of 4 API hallucinations captured pre-RC (`feedback_library_api_verification.md`): the buyer-facing path was already correct; the docs surface was missing.

- **R-U-C3 — HUDSimple variant family shipped (no-services, direct-API counterparts to HUDCurrency / HUDEnergy / HUDTimer).** Three new files in `Runtime/Catalog/HUD/` for the Monday-morning game designer who wants to display a coin counter / energy bar / countdown without wiring `IEconomyService` / `IProgressionService` / `ITimeService` first. **`HUDSimple`** (~95 LOC, public `MonoBehaviour`): generic counter HUD with `[SerializeField]` icon Image + TMP_Text label + click Button + `_initialValue` + `_formatString = "N0"` + DOTween punch-on-change (configurable scale + duration; set scale ≤ 1 to disable). API: `public virtual void SetValue(int value)` (animates if `_hasInitialValue`) + `public virtual void SetValueWithoutAnim(int value)` + `public int Value` + `public TMP_Text CountLabel`. **`HUDEnergySimple : HUDSimple`** (~55 LOC, sealed): adds optional `_maxCapLabel` (TMP_Text) + `_energyBarFill` (Image filled type) + `_initialMax` + `_capFormatString = "/{0}"`. API: `public void SetMax(int max)` + `public void SetValueAndMax(int value, int max)` + overrides `SetValue` / `SetValueWithoutAnim` to also update cap label + bar fill. SetMax(0) hides cap+bar. **`HUDTimerSimple`** (~210 LOC, sealed): standalone timer with `HUDTimerSimpleMode` enum (Countdown / Stopwatch) + `_initialCountdownSeconds = 60f` + `_autoplay = true` + `_pausesWithTimeScale` toggle + format validation + expiry color flash via DOTween + `OnExpired` event. API: `public void SetCountdownSeconds(float seconds)` + `public void ResetStopwatch()` + `public void Pause()` + `public void Resume()` (resets `_lastRealTime` to prevent post-pause time jump) + `public bool IsRunning` / `IsExpired` / `RemainingSeconds` / `ElapsedSeconds`. Service-bound `HUDCurrency` / `HUDEnergy` / `HUDTimer` remain canonical; Simple variants do NOT replace them — they coexist for the lower-friction buyer path that doesn't need cross-element synchronization.

- **R-U-C5/C7 Phase exit closure (M5.7 disposition — empirical post-discovery audit).** Pre-RC tag-cut session opened with the close_context premise that R-U-C5 needed a BREAKING `UIThemeConfig` schema change (add `_titleFont`/`_bodyFont`/`_labelFont` slots) and R-U-C7 needed creating `Runtime/Mobile/SafeAreaFitter.cs` from scratch. Empirical filesystem audit (5 reads + 3 greps) invalidated both premises before any code edit:
  - **R-U-C5 multi-font slots — INVALIDATED (mirror R-U-C2 closure 2026-05-09).** `UIThemeConfig` already ships 3 font slots (`_fontHeading` / `_fontBody` / `_fontCaption`) with matching `ThemeFontSlot` enum (`FontHeading` / `FontBody` / `FontCaption`) and `ThemedText.ResolveFont` consumer — surface is functionally identical to the audit's proposed `title`/`body`/`label` naming. Renaming would be cosmetic BREAKING with zero capability gain (capability-gate fail per `feedback_workflow.md`). R-U-C5's "multi-font" sub-item is closed as ALREADY-SHIPPED. Heading/Body/Caption naming preserved (semantic typography scale, not screen-region naming).
  - **R-U-C5 theme presets move — SHIPPED.** `Theme_Casual.asset` + `Theme_Premium.asset` now ship at `Runtime/Theme/Presets/` (alongside `Theme_Default.asset` already there) with the canonical Casual (Royal Match-style bright/saturated) + Premium (Hades-style dark/desaturated) palettes previously generated at sample-build-time by `CatalogM41ThemeBuilder.BuildThemeAsset`. Stable GUIDs: Casual `c8a51ab2b1b25a1ab21ab21ab21ab2c9`, Premium `1a8b6f3b5c7a3b5c7a8a6a3bc9a55a3b`. Buyer no longer needs to import the M4.1 sample to reskin — the 3 presets are first-class package assets.
  - **`CatalogM41ThemeBuilder` refactored — clone-at-build-time logic removed (~50 LOC deleted).** `BuildThemeAsset` / `ApplyPalette` / `ColorPalette` struct / `CasualPalette` / `PremiumPalette` / `Hex` parser deleted; `EnsureFolders` simplified (no more `Themes/` subfolder under `Assets/Catalog_M4_ThemePresets_Demo/`). Builder now `LoadAssetAtPath`s the 3 Runtime presets directly. Single source of truth = the package's authored `.asset` files. Buyer who wants different palettes edits the 3 .asset files directly (or duplicates them) — no need to fork the builder.
  - **R-U-C7 SafeAreaFitter component — CONFIRMED ALREADY-SHIPPED.** `Runtime/SafeArea/SafeAreaFitter.cs` (64 LOC, `OnEnable` apply + `Update` polling for resolution / safe-area changes + 4 directional toggles `_applyTop/Bottom/Left/Right`) was already shipped pre-M5.7 (likely Group 0 Foundation per README § Status row line 21). Roadmap line 710 misnamed the folder as `Runtime/Mobile/`; actual location is `Runtime/SafeArea/`. No Runtime work needed.
  - **R-U-C7 README §Non-goal #7 inversion — APPLIED.** Removed the line `**No automatic safe-area / notch handling beyond \`RectTransform\` anchors.** Use a dedicated package for that.` from `README.md § Non-goals`; renumbered the surviving `**No UI Toolkit support.**` item from #8 → #7. README § Status row already correctly listed `SafeArea` as Group 0 Foundation shipped at `v0.3.0-alpha` — internal contradiction between line 21 (shipped) and line 46 (non-goal) closed in this edit.
  - **Buyer migration from `v0.9.0-alpha` M4.1 sample.** Buyers who imported the M4.1 sample on `v0.9.0-alpha` had Casual/Premium clones generated under `Assets/Catalog_M4_ThemePresets_Demo/Themes/`. After upgrading: (1) re-run `Tools → Kitforge → UI Kit → Build M4.1 — Theme Presets` — the builder rebuilds `ThemePresetsDemo.unity` wired to the package presets in `Runtime/Theme/Presets/`. (2) The old `Assets/Catalog_M4_ThemePresets_Demo/Themes/` folder can be deleted; it's no longer referenced. (3) Buyers who customized the cloned `.asset` files lose those edits on rebuild — port the customization by duplicating the package preset to your `Assets/` folder, applying edits to the copy (UPM packages are read-only by Package Manager spec — edits to files under `Packages/` are reverted on package update), then swap the copy into the `_themes` list of the demo's `Demo` GameObject.

- **M5.6.B — Help tab polish (anchor ToC + clickable markdown links).** `KitforgeHelpTab` now collects `## ` heading elements during render into a `_anchors` list (`HeadingAnchor` readonly struct: `Level + Text + Element`) — click a chip in the new "Jump to section" toolbar at the top of the body and `ScrollView.ScrollTo(anchor.Element)` (UIElements 1.x API, real since Unity 2021.2) snaps the section header into view. Only `## `-level (top-section) anchors render in the chip row to avoid noise from the 4 `### ` subheadings in § 3 Catalog tables; `### ` anchors still get captured for future use (e.g., a deeper ToC sidebar if 2nd consumer asks). Inline link parsing via compiled `LinkRegex = \[([^\]]+)\]\(([^)]+)\)` — paragraph / quote / table-row lines containing markdown links get rebuilt as a flex-row of text segments + clickable `Label` link elements (styled `kfh-help-link` blue + bold + `cursor: link` + hover lighten). `OpenLink(href)` dispatches: (a) `http://` / `https://` → `Application.OpenURL`, (b) relative path (e.g., `Specs/CATALOG.md` from § 7 "Going deeper") → resolved via `Path.GetFullPath(Path.Combine(docsRoot, href))` against the cached `_packageResolvedPath` + opened with `EditorUtility.OpenWithDefaultApp` if the file/directory exists on disk, else `Debug.LogWarning` naming the missing target. Link `tooltip` shows the resolved href on hover. Targets: 5 inline links in CHEATSHEET (1 in line 3 blockquote → `Specs/CATALOG.md` + `Specs/Catalog/`; 4 in § 7 → `Specs/CATALOG.md`, `Specs/Catalog/`, `Specs/Services/`, `CHANGELOG.md` references). The 4 catalog-table rows in § 3 (Popups/Toasts/Screens/HUDs) don't contain link tokens (key DTO fields are `code`-style spans, not links), so they render unchanged via the same flex-row path with link-count = 0 → fast-path returns plain Label.

- **M5.7 — Verify-all-docs gate (pre-RC truth pass).** Caught + fixed 2 hallucination drifts post-Hub-shipped: (a) `README.md` Non-goal #6 still claimed "**No Editor authoring window.** No visual screen graph, no drag-and-drop registry. Inspector + prefab references only" — directly false post-Hub. Reworded to "**No visual screen-graph editor.** No node-based flow authoring, no drag-and-drop screen registry. Inspector + prefab references only — but the kit ships a single-front-door **Kitforge Hub** (`Tools → Kitforge → Hub`, UI Toolkit) covering Setup wizard / Catalog browser / Theme Studio / Test launcher / inline Cheatsheet. The Hub authors *scene state* (drop `KitforgeRoot.prefab`, scan themes) — not screen graphs." Preserves the original kit constraint (no node-graph authoring) while accurately surfacing the shipped Hub. (b) `KitforgeHubWindow.BuildPaneFor` wired all 5 tabs to real implementations across M5.2.A → M5.6.A but the dead `BuildPlaceholderPane` + `GetTabSubtitle` + `GetTabMilestone` helpers still lived in the file with stale "Coming in M5.X" strings (unreachable since BuildPaneFor handles all 5 tabs explicitly, but a future maintainer reading the source would treat the strings as ground truth). Deleted the 3 dead methods; the unreachable fallback now `Debug.LogError`s a kit-author bug message instead of silently rendering a lying placeholder. **Tag-cut deferrals (do at `git tag v1.0.0-rc.1`, NOT in this gate to avoid version drift):** bump `package.json.version` from `0.9.0-alpha` to `1.0.0-rc.1` + extend `package.json.description` with " + in-Editor Hub for designer workflow" + add `"editor"` / `"hub"` keywords + add a Status table row in `README.md` for `v1.0.0-rc.1` (Hub + 17 catalog complete + 5/5 panes shipped). Unity smoke test (Hub auto-open from clean install + Setup wizard 3 steps + Catalog drag-drop + Theme dropdown + Test launcher Play-mode + Help tab render) deferred per "ya haremos tests en Unity más adelante" — captured in parking lot for next session pre-tag.

- **M5.6.A — Help tab + CHEATSHEET inline render (Hub closes 5/5 panes).** New `Editor/Hub/Help/KitforgeHelpTab.cs` resolves `Documentation~/CHEATSHEET.md` via `UnityEditor.PackageManager.PackageInfo.FindForAssembly(typeof(KitforgeHelpTab).Assembly).resolvedPath` (mandatory because `~`-suffixed folders are excluded from `AssetDatabase` so `LoadAssetAtPath<TextAsset>` would silently return null) + `System.IO.File.ReadAllText` + line-by-line markdown classifier (`# / ## / ### / > / --- / | / ```` / paragraph / spacer`). Toolbar exposes 3 quick actions: **Open in editor** (`EditorUtility.OpenWithDefaultApp` on the resolved absolute path — gives game designer the markdown source in their preferred editor for copy/paste of snippets), **Run Audit** (`EditorApplication.ExecuteMenuItem("Tools/Kitforge/UI Kit/Audit")` — same green-gate ritual the audit window owns), **Build Group A Sample** (`Tools/Kitforge/UI Kit/Build Group A Sample` — workshop-drawer-correct shortcut to the most-trafficked sample for first-time onboarding). Code fences rendered in dark monospace box (`kfh-help-code-block`), tables rendered as raw `|`-separated rows (default font — designer-readable in narrow Hub width without an embedded markdown table parser, deferred until 2nd consumer). Missing-file fallback shows actionable warning naming the expected `<package>/Documentation~/CHEATSHEET.md` location (verify install integrity). `KitforgeHubWindow.BuildPaneFor` wires `HubTab.Help → new KitforgeHelpTab().Build()` (was placeholder pre-M5.6.A). Closes the M5.X cluster's last placeholder pane: Hub now ships 5/5 functional tabs (Setup ✅ M5.2.A · Catalog ✅ M5.3.A-D · Theme ✅ M5.4.A · Test ✅ M5.5.A-C · Help ✅ M5.6.A).

### Changed

- **M4.X — Unified audit tool (`Editor/Audit/`) replaces dual legacy stack (Path-A clean migration).** Deleted `Editor/QA/` (8 files: `QASuiteWindow` + `QASuiteRunner` + `RebuildSamplesBuilder` + `IntegrityScanRunner` + `IQAIntegrityCheck` + 3 checks `MissingReference`/`MissingScript`/`UIKitManager`) and `Editor/QA/Catalog/` (15 files: `CatalogAuditWindow` + `CatalogAuditService` + `CatalogAuditDiscovery` + 6 checks + `PrefabSnapshotCapture` + report writers + tests) — replaced with a single coherent pipeline under `Editor/Audit/` rooted at `Tools → Kitforge → UI Kit → Audit`. Unified data model: `UIKitAuditDimensions [Flags]` (`Catalog | PrefabStructural | Scenes | Visual | All`), `UIKitCheckScope {Scene,Prefab,Catalog,Visual}`, `UIKitAuditTarget` descriptor (Kind + AssetPath + Group + ComponentType + IsCatalog), `UIKitAuditFinding [Serializable]` (CheckName + Scope + Severity + Message + ChildPath + ComponentType), `UIKitAuditTargetReport` + `UIKitAuditRunReport` with breakdown counts (CatalogPass/Total + ScenePass/Total + PrefabPass/Total). Pluggable `IUIKitCheck` interface auto-discovered via `TypeCache`. 7 checks ship: `PrefabRefsCheck` (iterates `_refs` SerializedProperty struct, flags unassigned/broken refs), `PrefabThemeReactivityCheck` (counts `ThemedImage`/`ThemedText` + validates backing field wiring), `PrefabSpecMarkdownCheck` (parses `Documentation~/Specs/Catalog/*.md` `## Theme tokens consumed` section against actual themed counts — order-independent: computes themed counts inline, NOT from sibling check state — fixes a TypeCache execution-order non-determinism bug observed pre-cleanup), `MissingReferenceCheck` + `MissingScriptCheck` + `UIKitManagerCheck` (ported from legacy QA, native to new contract), **`ThemedFieldsWiredCheck` (NEW — fails Error if `ThemedImage._image` / `ThemedText._text` is null; closes the silent-no-op bug class that motivated the sweep — Group A pre-fix had 4 prefabs with broken `ThemedText` wiring invisible to visual inspection)**. Window has 6 tabs (Overview / Prefabs / Scenes / Snapshots / Reports / Settings) + toolbar with Discover / Run Everything / Run Catalog / Run Scenes / Run Prefab Structural / Capture Snapshots / Clean Snapshots / Clear Reports / Clear All / Reveal Reports + Show filter (All / FAIL / PASS) + summary label. Reports written to `Library/UIKitAudit/Reports/` (ephemeral, gitignored) with opt-in mirror to `Assets/Editor/QAReports/` (versioned for release evidence). Snapshots captured at 1080×1920 PNG via headless `RenderTexture` in scratch scene, hashed SHA256, persisted to `Assets/Editor/QASnapshots/` (always versioned, vision-readable for AI-assisted review). Headless mode: `[MenuItem("Tools/Kitforge/UI Kit/Audit (Run Headless)")]` returns exit `0` (pass) / `1` (fail) / `2` (error) for CI integration. Replaces previous `tool_qa_suite.md` memory entry (now dead knowledge).

- **M4.X Stage 2 — text-theming sweep across Groups B/C/D/E (~36 callsites migrated, +51 themed counts).** `CatalogGroupBuilderShared` extended with `TextThemeSlot` readonly struct + `ThemeBuilderSlots` static class (9 named tuples: `Heading`, `Body`, `BodyPrimary`, `Caption`, `PrimaryButtonLabel`, `SecondaryButtonLabel`, `BannerLabel`, `DarkBgHeading`, `DarkBgBody`) + `CreateThemedText(parent, name, text, size, style, slot)` helper that combines `CreateText` + `AddThemedText` with design-time fallback color (`TextLightColor` for `TextOnPrimary` / `TextOnAccent` slots, `TextDarkColor` otherwise — matches what `ThemedText.ApplyTheme` will produce at runtime, eliminating editor-vs-runtime visual delta). Migrations applied: Group B 10 sites (RewardPopup×4, ShopPopup+cell×5, NotEnoughCurrencyPopup×5, HUDShell×1), Group C 15 sites (DailyLogin×3, LevelComplete×7, GameOver×7, HUDEnergy×1, HUDTimer×1; HUDCapLabel/RegenLabel/Countdown skipped — semantic-color slot decisions deferred to M4.X cluster), Group D 5 sites (SettingsPopup title + closeLabel + `CreateRow` helper + Dropdown caption + Template item), Group E 6 sites (LoadingScreen title/subtitle + MainMenu title + 4 menu buttons + InstructionsPanel — all with `DarkBg*` slots since screens render on `BackgroundDark`; Group E's local `CreateText` deleted, local `CreateLabel` accepts a `TextThemeSlot` param). Group A intact (already migrated pre-session). Net themed text counts elevated +51 across B/C/D/E (47 → 98 themed components). **Q-decisions locked from `/_checker as triple` round 4** (user + pm + dev convergent verdict on slot mapping): **Q1.A** secondary button labels → `TextPrimary` (preserves dark-on-light visual on Default; reskins coherently on Premium); **Q2.A** HUDs included in Stage 2 with `DarkBgBody` slot for count labels (HUDCurrency / HUDEnergy / HUDTimer now reactive — eliminates the 2 standing warnings); **Q3.C** Group E text on `BackgroundDark` → `TextOnPrimary` (originally proposed `TextOnAccent` rejected after reading Group E builder — bg slot is `BackgroundDark` not `AccentColor`); **Q4.A** semantic colors `HUDCapLabelColor` + `WarningColor` (energy regen countdown, daily countdown) skipped — out of narrow Stage 2 scope, deferred to M4.X cluster pending dedicated `WarningText` slot decision; **Q5.B** refactor `CreateText` → `CreateThemedText` helper (rejected initial path-a "post-call `AddThemedText` override" — invasion menor + 1 helper used everywhere = readability win; replaces ~36 sites of duplicated 2-call pattern). `ThemedText.TryResolveColor` extended with 6 missing slots (`FailureColor`, `LoadingBarColor`, `TertiaryColor`, `MutedColor`, `TextOnPrimary`, `TextOnAccent`) for full parity with `ThemedImage.TryResolveColor` (16-slot coverage). **Symmetry fix:** `AddThemedText` in `CatalogGroupBuilderShared` now wires the `_text` SerializedProperty (was previously asymmetric vs `AddThemedImage` which always wired `_image` — silent `ApplyTheme` no-op for 4 prefabs in Group A pre-fix; the bug class is now locked closed by the new `ThemedFieldsWiredCheck` audit check above). **Final audit: 23/23 pass · 0 fail · 0 warn** (was 23/23 pass · 0 fail · 2 warn pre-sweep — HUDCurrency + HUDTimer baseline warnings eliminated by Q2.A inclusion).

- **M4.X — Pre-tag release evidence ritual + `Documentation~/QAReports/` folder.** Clarified the asymmetry between buyer-side and developer-side audit evidence: `MirrorReportsToAssets` toggle (audit window Settings tab) writes to `Assets/Editor/QAReports/` of the **consumer Unity project**, which is where buyers commit their own audit evidence in their game project. The package's own developer Unity project (currently `H:/==GIT==/PACHINKO/PACHINKO/`) is not git-tracked, so its `Assets/Editor/QAReports/` cannot serve as release evidence for the package itself. New folder `Packages/com.kitforgelabs.mobile-ui-kit/Documentation~/QAReports/` (inside the package's own git repo, `~` suffix excludes from Unity asset processing in buyer installs so zero footprint impact) hosts the developer-side release evidence: `_Summary.md` (latest baseline, overwritten each tag) + `_README.md` (documented ritual: `Clear All` → `Regenerate + Audit` → confirm green → copy `Library/UIKitAudit/Reports/_Summary.md` to `Documentation~/QAReports/_Summary.md` (and optionally a versioned subfolder `v<tag>/_Summary.md` for historical evidence) → commit → tag). Baseline committed today (M4.X cluster close): 23/23 pass · 0 fail · 17 catalog + 6 scenes + 17 prefabs (DailyLogin themed=7 post-#2.A WarningText, HUDEnergy themed=3 post-#2.A, HUDGems themed=1 post-#1 dedup fix). Buyer-facing `MirrorReportsToAssets` toggle stays — its purpose (buyer evidence in their consumer project) is orthogonal to the developer-side evidence in this folder.

- **M4.X — WarningText callsite migrations + `WarningBody` slot + `WarningColor` design-time fallback.** Q4 deferral (M4.X Stage 2 close) reframed via Decision Protocol step 4 (Contrarian): the proposed dedicated `WarningText` slot was not necessary — the 2 raw `text.color = WarningColor` callsites in `CatalogGroupCBuilder` (`DailyLoginPopup` already-claimed countdown line 99-108, `HUDEnergy` regen countdown line 433-441) can consume the **existing** `ThemeColorSlot.WarningColor` enum value through `ThemedText`, with no new slot, no new color decision across Default/Casual/Premium palettes, and no new arm in `ThemedText.TryResolveColor`. Migration: `CatalogGroupBuilderShared` gains 1 constant (`WarningColor = (1.00, 0.60, 0.15, 1)` matching `Theme_Default._warningColor` for design-time editor preview), 1 case branch in `CreateThemedText` (`WarningColor` → orange fallback before `AddThemedText`, mirroring the existing `TextOnPrimary`/`TextOnAccent` → `TextLightColor` branch), and 1 named `TextThemeSlot` entry in `ThemeBuilderSlots` (`WarningBody` = `FontBody` + `WarningColor` + `None`). `CatalogGroupCBuilder.cs` consumes Shared's constant via `using static` (deletes its local divergent `WarningColor (0.95, 0.62, 0.10)` const — single-source-of-truth aligned with `Theme_Default._warningColor`). 2 callsites refactored from `CreateText` + `text.color = WarningColor` to `CreateThemedText(... ThemeBuilderSlots.WarningBody)` (themed counts +2: DailyLogin +1, HUDEnergy +1). Both texts now reskin live across Default/Casual/Premium via `ThemedText.ApplyTheme`. **Out of scope this fix:** `HUDCurrency.MaxCapLabel` ("/5" suffix) — currently raw `text.color = HUDCapLabelColor (0.85, 0.85, 0.90)` light gray; HUD shell is intentionally non-themed (`HUDBackgroundColor` semi-transparent black, no semantic theme slot). Migrating this label would require either (a) inventing a `MutedOnDark` slot (speculative — only 1 consumer, violates §5 promote-when-2nd-consumer), or (b) reusing `TextSecondary` slot which is designed for dark-on-light (Δ ~0.4 on dark bg → readability regression). Decision: leave inline for HUD chrome, revisit if 2nd consumer surfaces or M3c UX audit flags. **Validation pending:** `Regenerate + Audit` → 23/23 pass + themed counts +2 (DailyLogin → 7, HUDEnergy → 3).

- **M4.X — Runtime theme swap API on managers + `GroupE_BootDemo` theme dropdown (manual swap E2E coverage for screens).** New `public void SetTheme(UIThemeConfig theme)` shipped on `UIManager` + `PopupManager` + `ToastManager`. Implementation re-applies theme to cached instances by re-running `UIModuleBase.Initialize(theme, services)` on every entry of `_screenCache` / `_popupCache` (toast manager has no cache — toasts are transient `Instantiate`/`Destroy` per spawn, so `SetTheme` only updates the field for future spawns). `Initialize` is idempotent in shipped catalog: zero subclasses override it (verified via grep `override\s+void\s+Initialize\s*\(`), so re-call resets `Theme`/`Services` properties + walks `IThemedElement` children via existing `ApplyThemeToChildren` — no state-reset side effects on cached screens (LoadingScreen progress / MainMenu stagger / popups state preserved). Consumers can swap theme at runtime in production (settings menu, theme picker, A/B testing). Net runtime API surface: 3 additive methods, zero breaking changes. New sample component **`ThemeSwitcherEScreens`** in `Samples~/Catalog_GroupE_Screens/` (same asmdef as `GroupEDemoHost`) — TMP_Dropdown-driven swap that calls `SetTheme` on all 3 managers in sequence on `onValueChanged`. Diverges intentionally from `M4.1 ThemeSwitcherSample`'s `Instantiate + Initialize + OnShow` bypass pattern (that pattern remains the canonical sample for popup-only theme demos per `feedback_popup_spawn.md` documented exception): screens are persistent in `_screenCache` so `Initialize`-bypass is unnecessary and would duplicate `UIManager`'s logic. **`CatalogGroupEBuilder.BuildDemoScene` extended:** `BuildUICanvas` signature now returns the canvas Transform (1-tuple element added) so `BuildThemeSwitcher` can reach the canvas root; new `BuildThemeSwitcher(scene, canvasRoot, ui, pm, toast)` step builds a `TMP_Dropdown` via `TMP_DefaultControls.CreateDropdown` (anchored top-right, 360×80 at offset (-24, -200) below the existing `Instructions` panel), creates a `ThemeSwitcher` GameObject hosting the new sample component, and wires `_uiManager` / `_popupManager` / `_toastManager` / `_themeDropdown` / `_themes` (Default + Casual + Premium) via `SerializedObject`. `Theme_Casual.asset` + `Theme_Premium.asset` loaded from `Assets/Catalog_M4_ThemePresets_Demo/Themes/` (cross-sample reuse — established by M4.1 buyer-imports-both pattern); fallback when M4.1 sample not generated: dropdown contains only "Default" entry + `Debug.LogWarning` instructing the buyer to run `Build M4.1 — Theme Presets` first. Closes the M4.X cluster item "GroupE_BootDemo theme dropdown — falta paridad con `ThemePresetsDemo` para E2E manual swap en screens" — buyer presses Play, dropdown swap on `LoadingScreen` shows progress-bar fill + bg + spinner reskin live; on `MainMenuScreen` shows panel bg + Play/Settings/Shop/Daily buttons + DailyIndicator dot reskin live; on `DailyLoginPopup` (when triggered via right-click → `Trigger DailyLogin`) — first spawn after theme swap uses new theme via `PopupManager._themeConfig`; if popup is already open during swap, `_popupCache` re-`Initialize` re-skins it live. Validation pending: `Tools → Kitforge → UI Kit → Audit → Regenerate + Audit` → 23/23 (no audit count change — ThemeSwitcher is a scene-level non-prefab UI element); `GroupE_BootDemo.unity` Play → dropdown swap Default → Casual → Premium → screens visibly reskin without scene reload.

- **M5.1 — `Theme_Default.asset` shipped in package at `Runtime/Theme/Presets/` (premise for KitforgeRoot prefab).** Path moved from buyer's `Assets/Settings/UI/UIThemeConfig_Default.asset` to package-internal `Packages/com.kitforgelabs.mobile-ui-kit/Runtime/Theme/Presets/Theme_Default.asset`. Constant `DefaultThemePath` updated in 7 files (`DefaultUIAnimPresetsCreator.cs` + `CatalogGroupABuilder.cs` + `CatalogGroupBBuilder.cs` + `CatalogGroupCBuilder.cs` + `CatalogGroupDBuilder.cs` + `CatalogGroupEBuilder.cs` + `CatalogM41ThemeBuilder.cs`). Bootstrap Defaults menu (`Tools → Kitforge → UI Kit → Bootstrap Defaults`) keeps generating the 10 `UIAnimPreset` SOs in buyer's `Assets/Settings/UIAnimPresets/`, but now creates `Theme_Default` at the package path on first run + commits to package source (one-shot for the kit author; subsequent buyers get the asset shipped). `EnsureFolders()` ensures `Runtime/Theme/Presets/` folder via `AssetDatabase.CreateFolder`. `Casual` + `Premium` presets stay sample-generated (`Assets/Catalog_M4_ThemePresets_Demo/Themes/`) — promotion to package source deferred to M5.4 (Theme Studio tab — when dropdown enumeration via `AssetDatabase.FindAssets("t:UIThemeConfig", "Runtime/Theme/Presets")` requires it).
- **M5.1 — Bootstrap Defaults dialog rewording.** Final dialog message changed from "Assign this Theme to your UIManager / PopupManager / ToastManager" (the U-A.3 pain point) to "Drop KitforgeRoot.prefab in your scene — Theme is already wired via KitforgeThemeBinder" (forward-compatible with R-U-C1 KitforgeRoot.prefab landing same milestone). Sample `Catalog_M4_ThemePresets/README.md` § Prerequisites updated: `Theme_Default` ships with the package (no Bootstrap Defaults required).

- **M4.X — One-click validation flow + builder headless mode + M4.1 added to regen sequence (M4.X follow-up).** Each `CatalogGroup{A,B,C,D,E}Builder` and `CatalogM41ThemeBuilder` exposes `public static bool BuildAllForAudit()` (silent, no `EditorUtility.DisplayDialog` calls, returns `bool` for downstream chaining); `[MenuItem]` `BuildAll()` preserved as the interactive entry point delegating to a private `BuildAllInternal(bool interactive)` core. `EnsureThemeAvailable(bool interactive)` / `CheckDependencies(bool interactive)` extracted per builder — `interactive=true` shows the existing "Bootstrap Defaults missing" dialog with Proceed/Cancel; `interactive=false` emits `Debug.LogError` with the same guidance and returns `false`. Group E adds the same gate to `CheckTheme` + `CheckGroupCBuilt` (the latter validates `DailyLoginPopup.prefab` exists before MainMenu wiring); M4.1 honors the same gate via `CheckDependencies` validating both Default theme + Group C prefabs (GameOver + LevelComplete). Audit window adds 2 toolbar buttons: **Regenerate + Audit** (one-click full validation: A→E + M4.1 sequential rebuild with progress bar + immediate Run Everything pass) and **Regenerate Samples** (rebuild only, no audit follow-up). Order is fixed (A→B→C→D→M4.1→E) to honor cross-group dependencies: Group C wires Group B's `RewardPopup`; M4.1 wires Group B + Group C stub services + GameOver / LevelComplete prefabs; Group E wires Group C's `DailyLoginPopup` AND M4.1's `Theme_Casual.asset` / `Theme_Premium.asset` (M4.1's `BuildThemeAsset` does `DeleteAsset + CopyAsset` each run → new GUIDs every regen → E must run AFTER M4.1 to wire fresh GUIDs into `ThemeSwitcherEScreens._themes[1]/[2]`, otherwise E captures old GUIDs that M4.1 then invalidates → broken refs in saved scene; surfaced 2026-05-09 when first naive A→E→M4.1 sequence produced FAIL on `GroupE_BootDemo.unity`, fixed by sliding M4.1 between D and E). M4.1 was initially excluded from the regen sequence (M4.X first cut shipped A→E only) — gap surfaced when sample re-import in Package Manager invalidated `ThemeSwitcherSample.cs` GUID, leaving `ThemePresetsDemo.unity` with broken refs that survived the next `Regenerate + Audit` because the M4.1 builder never ran. Now any sample-reimport recovery is a single click (M4.1 builder regenerates the scene with current GUIDs as part of the sequence). Replaces the pre-session ritual (4 manual `Build Group X Sample` menu clicks + 1 `Run Everything` click = 5 clicks) with 1 click; absorbs the `as user` `Friction` finding (U3) from the Stage 2 `/_checker as triple` round 3 stress-test. **Clear All** toolbar button added — wipes reports + mirror + snapshots in one confirmed action (replaces 3 separate clicks: Clear Reports + Clean Snapshots + manual mirror cleanup).



> Path-to-`v1.0.0-rc` re-sequenced 2026-05-06: `M3a ✅ → M4 (this) → M3c UX audit → M5/RC tag`. M4 closes when all 12 sub-steps land (8 mandatory + 4 capability-gated); single tag at end (no intermediate version bumps). M3b polish deferred post-1.0 pending foundational review (see `[0.8.1-alpha]` block below).

### Fixed

- **M4.X — Audit catalog discovery dedup by component-type.** `UIKitAuditDiscovery.FindCatalogPrefabFor(Type)` returned a single prefab path even when multiple prefabs in the catalog shared the same `MonoBehaviour`-derived type. Concrete repro: `HUDCoins.prefab` and `HUDGems.prefab` both consume the `HUDCurrency` component (no separate `HUDGems` type) — discovery picked the first match (catalog target with `IsCatalog=true`) and the second prefab fell through to `DiscoverNonCatalogPrefabs` as a non-catalog target, where `PrefabThemeReactivityCheck.AppliesTo` filters on `IsCatalog` and skipped the check, reporting `themed=0` regardless of actual `ThemedImage`/`ThemedText` wiring. Pre-existing v0.9 behavior, not Stage 2 regression. Refactor: `FindCatalogPrefabFor(Type)` → `FindCatalogPrefabsFor(Type)` returning `List<string>` (paths sorted `OrdinalIgnoreCase` for cross-machine determinism). `TryCollectCatalogType` iterates all matching paths and adds one catalog target per `(Type, prefabPath)` pair via new `AppendCatalogTarget` helper. Label disambiguation: when `prefabPaths.Count == 1` the label remains `type.Name` (zero baseline churn for the 16 existing 1:1 catalog targets); when `>1` the label becomes `Path.GetFileNameWithoutExtension(prefabPath)` so the two HUD targets surface as `HUDCoins` + `HUDGems` (not `HUDCurrency` + `HUDCurrency` collision — `Label` is the file stem for snapshot PNG, per-target md/json report, and `FindReport` lookup key). `TypeShortName` retained on both targets so `PrefabSpecMarkdownCheck` continues to resolve `Documentation~/Specs/Catalog/HUDCurrency.md` for both. Net dimension counts: catalog 16→17, non-catalog 1→0, total prefab + scene targets unchanged at 23. Stale orphan reports `Library/UIKitAudit/Reports/HUDCurrency.md` + `HUDGems.md` (formerly non-catalog) cleared on next `Regenerate + Audit` (idempotent rewrite). Validation pending: `Tools → Kitforge → UI Kit → Audit → Regenerate + Audit` should produce two `HUDCurrency`-typed catalog targets each reporting `themed > 0` for their respective prefab; non-catalog dimension drops to 0 entries.

- **M4.7 — ThemedImage runtime read in Group C builder.** `ThemeColorSlot` enum extended (append-only, preserves serialized values) with the 6 missing slots: `FailureColor`, `LoadingBarColor`, `TertiaryColor`, `MutedColor`, `TextOnPrimary`, `TextOnAccent`. `ThemedImage.TryResolveColor` switch covers all 16 slots. `CatalogGroupCBuilder.cs` migrated: new helpers `AddThemedImage(go, color, slot)` + `OverrideThemedImageSlot(go, slot)`; `CreateCard` wires `BackgroundLight`; `CreateButton` accepts a slot param (`CreatePrimaryButton` → `PrimaryColor`, `CreateSecondaryButton` → `None` per palette mismatch — see deferred list below); DailyLogin claim button override → `SuccessColor`; LevelComplete NEW BEST banner → `SuccessColor`; GameOver `HeaderTint` → `FailureColor`; HUDEnergy bar fill → `SuccessColor`. Theme dropdown swap in `Catalog_M4_ThemePresets/ThemePresetsDemo.unity` now visibly reskins cards, Primary buttons, GameOver header tint, LevelComplete banner, and HUDEnergy bar across Default / Casual / Premium. Closes the deferred-validation gap M4.1 shipped with. **Out of scope, deferred M4.7-bis (mandatory pre-tag `v0.9.0-alpha`):** Secondary buttons (palette mismatch — `Theme_Default._secondaryColor (0.35, 0.40, 0.50)` vs builder constant `ButtonSecondaryColor (0.85, 0.86, 0.90)`, Δ 0.5; reconciliation requires cross-theme palette decision); HUD backgrounds (no semantic theme slot); sprite icons (sprite-driven, not color-driven); Groups A / B / D / E builders (same migration sweep + helper extraction to `CatalogBuilderBase` — composability criterion validated with 5 consumers).
- **`CatalogM41ThemeBuilder.BuildDemoScene` post-`NewScene` asset reload.** Builder previously loaded `Theme_Casual.asset` + `Theme_Premium.asset` references in `BuildAll()` BEFORE `EditorSceneManager.NewScene` fired in `BuildDemoScene`. The `NewScene` call invalidates in-memory asset references created in the same call (the Casual + Premium assets had just been generated via `AssetDatabase.CopyAsset` + `LoadAssetAtPath` and were not yet stably indexed when the scene started serializing). Result: `WireThemeOption(...).objectReferenceValue = casual` serialized as `{fileID: 0}` in the scene YAML; `_themes[1]` and `[2]` shipped null in the wired `ThemeSwitcherSample` component. Dropdown swap to Casual/Premium silently set `_activeTheme = null` and `UIModuleBase.Initialize(null, ...)` early-returned without applying the theme — buyer saw the same off-white card / bright red HeaderTint regardless of dropdown selection (build-time hardcoded colors persisting). Default theme survived because it was loaded once at session start with a stable `AssetDatabase` index pre-modifications. Fix: move `LoadAssetAtPath` for all 3 themes INSIDE `BuildDemoScene` AFTER `NewScene`, so refs are obtained against the live `AssetDatabase` state at scene-serialization time. Generic pattern: any builder that creates assets via `CopyAsset` / `CreateAsset` and then opens a new scene to wire those assets must re-load by path post-`NewScene`. Captured in `feedback_workflow.md` → "AssetDatabase ref lifecycle around `EditorSceneManager.NewScene`".
- **`GameOverPopup.ApplyButtonAlpha` no longer overwrites theme-applied button color.** Method previously set `button.image.color = FullColor (white) | GrayedColor (white α 0.5)`, killing the RGB channel set by `ThemedImage.ApplyTheme`. Refactored to read the current `image.color` and modify only the alpha channel (`c.a = full ? 1f : 0.5f`). Static readonly `FullColor` / `GrayedColor` constants removed (unused after refactor). Result: `ContinueAdButton` Primary slot now reskins correctly across themes while preserving the alpha-grayed state when the rewarded ad is unavailable. Sibling pattern check across `Runtime/Catalog`: clean — no other catalog popup has the same `image.color = staticColor` runtime override (verified via `\.image\.color\s*=` grep). Generic pattern captured in `feedback_workflow.md` → "Runtime overrides silently kill theme contract — verify both build-time wiring AND runtime overrides when validating skin-it-once".
- **M4.7-bis.A — Group A ↔ Group C contract reconciliation.** Post-M4.7 discovery surfaced that `CatalogGroupABuilder` was already migrated to `ThemedImage` runtime read at an earlier session, but with a divergent helper signature `AddThemedImage(go, ThemeSpriteSlot, ThemeColorSlot)` void (vs Group C M4.7 `AddThemedImage(go, Color, ThemeColorSlot)` returns Image). Both builders refactored to a canonical 4-arg signature with explicit `_image` SerializedObject wiring. Architectural decisions resolved this milestone: **D1 Helper signature canonical** = `AddThemedImage(GameObject go, Color fallbackColor, ThemeSpriteSlot spriteSlot, ThemeColorSlot colorSlot)` returns Image (replaces both legacy signatures). **D3 `_image` field wiring** = explicit via SerializedObject (the M4.7 close_context claim "AddThemedImage MUST set _image AND _colorSlot" was correct in spirit; Group C already implemented it; Group A previously relied on Awake `GetComponent<Image>()` auto-resolve fallback — now aligned). **D4 Tutorial `stepImage`** `CatalogGroupABuilder.cs:252` migrated to `ThemeColorSlot.BackgroundLight` (was hardcoded gray `(0.85, 0.86, 0.90)`, jarring in Premium dark theme). **D7 Group A Secondary divergence resolved by reverting** `CreateSecondaryButton` to `slot=None` matching Group C's deferral pattern. Caveat: Pause-popup `CreateThemedLabelledButton` callsites with `ThemeColorSlot.SecondaryColor` (5 buttons `CatalogGroupABuilder.cs:178-182`: Restart, Settings, Home, Shop, Help) NOT reverted in this milestone — same Secondary palette issue but uses a different helper; deferred to M4.7-bis.B inline-decide alongside D6. **D2 Secondary cross-theme palette reconciliation** deferred to M4.X cluster (Default `_secondaryColor (0.35, 0.40, 0.50)` Δ 0.5 vs builder hardcoded fallback `(0.85, 0.86, 0.90)`; cluster includes `_defaultAnimPreset` null + Bootstrap Defaults palette research). **D5 Loading/MainMenu screen backgrounds** themed-or-not decision deferred to M4.X (Group E sweep uses placeholder slot until decided). **D6 Group B header tints semantic mapping** (ClaimTint→`SuccessColor`, NotEnough.HeaderTint→`WarningColor`) deferred to M4.7-bis.B sweep inline-decide. **Theme_Default `_backgroundLight`** reset from `(0.49, 0.49, 0.49)` corrupt-gray to `(0.96, 0.97, 0.98)` off-white matching C# class default — root cause: manual inspector edit pre-M4.1 (asset predates M4.1 slot additions; non-deterministic float + all-channels-equal grayscale signature = slider drag pattern; Casual + Premium asset values intact prove builder didn't propagate corruption). M4.7-bis.B (next sessions): sweep `CatalogGroupBBuilder` / `CatalogGroupDBuilder` / `CatalogGroupEBuilder` with canonical helper + extract `internal abstract class CatalogGroupBuilderBase` (composability §5 now validated with 2 aligned consumers).
- **M4.7-bis.A.bis — Group C primary-button wrapper sprite-slot reconciliation (path b — extend C to A canonical).** Pre-extract audit for `internal abstract class CatalogGroupBuilderBase` (M4.7-bis.B.1) surfaced a wrapper-level divergence missed by M4.7-bis.A: the `AddThemedImage(go, Color, ThemeSpriteSlot, ThemeColorSlot)` helper itself is byte-identical between `CatalogGroupABuilder` (line 549) and `CatalogGroupCBuilder` (line 720), but their `CreatePrimaryButton` wrappers diverge. Group A wires `(ButtonPrimaryColor, ThemeSpriteSlot.ButtonPrimary, ThemeColorSlot.PrimaryColor)` per the project-local "Themed* contract" convention (`Packages/.../MEMORY.md` line 14: "Every primary button → `ThemedImage(ButtonPrimary, PrimaryColor)`"); Group C M4.7 silently dropped the sprite slot to `ThemeSpriteSlot.None` via a 4-arg `CreateButton(parent, name, color, slot)` overload that hardcoded `_spriteSlot=None`. Result: with `Theme_Default` shipping null sprite slots today the divergence is runtime-irrelevant (`ThemedImage.ApplyTheme` line 28 null-guards `_image.sprite = sprite`), but the moment a buyer assigns a sprite to `Theme.ButtonPrimary` (or M4.X / M4.2 Bootstrap Defaults palette research delivers default button sprites pre-1.0) Group C primary buttons silently ignore the slot — a direct violation of the kit's "skin it once" pitch. Reconciled by extending `CatalogGroupCBuilder.CreateButton` to a 5-arg signature `(Transform parent, string name, Color color, ThemeSpriteSlot spriteSlot, ThemeColorSlot colorSlot)` mirroring the canonical `AddThemedImage` signature; `CreatePrimaryButton` now wires `(ButtonPrimaryColor, ThemeSpriteSlot.ButtonPrimary, ThemeColorSlot.PrimaryColor)` matching Group A; `CreateSecondaryButton` continues to wire `(ButtonSecondaryColor, ThemeSpriteSlot.None, ThemeColorSlot.None)` preserving the M4.7-bis.A Secondary deferral (D2 cluster). Visual regression risk on this commit alone: zero (`Theme_Default` ships null sprites). Forward-compatibility: when M4.X delivers default button sprites, Groups A + C reskin uniformly; without this fix Group C would have been the silent outlier. Decision lens: 4-role convergent `/_checker as triple` (user `Fit`+`Time-to-value`, pm `Scope boundary`+`Simplicity test`, dev `Decision continuity`) ranked path (b) extend-C-to-A as the only option that honors the canonical project convention without introducing process complexity (overload / two-helper-flavors / deferred deuda). Audit confirmed only 2 callers of `CreateButton` 4-arg in Group C (`CreatePrimaryButton` + `CreateSecondaryButton`); no orphan callsites. Generic pattern captured: helper-signature alignment is necessary but not sufficient for "consumers aligned" — wrapper callsites must also be audited before extracting a shared base (composability §5 corollary "Extract-from-divergent-consumers anti-pattern" — wrappers as second-order divergence).

- **M4.7-bis.B.4 — `CatalogGroupEBuilder` migrated to `using static CatalogGroupBuilderShared` + Loading/MainMenu screens themed via existing slots (path-b-modified per second `/_checker as triple` convergent verdict).** Pre-sweep audit confirmed E's structural distinction from popup-catalog groups: 2 prefab targets (`LoadingScreen` + `MainMenuScreen`) are screen templates (full-canvas immersive) not popups (centered card-on-backdrop); local helper surface diverges intentionally (`CreateButtonGroup` / `BuildMenuButton` / `CreateBackdropIn` / `CreatePanel` / `CreateStretchChild` / `CreateLabel` / `ApplyVerticalLayout` / `PositionCentered` / `AnchorTopStretch` are screen-template-specific with no popup parallel — composability §5 strict says retain inline-E); E does NOT carry the helper-shape divergence pattern of A/B/D pre-migration (no `CreateButton` / `CreatePrimaryButton` / `CreateSecondaryButton` — uses `BuildMenuButton(parent, name, label, bg)` local). 5 helpers byte-identical to Shared (`CreateRoot`, `CreateChild`, `AddImage`, `StretchInside`, `SaveAsPrefab`) deleted on consume; E's `CreateRoot` body slightly reformulated (`StretchInside(go.AddComponent<RectTransform>())` chained vs Shared's 2-line equivalent) is functionally identical → consumes Shared cleanly. **`CreateText` retained inline-E** (composability §5 strict): E's `CreateText` defaults `tmp.color = TextLightColor` (white text for dark screen-template backgrounds) while Shared's defaults `TextDarkColor` (dark text for popup off-white cards). Single consumer in E with a different default contract — extraction would either require a new `CreateTextLight` shared variant (single-consumer = §5 violation) or post-call `.color =` overrides at every callsite (10+ callsites — readability regression). Decision: keep inline-E. **Critical scope decision via second `/_checker as triple` (user + pm + dev convergent stress-test, separate from D.3 Settings-widget decision because E's surface and intent differ):** initial conservative path-a (consume Shared utilities only, all colors stay inline-E) was rejected by user-lens Real conditions scenario (LoadingScreen is **literally the first frame** the buyer sees on boot — if it stays dark-fixed when they swap to Casual cream theme, the kit's "skin it once" pitch fails on frame 1, before even reaching any popup). Ambitious path-c (introduce dedicated `LoadingBg` / `MainMenuBg` / `BarTrack` / `ButtonPlay` / `ButtonSecondary` slots) was rejected by pm-lens (5+ new slots = clear M4.X scope creep) and pre-empted by dev-lens (`LoadingBarColor` already exists in `ThemeColorSlot` enum from M4.7 — purpose-built for exactly this BarFill callsite, consuming it now finally honors the slot's original intent rather than leaving it as dead enum value). **Convergent verdict: path-b-modified** — 6 themed migrations + 2 constant deduplications + 5 helper deletions, zero new slots invented, all reuses semantically defensible: `BgDark` (LoadingScreen root) → `BackgroundDark` (theme.BackgroundDark Default `(0.12, 0.16, 0.24)` — collapses with `BgMainMenu` source value `(0.12, 0.16, 0.24)` → exact match, lose nothing on Default; `BgDark` original `(0.10, 0.12, 0.16)` Δ ~0.04 darker → on Default, LoadingScreen brightens by ~0.02 per channel, imperceptible). `BgMainMenu` (MainMenu root) → `BackgroundDark` (exact match Default — zero visual delta on Default; reskins on Casual to bright cream, on Premium to deepest dark). **`BarTrack` → `MutedColor`** (inert track, same rationale as D Slider Track — `MutedColor` Default `#6B727F` Δ ~0.17 lighter than `BarTrack` original `(0.25, 0.28, 0.35)` — slightly more visible loading bar track on Default, by-design improvement). **`BarFill` → `LoadingBarColor`** ⭐ — this slot was added in M4.7 specifically for loading bars and remained an unused enum value through M4.7-bis.A → A.bis → A.ter → B.1-B.3; B.4 finally consumes it at the only callsite for which it was designed. **`BtnPlay` → `PrimaryColor`** (Play CTA — primary action, semantic match identical to D's `CloseButton` migration; MainMenu Play button now reskins blue→orange→slate across Default/Casual/Premium). **`BtnSecondary` → `MutedColor`** (mild stretch documented — Settings/Shop/Daily buttons sit on dark MainMenu bg; `MutedColor` Default `#6B727F` Δ ~0.18 lighter than `BtnSecondary` original `(0.22, 0.26, 0.36)` → buttons become more contrasted/visible on dark bg, by-design improvement; same M4.X reassessment flag as D's slider+toggle stretch — reuses `MutedColor` semantic stretch already documented in B.3). **`DotRed` → `DangerColor`** (DailyIndicator dot for "new content" — `DangerColor` Default `(0.92, 0.30, 0.30)` is **exact match** to `DotRed` literal value, semantic stretch is "danger = attention-grabbing red" which is a defensible reuse of the slot for a non-error attention signal; alternative `WarningColor` was rejected because Default `WarningColor` is orange not red — would change Default visual). **`BackdropColor` + `TextLight` constants deduplicated** (consume Shared.`BackdropColor` + `TextLightColor` via `using static`). **Inline-E retained** (per scope-lock — E-specific design intent, no theme slot semantically maps): `CreateSpinner` color `TextLightColor` (already migrated to Shared constant), `CreateLogoSlot` color `Color(1f, 1f, 1f, 0f)` transparent placeholder for buyer-supplied logo sprite, `BuildInstructionsPanel` bg `Color(0f, 0f, 0f, 0.55f)` editor-only debug overlay (not part of buyer-facing UI). E's 7 fallback-color constants kept (`BgDark`, `BgMainMenu`, `BarTrack`, `BarFill`, `BtnPlay`, `BtnSecondary`, `DotRed`) — they serve as fallback colors in the AddThemedImage 4-arg calls and document the original visual intent for future maintainers. **Net LoC delta:** `CatalogGroupEBuilder.cs` 554 → 509 (−45 — smaller than B/D deltas because E retains 8 screen-template-specific helpers that have no Shared parallel, by §5 strict reading). **Visual regression risk on regenerate:** LoadingScreen + MainMenu visibly shift on Default by Δ ~0.02-0.18 across 6 themed elements (mostly imperceptible — `BackgroundDark` matches `BgMainMenu` exactly; `MutedColor` lightens BarTrack and BtnSecondary slightly = improved contrast; `LoadingBarColor` matches `BarFill` exact; `PrimaryColor` matches `BtnPlay` exact; `DangerColor` matches `DotRed` exact). Reskins fully on Casual / Premium swap as designed. **Confirmed green pending user validation**: Test Runner expected green (no API surface change in Shared); `Build Group E Sample` expected to regenerate 2 prefabs + 1 scene with themed elements visible immediately on Default; manual theme-swap validation requires changing `UIThemeConfig` in `GroupE_BootDemo.unity` Inspector at runtime since the demo doesn't include a theme-switcher UI (deferred to M4.X cluster — adding a theme dropdown to GroupE_BootDemo would mirror `Catalog_M4_ThemePresets/ThemePresetsDemo` pattern for screens). **B.4 done; M4.7-bis.B.5 (`\\.image\\.color\\s*=` runtime override audit across all 5 builders post-sweep) + B.6 (master samples-green per builder) + B.7 (CHANGELOG close + master theme-swap end-to-end test) next** (ETA 1 session for B.5+B.6, B.7 separate).

- **M4.7-bis.B.3 — `CatalogGroupDBuilder` migrated to `using static CatalogGroupBuilderShared` + Settings widgets themed via existing slots (path-b-modified per `/_checker as triple` convergent verdict).** Pre-sweep audit confirmed D's helper surface byte-identical to canonical Shared (10 helpers) with one anti-pattern flag identical to A pre-A.ter / B pre-B.2: `CreatePrimaryButton(Transform, string)` 1-call wrapper using `AddImage(go, ButtonPrimaryColor)` raw (no `ThemedImage` attached). Resolved without a fourth iteration — consumes Shared's themed `CreatePrimaryButton(ButtonPrimary + PrimaryColor)` directly. **Critical scope decision via `/_checker as triple` (user + pm + dev convergent stress-test):** D is the Settings popup — arguably the most theme-visible UI surface in any mobile game (sliders for music/SFX, dropdown for language, toggles for notifications/haptics). Initial conservative proposal (path-a, zero new themed migrations beyond free Card+CloseButton) was rejected by user-lens Fit scenario (buyer swaps to Premium dark theme, sees card reskin but slider fill stays bright-blue chillón → "skin parcial" perception → 🔴 pitch FAIL on first impression). Ambitious path-c (introduce dedicated `SliderTrack` / `SliderFill` / `ToggleBg` / `ToggleCheck` slots) was rejected by pm-lens Scope boundary (4-6 new slots invented mid-sweep = scope creep; new slots imply 3 theme presets re-edit + `ThemedImage.TryResolveColor` switch-case additions + migration documentation = M4.X cluster, not B.3). **Convergent verdict: path-b-modified** — reuse 2 existing slots that semantically map to widget intent. **Slot mapping locked:** `MutedColor` (designed as "inert background" per M4.1 Material 3 paired-with-PrimaryColor research) → Slider Background (track), Toggle Background, Dropdown Background, Dropdown Template Background, Dropdown Item Background — clean semantic match across 5 callsites. `PrimaryColor` → Slider Fill (active filled portion) + Toggle Checkmark (active state indicator) — 2 callsites with mild semantic stretch flagged below. **Inline-D retained:** Slider Handle stays inline `Color.white` (no theme slot semantically maps to "draggable thumb" — would require dedicated `HandleColor` slot in M4.X if buyer customization surfaces). **Mild stretch flagged for M4.X reassessment:** `PrimaryColor` semantically defined as "active CTA / button fill" (Material 3 `Primary` token) — extending to "active widget state" (slider fill / toggle check) is judgment-call coupling. Risk surface: if buyer customizes `Theme.PrimaryColor` to a brand-saturation extreme (e.g., neon pink for a kids' game), Slider Fill + Toggle Check inherit that saturation → could be desired (full brand consistency) or undesired (over-saturation in widget chrome). **Reversibility**: trivial — single-line edit per callsite when M4.X delivers dedicated `ActiveStateColor` slot (or `WidgetActive` / `IndicatorColor` per Material 3 `Tertiary` extension naming). M4.X cluster gates this decision on real buyer feedback rather than speculative slot proliferation. **Dead code removed**: `RowDividerColor` constant (line 26, declared `static readonly` but never used in any helper or popup builder body — likely vestigial from earlier divider-row design that never shipped); `closeImg.color = ButtonPrimaryColor` override at line 101 (redundant before migration — Shared's `CreatePrimaryButton` already passes ButtonPrimaryColor as fallback; redundant after migration — `ThemedImage.ApplyTheme` runtime stomps the override every refresh). **Free themed migrations on consume**: Card → `PanelBackground` sprite slot + `BackgroundLight` color slot (inherited from Shared.`CreateCard`); CloseButton → `ButtonPrimary` + `PrimaryColor` (inherited from Shared.`CreatePrimaryButton`). **Net LoC delta:** `CatalogGroupDBuilder.cs` 484 → 370 (−114). **Visual regression risk on regenerate:** Settings popup card visibly shifts from off-white inline `(0.97, 0.98, 1f)` to themed `BackgroundLight` (Default `(0.96, 0.97, 0.98)` ≈ same off-white; Casual / Premium reskin to themed lights). All 5 widget backgrounds (slider track, toggle bg, dropdown × 3) shift from light gray `(0.85-0.93, 0.86-0.94, 0.90-0.96)` to themed `MutedColor` (Default `#6B727F` blue-tinted gray Δ ~0.45 — visibly darker mid-tone; reskins on swap as designed). All 2 widget active indicators (slider fill, toggle check) shift from inline blue `(0.20, 0.55, 0.95)` to themed `PrimaryColor` (Default identical, Casual orange `(0.95, 0.50, 0.20)`, Premium slate-azul). `Theme_Default.PrimaryColor` matches builder fallback by construction → zero Default delta; Casual / Premium produce intended reskin visible in `ThemePresetsDemo`. **Confirmed green pending user validation**: Test Runner expected green (no API surface change in Shared); Build Group D Sample expected to regenerate 1 prefab + 1 scene with themed widgets visible immediately on Default theme load; theme-swap on `ThemePresetsDemo` should now reskin slider tracks + fills + toggle bgs + checks + dropdown bgs uniformly across Default / Casual / Premium. **B.3 done; M4.7-bis.B.4 sweep next** (Group E builder — structurally distinct: scene templates not popup catalog, mostly inline-E by intent; ETA 1 session).

- **M4.7-bis.B.2 — `CatalogGroupBBuilder` migrated to `using static CatalogGroupBuilderShared` + D6 inline-decided + `CreateSecondaryButton` promoted to themed (3rd consumer + 11 callsites gain theme reactivity).** Pre-sweep audit confirmed B's helper surface byte-identical to the canonical Shared (14 helpers + 6 constants), with one anti-pattern flag identical to A's pre-A.ter state: `CreateButton(parent, name, color)` 3-arg legacy + `CreatePrimaryButton`/`CreateSecondaryButton` as 2-call wrappers. Resolved without a fourth `B.ter` iteration — the helper-alignment chapter is sealed at M4.7-bis.A.ter (3 iterations: helper signature → wrappers → constructive composers → underlying helper); B.2 simply deletes B's local duplicates and consumes the canonical 5-arg `CreateButton` from Shared. **D6 inline-decided** (deferred from M4.7-bis.A): NotEnough `HeaderTint` migrated from `AddImage(headerGO, HeaderTintColor)` to `AddThemedImage(headerGO, HeaderTintColor, ThemeSpriteSlot.None, ThemeColorSlot.WarningColor)` — semantic match (NotEnoughCurrency = warning state, theme.WarningColor responds to skin swap); Reward `SuccessTintColor` (Claim button color override) **retained inline-B** — Reward popup is celebration-specific palette per builder intent, not generic success-state indicator; promoting to `ThemeColorSlot.SuccessColor` would conflate two distinct semantic axes (celebration vs status). **Claim button override pattern locked**: `CreatePrimaryButton(card.transform, "ClaimButton")` migrated to `CreateButton(card.transform, "ClaimButton", ButtonPrimaryColor, ThemeSpriteSlot.ButtonPrimary, ThemeColorSlot.None)` so the post-construction `claimImg.color = SuccessTintColor` line at `CatalogGroupBBuilder.cs:117` survives `ThemedImage.ApplyTheme` runtime refresh — `Runtime/Theme/ThemedImage.cs:29` `TryResolveColor(_colorSlot, theme, out color)` returns false when `_colorSlot == None` and the `_image.color` assignment is skipped, preserving the runtime override. This is the canonical pattern for "themed sprite, manually-colored" buttons (sprite reskin survives, color stays inline) — promote to a `Shared` helper if 2nd consumer surfaces in D/E sweep. **Critical scope-extension at B.2 close — `CatalogGroupBuilderShared.CreateSecondaryButton` promoted from `(ButtonSecondaryColor, None, None)` raw to fully themed `(ButtonSecondaryColor, ThemeSpriteSlot.ButtonSecondary, ThemeColorSlot.SecondaryColor)`.** Pre-promotion audit surfaced 11 callsites across A+B+C consuming the legacy raw shape — Group A: `CancelButton` (DailyLogin), `PreviousButton` + `SkipButton` (Tutorial); Group B: `CloseButton` (Shop), `BuyMoreButton` + `DeclineButton` (NotEnoughCurrency); Group C: `WatchToDoubleButton` (RewardClaim), `RetryButton` + `MainMenuButton` (LevelComplete), `ContinueCurrencyButton` + `RestartButton` + `MainMenuButton` (GameOver). All 11 buttons previously failed the kit's "skin it once" pitch — they did NOT add `ThemedImage` components and did NOT reskin on theme swap. Asymmetric with `CreatePrimaryButton` which was already themed (`ButtonPrimary` + `PrimaryColor`) since B.1.a extract. Promotion rationale: composability §5 "promote-when-2nd-consumer rule" satisfied trivially (3 consumers, 11 callsites); pitch alignment (theme reactivity is the kit's core value); symmetry (Primary themed → Secondary themed by default). Escape hatch for future raw-secondary needs: `CreateButton(parent, name, color, ThemeSpriteSlot.None, ThemeColorSlot.None)` explicit 5-arg call. Visual delta on regenerate: Secondary buttons gain `ButtonSecondary` sprite from theme + color from `theme.SecondaryColor`. With `Theme_Default` shipping null sprite slots (M4.7-bis.A footnote) sprite reskin is invisible until M4.X delivers default button sprites; color reskin is visible immediately if `theme.SecondaryColor` differs from the legacy raw `(0.85, 0.86, 0.90)` — for `Theme_Default._secondaryColor (0.35, 0.40, 0.50)` Δ 0.5 brings buttons into the dark-slate palette already used for Pause-popup secondaries (which were never raw — `CreateThemedLabelledButton` callsites lines 178-182 explicitly wired `ThemeColorSlot.SecondaryColor` since M4.7-bis.A). **Pause-popup A Secondary deferral resolved by convergence** (M4.7-bis.A pending decision): no revert needed — A's Pause uses `CreateThemedLabelledButton` (inline-A wrapper, calls Shared.`CreateButton` 5-arg explicitly) which already wires `ButtonSecondary` + `SecondaryColor`; with Shared.`CreateSecondaryButton` now matching, the two paths converge on identical theme contract. `CreateThemedLabelledButton` remains inline-A (single consumer in A; no body-match in B today; reassess for promotion if D/E sweep surfaces a parallel). **Inline-B retained** (per scope-lock — single-consumer in B today): `CellColor` (Shop cell background — subtle off-white tone for grid contrast, no theme slot semantically maps), `HeaderTintColor` (now used as fallback color in the themed AddThemedImage call; constant retained for explicit fallback), `SuccessTintColor` (Reward celebration palette per intent above), `HUDBackgroundColor` (semi-transparent black for HUD overlays — same deferral as inline-C `HUDBackgroundColor`, no theme slot). **Net LoC delta:** `CatalogGroupBBuilder.cs` 684 → 553 (−131); `CatalogGroupBuilderShared.cs` 171 → 171 (signature change only, no body delta in CreateSecondaryButton). **Visual regression risk on extract:** A's 3 Secondary callsites (Cancel/Previous/Skip) and C's 5 Secondary callsites visually shift from light gray `(0.85, 0.86, 0.90)` to `theme.SecondaryColor` on next regenerate — desired (the whole point of theme reactivity); B's 3 Secondary callsites gain ThemedImage components on first generate (no prior prefab to compare). **Confirmed green post-edit** (user-validated): Test Runner all-green; Group B sample tested in multiple cases — no error logs, no regressions. Theme-swap end-to-end test deferred to M4.7-bis.B.7 (CHANGELOG close) when all 5 groups ship aligned. **B.2 done; M4.7-bis.B.3-B.4 sweep next** (D + E builders; ETA 1-2 sessions).

- **M4.7-bis.B.1 — `CatalogGroupBuilderShared` static utility class extracted (helpers + constants single-source-of-truth locked).** Post-A.ter audit confirmed 12 of 16 helper bodies byte-identical between `CatalogGroupABuilder` and `CatalogGroupCBuilder`, 6 of 6 shared constants used in both consumer bodies, and zero remaining wrapper-divergence (3-iteration helper-alignment chapter sealed). Composability §5 fully satisfied (2 proven aligned consumers). Architectural finding at implementation time: the D1 triple-checker recommendation `internal abstract class CatalogGroupBuilderBase` (named after `UIAnimPopupBase` precedent) does NOT apply — `UIAnimPopupBase` is `MonoBehaviour` (instantiable) while Catalog Group X builders are `public static class` (MenuItem containers); C# does not allow static classes to inherit. Pivoted to the more idiomatic Unity editor-utility pattern: **`internal static class CatalogGroupBuilderShared`** + consumers add `using static KitforgeLabs.MobileUIKit.Editor.Generators.CatalogGroupBuilderShared;` directive (precedent: `AssetDatabase`, `EditorGUIUtility`). Subclasses keep their `public static` shape, call helpers unqualified at the call site (zero ceremony, zero source-line churn at call sites). The `Base` suffix was abandoned in favor of `Shared` — accurate for utility class semantics (no inheritance, no virtual dispatch, no override candidates). **Scope-lock anexo (locked at extract time):** `CatalogGroupBuilderShared` exposes `internal static readonly Color BackdropColor / CardColor / ButtonPrimaryColor / ButtonSecondaryColor / TextDarkColor / TextLightColor` + `internal static void EnsureFolders(string demoFolder)` (parametrized — subclass passes its demo folder name; `OutputRoot`/`PrefabsFolder` constants stay in subclass for `SaveAsPrefab` paths) + `internal static GameObject CreateRoot(string)` + `Button CreateBackdrop(Transform)` + `GameObject CreateCard(Transform, Vector2)` + `GameObject CreateChild(Transform, string)` + `Image AddImage(GameObject, Color)` + `Image AddThemedImage(GameObject, Color, ThemeSpriteSlot, ThemeColorSlot)` + `void AddThemedText(GameObject, ThemeFontSlot, ThemeColorSlot, ThemeFontSizeSlot)` (promoted as symmetric pair of `AddThemedImage` despite single consumer at extract time — explicit deviation from §5 strict reading on the rationale that the symmetry locks the contract for B/D/E sweep) + `TextMeshProUGUI CreateText(Transform, string, string, int, FontStyles)` + `(GameObject, Button, Image) CreateButton(Transform, string, Color, ThemeSpriteSlot, ThemeColorSlot)` + `CreatePrimaryButton(Transform, string)` + `CreateSecondaryButton(Transform, string)` + `void StretchInside(RectTransform)` + `AnchorTopOfCard(RectTransform, float, float)` + `ForceButtonHeight(GameObject, float)` + `WireAnimatorCard(MonoBehaviour, RectTransform)` + `T SaveAsPrefab<T>(GameObject, string) where T : Component`. Total: 6 constants + 17 helpers (~171 LoC). **Inline-A retained** (per scope-lock — single consumer in A today): `ToastBackgroundColor`, `CreateLabelledButton` / `CreateThemedLabelledButton` / `CreateLabelledToggle`, `BuildConfirmPopup` / `BuildPausePopup` / `BuildTutorialPopup` / `BuildNotificationToast` / `BuildDemoScene`. **Inline-C retained** (per scope-lock — single consumer in C today): `SuccessTintColor` / `WarningColor` / `FailureColor` / `HUDBackgroundColor` / `HUDCapLabelColor` (semantic mapping deferred to D6 inline-decide in B sweep), `OverrideThemedImageSlot` (single consumer — DailyLogin claim button override; promote when 2nd consumer surfaces in B/D/E per §5 strict), `BuildDailyLoginPopup` / `BuildLevelCompletePopup` / `BuildGameOverPopup` / `BuildHUDEnergy` / `BuildHUDTimer` / `BuildDemoScene` / `TryAttachStubServices` / `WireHUDServices`. **Net LoC delta:** `CatalogGroupABuilder.cs` 645 → 489 (−156); `CatalogGroupCBuilder.cs` 818 → 672 (−146); `CatalogGroupBuilderShared.cs` 0 → 171 (+171); total 1463 → 1332 (−131). **Promote-when-2nd-consumer rule** for B / D / E sweep (M4.7-bis.B.2-B.4): if any helper currently inline-A or inline-C appears with byte-identical body in a B / D / E builder during sweep, promote to `CatalogGroupBuilderShared` inline at sweep time (composability §5 — 2 proven aligned consumers triggers extract; new helpers introduced during sweep follow the same gate). Symmetric corollary: if a helper extraction-candidate is found ONLY in 1 consumer post-sweep (e.g., `OverrideThemedImageSlot` if no B/D/E consumer surfaces), it stays inline — composability §5 does NOT mandate extraction for symmetry alone, only for proven duplication. **Visual regression risk on extract:** zero — helper bodies byte-identical pre/post extract; `using static` preserves call-site shape; `Theme_Default` ships null sprite slots so even slot-driven branches remain runtime-equivalent. **Confirmed green post-extract** (user-validated): Test Runner all-green; `Build Group A Sample` regenerates 4 prefabs + scene with byte-identical visual output; `Build Group C Sample` regenerates 5 prefabs + scene with byte-identical visual output; `Build M4.1 — Theme Presets` dropdown swap reskins primary buttons + cards across A AND C uniformly across Default / Casual / Premium. **Footnote — unrelated Unity 6.3 native crash observed during validation:** an LMDB cursor crash (`mdb_cursor_sibling` → `mdb_cursor_prev` → `LMDB_Enumerator::Next` → `LMDBIndexStorage::RemoveDocuments` → `UnityEditor.Search.SearchDatabase.MergeArtifacts` on `ProcessIncrementalUpdate` background thread) was triggered by Unity Search's incremental indexer when 5 prefabs + 1 scene were imported in <1s during `Build Group C Sample`. Native LMDB cursor corruption, NOT a refactor regression — all 5 prefabs + scene were generated correctly on disk before the crash, no C# exception in Editor.log, and a simple Unity restart cleared the LMDB state cleanly. Captured in `feedback_workflow.md` → "Unity 6.3 LMDB crash on burst-import — restart clears, blameless of code". Generic pattern captured: when the crash trace's deepest frame is in a Unity native subsystem (Search/Indexer, Asset Import, Domain Reload) with NO C# stack frame from the project, the refactor is exonerated by default — investigate crash dumps before assuming code regression. **B.1 done; M4.7-bis.B.2-B.4 sweep next** (ETA 2-3 sessions).

  - **3rd-iteration path-b — `CreateButton` underlying helper itself diverges (M4.7-bis.A.ter — sealing the helper-alignment chapter pre-B.1).** Body-level cross-check of all 16 helper bodies between `CatalogGroupABuilder` and `CatalogGroupCBuilder` (the deep audit pre-`CatalogGroupBuilderBase` extract) surfaced a 3rd instance of the recursive wrapper-divergence pattern — this time at the deepest layer: the `CreateButton` helper itself. Group A still carried a 3-arg legacy signature `CreateButton(Transform parent, string name, Color color)` that adds only an `Image` (no theme), with `CreatePrimaryButton` doing a 2-call dance (`CreateButton` + `AddThemedImage` post-attach at line 521) and `CreateSecondaryButton` bypassing theme entirely; Group C M4.7-bis.A.bis canonical extended to a 5-arg `CreateButton(parent, name, color, ThemeSpriteSlot, ThemeColorSlot)` with a `themed` branch (line 754-762) that picks `AddThemedImage` vs `AddImage` based on whether either slot is `None`. End-result of the spawned prefabs is byte-identical (same components, same theme slot wiring), but the helper SHAPE diverged at the lowest layer of the constructive-composer recursion (`CreateLabelledButton` → `CreateButton` → ...). For the `CatalogGroupBuilderBase` extract this is a blocker — the base must expose ONE canonical `CreateButton` shape; the 3-arg pattern would have been silently inherited and re-spread across B/D/E during sweep. Reconciled by the 4th-degree path-b (mirroring M4.7-bis.A.bis): extended Group A's `CreateButton` to the 5-arg canonical body byte-identical to Group C's; collapsed `CreatePrimaryButton` from 2-call to 1-call (`return CreateButton(...ButtonPrimary, PrimaryColor)`); flipped `CreateSecondaryButton` to pass `(None, None)` explicitly; updated the single dependent caller `CreateLabelledButton:532` to pass `(None, None)` (no semantic change — it routed through the legacy 3-arg path that ended at `AddImage`; the `themed` branch in the new 5-arg now picks `AddImage` for the same input, keeping the prefab byte-identical). `CreateThemedLabelledButton` left unchanged (still calls `CreateLabelledButton` + post-`AddThemedImage`; remains an inline-A helper with no parallel in C, deferred to B.2 sweep inline-decide alongside the 5 Pause-popup Secondary callsites and D6). Visual regression risk on this commit: zero — the runtime contract is preserved; only the helper shape converges. Generic pattern, now closed across 3 iterations: helper-divergence is recursive — surfaces first at the helper signature (M4.7-bis.A: `AddThemedImage` 4-arg vs void), then at second-order wrappers (M4.7-bis.A.bis: `CreatePrimaryButton`), then at constructive composers (M4.7-bis.A.bis: `CreateCard` calling `AddThemedImage`), and finally at the underlying helper itself when subsequent path-b reconciles introduce new canonical shapes the older consumer never received (M4.7-bis.A.ter: `CreateButton`). Mandatory pre-extract gate: byte-level cross-check of ALL helper bodies before `CatalogGroupBuilderBase` extract — interface-signature spot-check is necessary but never sufficient; the deep audit is what surfaced the 3rd divergence and unblocked B.1. Captured in `feedback_workflow.md` → "Wrapper-divergence is recursive — three iterations to seal helper-alignment".

  - **Card sprite-slot reconciliation parallel finding (same milestone, same path b).** Body-level cross-check of helper bodies across Groups A + C — performed as the same pre-extract audit step that surfaced the primary-button divergence above — uncovered a second instance of the same wrapper-divergence pattern in `CreateCard`. Group A line 479 wires `AddThemedImage(go, CardColor, ThemeSpriteSlot.PanelBackground, ThemeColorSlot.BackgroundLight)` per the project-local "Themed* contract" convention (`Packages/.../MEMORY.md` line 13: "Every Card background → `ThemedImage(PanelBackground, BackgroundLight)`"); Group C M4.7 wired `(CardColor, ThemeSpriteSlot.None, ThemeColorSlot.BackgroundLight)` — color slot correct, sprite slot dropped to `None`. Same root cause as the primary-button divergence (M4.7 migrated the color contract but not the sprite contract); same null-guard runtime invisibility today; same forward-compat risk the moment `Theme.PanelBackground` carries a sprite. Reconciled by 1-line edit at `CatalogGroupCBuilder.cs:701` flipping `ThemeSpriteSlot.None` → `ThemeSpriteSlot.PanelBackground` to match the Group A canonical. `CreateCard` is a constructive composer (a wrapper helper that calls the canonical `AddThemedImage` helper internally), confirming the generic pattern is recursive: any helper that composes other helpers must be audited at the body level — interface-signature alignment surfaces only first-order divergence. Generic pattern extended: extract-from-divergent-consumers anti-pattern includes second-order wrapper divergence (`CreatePrimaryButton`) AND third-order constructive-composer divergence (`CreateCard` calling `AddThemedImage`). Other AddThemedImage callsites in Group C audited inline: success/failure banners (`CreateBanner`) wire `ThemeSpriteSlot.None` correctly (no parallel "BannerBackground" sprite slot exists in the enum today; deferred to M4.X if buyer demand surfaces). Audit confirmed only 1 callsite of `CreateCard` per builder (popup root); no orphan consumers. Visual regression risk on this commit: zero (`Theme_Default` ships null sprites for all slots). With this second reconcile, A↔C alignment now spans `AddThemedImage` (helper) + `CreatePrimaryButton` (second-order wrapper) + `CreateCard` (third-order constructive composer) — composability §5 "2 proven aligned consumers" criterion fully satisfied for B.1 base-class extraction.

- **M4.7-bis.B.6+B.7 — master samples-green via QA Suite + tag pre-conditions for `v0.9.0-alpha`.** `Tools → Kitforge → UI Kit → QA Suite → Rebuild + Run All` ejecuta los 6 builder MenuItems sequentially + valida los 23 targets (6 demo scenes A-E + ThemePresets + 17 prefabs) — clean run `[QASuite] Run complete · scanned 23 · 0 errors · 0 warnings` confirma structural integrity post-regen across all 5 aligned builders consuming `CatalogGroupBuilderShared`. Manual theme-swap E2E on `Catalog_M4_ThemePresets/ThemePresetsDemo.unity` (dropdown Default/Casual/Premium): GameOver + LevelComplete cards, Primary buttons, Secondary buttons, banners, HUDEnergy bar reskin uniformly across A+C builders — proxy for B+D+E coverage since all 5 consume the same `CatalogGroupBuilderShared` themed contract. Group E manual swap on `GroupE_BootDemo.unity` deferred to M4.X cluster (add dropdown mirroring `ThemePresetsDemo` pattern; no buyer-visible regression risk — E's themed elements use the same `ThemedImage` runtime contract validated by A+C). EditMode Test Runner all-green. M4 hardening core-packaging block closes here; `v0.9.0-alpha` tag pre-conditions met.

### Added

- **M5.5.C — F-UX-6 Communication actionable + CHEATSHEET RegisterPrefab false-claim fix (🔴 second API hallucination caught + corrected).** **Library API verification gate caught a second M5.1 CHEATSHEET hallucination**: the friction-points table line `"Custom popup _popupManager.RegisterPrefab(typeof(MyPopup), prefab) once at startup"` references an API that **does not exist**. Empirical grep of `Runtime/Core/PopupManager.cs` + `Runtime/Core/UIManager.cs` + `Runtime/Toast/ToastManager.cs` confirmed: NO `public RegisterPrefab` method on any manager — prefabs are wired via `[SerializeField] private UIModuleBase[] _popupPrefabs` / `_screenPrefabs` Inspector arrays only; demo builders write into these via `SerializedObject` at build time; runtime has no programmatic registration API. Fix: CHEATSHEET friction-points row replaced with truthful guidance ("Drag your popup prefab into the `_popupPrefabs` array on `KitforgeRoot/PopupManager` (Inspector). For catalog popups: open a Group sample scene (built via `Tools → Kitforge → UI Kit → Build Group X Sample`) — those ship with prefabs pre-wired."). Two API hallucinations this session (M5.5.A caught Push/Show + ToastManager.Show generic; M5.5.C caught RegisterPrefab) — both traced to the same root cause: assumption-based snippet/doc generation without grepping library source first. Lesson reinforced: `feedback_workflow.md` line 62 ("Library API verification gate") MUST run on EVERY library-touching documentation pass, not just code-touching ones. **F-UX-6 Communication actionable** lands as 2-axis upgrade in `KitforgeTestLauncher`: (1) **LogError next-step format**: every error message rewritten with `→` next-step pointer per F-UX-6 spec — examples: `"PopupManager not found in active scene → Run Setup → Step 2 (Add Scene Root)..."`, `"Generic method '{methodName}' not found on {ManagerType} → reinstall the Kitforge Mobile UI Kit package; runtime contract drift detected."`, `"Spawn failed for {displayName} → {InnerException}. Likely cause: prefab not wired into PopupManager._popupPrefabs[]..."`. Format: `[Subsystem] {observable problem} → {concrete next step the user can take}`. Replaces ambient "spawn failed" with actionable diagnostic. (2) **`EditorWindow.ShowNotification` toasts on Hub**: new `NotifyHub(string)` helper calls `_hostWindow.ShowNotification(new GUIContent(message))` for spawn outcomes — success ("Spawned ConfirmPopup"), block ("Spawn blocked: no PopupManager"), failure ("Spawn failed: ConfirmPopup"). Provides immediate visual feedback in the Hub window without forcing buyer to look at Console for confirmations. Toasts are Unity-native (`EditorWindow.ShowNotification` is the kit's standard per D4 LOCKED) — text-only, auto-fade, non-blocking. **Test Launcher refactor**: `KitforgeTestLauncher` constructor now takes `(KitforgeHubState state, EditorWindow hostWindow)` — `hostWindow` reference passed from `KitforgeHubWindow.BuildPaneFor` (1-line change `new KitforgeTestLauncher(_state, this)`). `_hostWindow` field nullable-safe (NotifyHub early-returns if null) so launcher remains testable in isolation if needed. **Force scenarios section note enriched**: prepended sentence "Requires the relevant Group sample built and prefabs wired in the active scene's PopupManager — easiest path: open the demo scene from Tools → Kitforge → UI Kit → Build Group X Sample." per `as qa` round 1 finding (workshop-drawer fail at spawn time when prefab not registered). Workshop-drawer test pass: F-UX-6 P0 audit "Communication actionable" delivered — designer's lunes 9am sees clear next-step guidance instead of cryptic stack traces; Hub-side ShowNotification confirms spawn worked without Console scrolling. M5.5 cluster CLOSED at this milestone — Test Launcher fully functional within v1.0.0-rc API freeze constraints (no runtime API additions; service contracts read-only preserved). **Out of scope (deferred):** auto-register prefabs at spawn (M5.5.D — would require either runtime API addition OR Editor-side `SerializedObject` mutation hack on manager's `_popupPrefabs[]`); health badge in Hub header showing service-wiring completeness (M5.5.D — `UIServicesRefsCheck` audit integration); per-element DTO Inspector edit-before-spawn (post-RC polish — requires SerializedObject-on-runtime-instance plumbing); buyer-facing public `RegisterPrefab` API (post-1.0 candidate — buyers may eventually request it; absence is currently acceptable per Inspector-wiring + builder-autowire pattern). **Triple sweep meta-finding**: this session caught 2 API hallucinations across separate documentation/code passes — institutional learning: when CHEATSHEET / SnippetBuilder / spec text references a library API, the gate must read library source FIRST regardless of how confident the writer feels. Cost of skip: text drift propagates to buyer-facing surface; cost of verification: ~30 sec grep per session that touches library refs. Captured implicitly in M5.5.A CHANGELOG entry already; reinforced here. Static review: function lengths all ≤ 15 lines; `EditorWindow.ShowNotification(GUIContent)` Unity 6 stable API; null-safe `_hostWindow` access pattern; LogError next-step format follows F-UX-6 spec verbatim.

- **M5.5.B — Mock DTO smart defaults + Force scenarios panel (M5.5.A FIX SOON absorbed; service-mutation-free design preserves API freeze).** **Smart defaults** in `KitforgeMockDtoFactory`: new `ApplySmartDefaults(data, displayName)` reflection helper detects public string fields named `Title` / `Subtitle` / `Message` and populates them with `"Test {DisplayName}"` / `"Mock {DisplayName}"` / `"Mock spawn — click any button to dismiss."` respectively. Closes the workshop-drawer-relevant FIX SOON from M5.5.A `as qa` round (empty-popup spawn confused tester whether mock fired or not). Reflection-only — no per-DTO hardcoded coupling; covers ~9 of 13 spawnable elements meaningfully (popups with Title/Message contracts; HUDs and screens unaffected by Title-only DTOs degrade silently per existing default-field defaults). New `CreateForComponent(Type, displayName)` overload mirrors `Create(entry)` for force-scenario consumers (decouples DTO instantiation from `KitforgeCatalogEntry`); `Create(entry)` simplified to 1-line wrapper. Companion edit in `KitforgeCatalogSnippetBuilder`: `ResolveDataType(entry)` → 1-line wrapper around new `ResolveDataTypeForComponent(Type)` overload (single source of truth — avoid duplicating namespace-lookup logic in MockDtoFactory). **Force scenarios panel** in `Editor/Hub/Test/`. **`KitforgeForceScenario.cs`** — public sealed POCO; 5 props (`Label` / `Description` / `ComponentType` / `Pattern` / `ConfigureMock` action that mutates the pre-instantiated mock DTO). **`KitforgeForceScenariosRegistry.cs`** — public static; hardcoded list of 4 scenarios via `BuildAll()` split into per-scenario builder helpers (each ≤ 8 lines). Scenarios shipped: (1) **DailyLogin · Day 1 first claim** — `CurrentDay=1` + `AlreadyClaimedToday=false` + `LastClaimUtc=DateTime.MinValue` (validates first-launch UX); (2) **NotEnoughCurrency · Coins (500)** — `Currency=Coins` + `Required=500` + `Missing=500` (validates monetization moment); (3) **GameOver · Continue with ad** — `ContinueMode=Ad` + `Score=1234` (validates death moment); (4) **LevelComplete · 3 stars new best** — `Stars=3` + `IsNewBest=true` + `Score=12345` (validates celebration moment). Each scenario picks a distinct workshop-drawer-relevant lifecycle moment (first-launch / monetization / death / celebration) — covers ~80% of designer's prototype testing surface in 4 buttons. **Service-mutation-free design**: F-UX-5 audit text named "Force Day N · Coins=0 · Skip Hour" suggesting service-state mutation, but service interfaces (`IEconomyService` / `IProgressionService` / `ITimeService`) are intentionally read-only per architectural contract — adding mutators would breach v1.0.0-rc API freeze + couple test concerns to runtime contracts. Force scenarios pivoted to **DTO-override pattern**: spawn the popup with a pre-configured DTO that simulates the scenario's state (CurrentDay=1 in DTO, NOT in any service). Buyer's wired services remain authoritative for their game; Force scenarios are pure UI testing. Trade-off documented in code comment + section note ("DTO override; no service mutation"). **Test Launcher refactor**: spawn flow decoupled from `KitforgeCatalogEntry` — `SpawnTypedData(Type componentType, KitforgeSpawnPattern pattern, object data, string displayName)` is the new pivot, called from both `SpawnEntry` (catalog grid) and `SpawnForceScenario` (force scenarios). `SpawnViaManager<TManager>` / `InvokeManagerMethod` / `FindGenericMethod` / `BuildInvokeArgs` retained but now take Type+data instead of Entry. New `BuildForceScenarioRow` mirrors `BuildElementRow` shape (label + Spawn button, Play-mode-gated). New `AppendForceScenariosSection` placed FIRST in the scrollview (above Popups/Toasts/Screens/HUDs) per workshop-drawer test importance — designer's most likely Test Launcher use case is the 4 named scenarios, not "spawn ConfirmPopup with empty fields". Section note clarifies "DTO override; no service mutation" so designer understands the boundary. **Out of scope (deferred):** service-mutation-based scenarios (Skip Hour for ITimeService, persistent currency reset for IEconomyService) — would require new runtime API surface + breaks v1.0.0-rc API freeze; defer to v1.1.0 if user pain surfaces; cheatsheet documents "if you need to test against real service state, write a 1-line C# helper that calls your IEconomyService.Set(0)"; per-element DTO Inspector edit-before-spawn (M5.5.C if needed); buyer-extensible `[KitforgeForceScenario]` attribute API (v1.1.0 alongside `[KitforgeCatalogEntry]`); 5th+ scenarios via user feedback. Workshop-drawer test pass: F-UX-5 P0 daily action delivered without breaching API freeze contract; designer covers 4 distinct prototype-testing moments in single-click. Static review: function lengths all ≤ 15 lines (`SpawnEntry` / `SpawnForceScenario` / `SpawnTypedData` triplet keeps each ≤ 12); refactor preserves M5.5.A reflection patterns (Activator.CreateInstance + ParameterInfo.HasDefaultValue + MakeGenericMethod); registry's per-scenario builder helpers each ≤ 8 lines for readability.

- **M5.5.A — Test Launcher foundation + library API correctness sweep (🔴 FIX NOW absorbed).** Test tab content lands as: top banner (Play-mode-aware: amber "Enter Play mode to spawn..." in Edit / green "Play mode active — click Spawn..." in Play) + scrollable element list grouped by spawn pattern (Popups · Toasts · Screens · HUDs note). Each row: `{DisplayName} · Group {X} · {ComponentType.Name}` label + `Spawn` button (disabled in Edit mode, enabled in Play). HUDs section renders informational note redirecting to Catalog tab drag-to-scene (HUDs are scene prefabs, not Manager-spawned). **Spawn implementation** via reflection: 2 new files in `Editor/Hub/Test/`. **`KitforgeMockDtoFactory.cs`** — public static `Create(entry)` resolves DTO type via existing `KitforgeCatalogSnippetBuilder.ResolveDataType` + invokes parameterless `Activator.CreateInstance(dataType)`; honors the M5.1 R-U-C2 audit empirical contract that all 13 DTOs have parameterless ctor + meaningful default field values. **`KitforgeTestLauncher.cs`** — internal sealed UIToolkit pane builder. `SpawnEntry(entry)` dispatches by `Pattern`: Popup → `SpawnViaManager<PopupManager>(entry, "Show")`, Screen → `SpawnViaManager<UIManager>(entry, "Push")` (NOT Show — see API correction below), Toast → `SpawnViaManager<ToastManager>(entry, "Show")`. `SpawnViaManager<TManager>` resolves manager via `UnityEngine.Object.FindAnyObjectByType<TManager>()` (Unity 6 modern API); on null emits actionable LogError directing buyer to Setup → Step 2 (Add Scene Root). `InvokeManagerMethod` finds the generic method via `MethodInfo.IsGenericMethod` filter, calls `MakeGenericMethod(entry.ComponentType)`, builds args array from `ParameterInfo.HasDefaultValue` / `DefaultValue` (handles optional parameters: `PopupPriority priority = Gameplay`, `float? duration = null`, `object data = null`). Wraps invoke in `try / catch (TargetInvocationException ex)` to surface inner exception messages cleanly. Live mode reactivity: `EditorApplication.playModeStateChanged += OnPlayModeChanged` subscribed in `Build()`, unsubscribed via `DetachFromPanelEvent` callback (prevents leak when window closed). State change → `Refresh()` rebuilds banner + list with new enabled state on Spawn buttons. **🔴 FIX NOW absorbed — library API correctness sweep**: prior CHEATSHEET (M5.1) + `KitforgeCatalogSnippetBuilder` (M5.3.C) + `KitforgeCatalogBrowser.BuildSpawnInstruction` (M5.3.C) shipped wrong APIs based on assumption rather than empirical verification — caught by `as dev` library-verification gate during M5.5.A kickoff (read `PopupManager.cs:28` + `ToastManager.cs:26` + `UIManager.cs:31` end-to-end before Test Launcher reflection). Fixes: (1) **Screens use Push, not Show** — `UIManager` exposes `Push<T>(object data = null)` + `Replace<T>(object data = null)` + `Pop()`; there is NO `UIManager.Show<T>(...)`. CHEATSHEET § 2 spawn pattern + § 3 Screens table header + Snippet Builder + Browser canonical-call helper all corrected to `_uiManager.Push<T>(data)`. (2) **Toasts use generic Show, not non-generic** — `ToastManager.Show<T>(object data, float? duration = null)` is generic with `where T : UIToastBase`. CHEATSHEET § 2 + § 3 Toasts table header + Snippet Builder all corrected to `_toastManager.Show<NotificationToast>(data)` form. Browser `ResolveManagerName` helper retired in favor of `ResolveCanonicalCall(pattern, typeName)` returning the full canonical call string (`_popupManager.Show<{T}>(data)` / `_uiManager.Push<{T}>(data)` / `_toastManager.Show<{T}>(data)`) — single source of truth for the API surface across detail-pane instructions. Lesson trace: `feedback_workflow.md` line 62 ("Library API verification gate before code — read library source FIRST, write code SECOND"). Cost of skip: text drift in cheatsheet + snippet shown to designer = wrong copy-paste at integration time. Cost of verification: ~30 sec grep before generating snippets. Anti-pattern label: "API hallucination via familiarity assumption". USS additions: `kfh-test-banner` (amber Edit / green Play `--play` modifier), `kfh-test-list` flex column, `kfh-test-section-header` 45-grey row separator, `kfh-test-section-note` italic muted, `kfh-test-row` 54-grey row + `--button` 80px min-width. **Out of scope (deferred):** Force scenarios panel ("Force Day N" / "Coins=0" / "Skip Hour") — M5.5.B; per-element DTO mock-fill UI (Inspector-style edit before spawn) — M5.5.C; LogError actionable-format integration ("next-step" formatting per F-UX-6) — M5.5.D; ShowNotification toasts on Hub for spawn-fired feedback — M5.5.D; health badge (UIServicesRefsCheck + ThemeReskinDeltaCheck audit checks) — M5.5.D. Workshop-drawer test pass: spawning popup/screen/toast with mock DTO without writing game code = the F-UX-5 P0 daily action; F-UX-5 spec text "Play-mode-only · 17 popups DTO mock-fill" honored (excluded HUDs from spawn list redirect to Catalog drag — workshop-drawer-correct per M5.3.D pattern restriction). Static review: function lengths all ≤ 15 lines (`InvokeManagerMethod` extracted from `SpawnViaManager` to honor); reflection patterns standard `MethodInfo.MakeGenericMethod` + `ParameterInfo.HasDefaultValue` stable BCL surface; `FindAnyObjectByType<T>` Unity 6 API; `EditorApplication.playModeStateChanged` + `DetachFromPanelEvent` UIToolkit-stable.

- **M5.4.A — Theme Studio foundation (themes dropdown + selection persistence + summary pane).** New file `Editor/Hub/Theme/KitforgeThemeStudio.cs` (~150 LOC, internal sealed). Theme tab content lands as: top toolbar (themes dropdown + "New Theme" / "Capture All Snapshots" placeholder buttons disabled with M5.4.D milestone tooltips) + detail pane (theme summary OR empty hint). **Themes discovery**: `AssetDatabase.FindAssets("t:" + nameof(UIThemeConfig))` enumerates ALL `UIThemeConfig.asset` instances in the project (package-shipped + buyer-created), sorted `OrdinalIgnoreCase` for cross-machine determinism. Honors `kitforge_theme_extensibility` memory rule LOCKED 2026-05-09 ("themes ilimitados — M5.4 dropdown MUST scan AssetDatabase, never hardcode 3"). Empty case (no themes found): dropdown displays placeholder `"(no themes found)"` + disabled state; detail pane renders actionable hint pointing buyer to `Tools → Kitforge → UI Kit → Bootstrap Defaults` (creates `Theme_Default`) or `Assets → Create → Kitforge → Theme` (custom). **Selection persistence**: new `KitforgeHubState._selectedThemeKey` (`[SerializeField] string`, default empty) + `SelectedThemeKey` property using shared `Persist()` helper. Key is the asset path string (`Assets/Settings/UI/Theme_Default.asset` etc.) — robust against display-name renames since `AssetDatabase.LoadAssetAtPath` survives asset moves through Unity's GUID system at the buyer's project level. Stale-path guard: `ResolveSelectedTheme` returns null if path no longer resolves → detail pane reverts to hint cleanly. **Hub wiring**: `KitforgeHubWindow.BuildPaneFor` adds `if (tab == HubTab.Theme) return new KitforgeThemeStudio(_state).Build();` (1-line addition + `using KitforgeLabs.MobileUIKit.Editor.Hub.Theme;`). Theme tab now ships with real content; remaining Test / Help tabs continue to render placeholder until M5.5 / M5.6. **Toolbar buttons disabled with informative tooltips**: "New Theme" + "Capture All Snapshots" reserved for M5.4.D (will delegate to existing `Assets → Create → Kitforge → Theme` for new asset OR `PrefabSnapshotCapture` pipeline for snapshots — same pattern as audit window's `Capture Snapshots` toolbar). **Detail pane content** (when theme selected): summary box (theme `name` bold + `Asset: {path}` meta line) + status placeholder ("Live preview · slot editing · Capture All Snapshots — coming in M5.4.B / .C / .D"). USS additions: `kfh-theme` flex column, `kfh-theme-toolbar` (45-grey bg row), `kfh-theme-dropdown` (240px min-width), `kfh-theme-detail` flex-grow column, `kfh-theme-summary-title` 16px bold, `kfh-theme-summary-meta` 11px subtitle. **Out of scope (deferred):** live mock-popup preview pane (M5.4.B — Path B in-memory `RenderTexture` 100-200ms re-render per roadmap risk #2 mitigation); collapsible slot editing UI (M5.4.C — 6 categories: Colors / Sprites / Fonts / Audio / Animation / Safe-area); "New Theme" creation button (M5.4.D — `ProjectWindowUtil.CreateAsset` integration); "Capture All Snapshots" delegate to `PrefabSnapshotCapture` (M5.4.D); auto-refresh dropdown on `AssetPostprocessor.OnPostprocessAllAssets` callback (M5.4.D — currently buyer reopens Theme tab to re-discover after creating a new theme); `ThemeConsistencyCheck` audit check validating theme has non-null required slots (M5.5 — feeds Communication actionable). **Namespace note**: file lives in `KitforgeLabs.MobileUIKit.Editor.Hub.Theme` while runtime UIThemeConfig is in `KitforgeLabs.MobileUIKit.Theme`; C# namespace lookup unambiguous via `using KitforgeLabs.MobileUIKit.Theme;` at file top + sibling-namespace search rules (`UIThemeConfig` not defined in local Editor.Hub.Theme namespace, falls through to global usings). Static review: `AssetDatabase.FindAssets` / `GUIDToAssetPath` / `LoadAssetAtPath<T>` stable Unity 6 APIs; `DropdownField` constructor `(label, choices, defaultIndex)` UIElements stable; `RegisterValueChangedCallback<string>` stable; function lengths all ≤ 15 lines.

- **M5.3.D — Drag-pattern restrict + per-pattern instructions + draggable cursor cue (M5.3.C debt absorbed).** Drag-to-scene is now restricted to `KitforgeSpawnPattern.HUD` cells only — single-role `as qa` finding caught the foundational UX issue: dragging a popup/screen/toast cell into the scene creates a dormant prefab not driven by `PopupManager`/`UIManager`/`ToastManager`, leading to "popup never appears at runtime" buyer confusion. **Drag callbacks** (`MouseDownEvent` + `MouseLeaveEvent` + `MouseUpEvent`) now register only when `entry.Pattern == HUD`; popups/screens/toasts cells render with `ClickEvent` selection only. Extracted `RegisterDragCallbacks(cell, entry)` helper to keep `BuildCell` ≤ 15 lines per Unity rule. **Per-pattern detail-pane instruction**: HUD pattern + prefab built → "Drag the cell into `KitforgeRoot/UICanvas/ScreenRoot`. HUDs auto-bind to required services on enable." (drop-target ambiguity NOTE absorbed); HUD + prefab missing → existing "Drag-to-scene unavailable — run Tools → Kitforge → UI Kit → Build Group {X} Sample first."; Popup/Screen/Toast → "Use the spawn snippet below — `{manager}.Show` is the canonical path. Manual scene drop creates a dormant prefab not driven by the manager." Manager name resolved via `ResolveManagerName(pattern)` switch (`_popupManager` / `_uiManager` / `_toastManager`). **Cursor cue**: HUD cells receive `kfh-catalog-cell--draggable` class with `cursor: pan` USS (UIToolkit's hand-grab equivalent) — visual indicator that this cell type is the draggable subset. Non-HUD cells retain default `cursor: link` from `kfh-catalog-cell` base. Workshop-drawer test pass: foundational correctness — designer's lunes 9am drag gesture is canonical for HUDs (static scene UI bound on enable) and explicitly redirected to snippet for popups/screens/toasts (which are spawn templates, not scene placements). M5.3 cluster CLOSED at this milestone — Catalog Browser tab fully functional in v1.0.0-rc scope: 17 elements grid · click selection · drag-to-scene for HUDs · DTO field list · spawn snippet copy-able. Thumbnails (M5.3.B-bis) deferred post-RC per workshop-drawer ROI re-evaluation. **Out of scope (deferred):** thumbnail rendering via `PrefabSnapshotCapture` (post-RC); copy-button on snippet TextField (M5.3-polish); search / group filter input (defer until catalog grows beyond 17); cursor cue divergence on cells with unbuilt prefabs (current `cursor: pan` shows on all HUD cells regardless of build state — drag attempt with null prefab still LogWarnings, no functional regression). Static review: `cursor: pan` is a stable UIElements USS value (UIToolkit cursor enum aligned with `MouseCursor` enum); function lengths all ≤ 15 lines; class order preserved.

- **M5.3.C — Catalog Browser drag-to-scene + DTO snippet + label alignment (M5.3.A debt absorbed).** Two new files in `Editor/Hub/Catalog/`. **`KitforgeCatalogPrefabResolver.cs`** — public static; resolves prefab path per `KitforgeCatalogEntry` via 2-level matching: (1) scan `Assets/Catalog_Group{X}_Demo/Prefabs/` for prefabs with the entry's component type, (2) prefer exact filename-matches-DisplayName when multiple component matches exist (necessary for HUDCoins+HUDGems disambiguation since both share `HUDCurrency`), fall back to first-component-match otherwise (works for single-prefab-per-type cases like ConfirmPopup where DisplayName "Confirm" doesn't match filename "ConfirmPopup"). Returns null if folder doesn't exist (sample not built yet) — Browser surfaces actionable guidance instead of silent fail. **`KitforgeCatalogSnippetBuilder.cs`** — public static; generates per-entry spawn snippet (4 patterns: `_popupManager.Show<T>(new TData {...})` / `_uiManager.Show<T>(new TData {...})` / `_toastManager.Show(new TData {...})` / HUD instruction comment) + DTO field list via reflection (`BindingFlags.Public | BindingFlags.Instance` on `{ComponentType}Data` resolved within the same namespace + assembly as the component). `GetReadableTypeName` maps primitive `Type` to canonical short names (`string` / `int` / `float` / `bool` / `DateTime` / arrays). **`KitforgeCatalogBrowser.cs` extended**: (a) per-cell drag callbacks (`MouseDownEvent` primes `_dragReady` flag · `MouseLeaveEvent` while ready triggers `DragAndDrop.PrepareStartDrag` + `DragAndDrop.objectReferences = {prefab}` + `DragAndDrop.StartDrag(displayName)` · `MouseUpEvent` clears the flag) — works alongside existing `ClickEvent` selection (drag fires only when mouse leaves the cell with button held; click without drag still selects). Drag attempt with no resolved prefab emits actionable `Debug.LogWarning` directing buyer to `Tools → Kitforge → UI Kit → Build Group {X} Sample`; (b) detail pane content: title + meta + description (existing) + drag instruction (light-blue banner — adapts copy based on prefab availability) + DTO field list (header + bullet rows OR fallback "HUD components are driven by services" / "No DTO detected") + spawn snippet (read-only multiline `TextField`, monospace-ish dark code-box styling, copy-able by drag-select). **M5.3.A FIX SOON absorbed — HUD label alignment**: `KitforgeCatalogRegistry` HUDCoins / HUDGems strings updated `"HUD Coins"` → `"HUDCoins"` and `"HUD Gems"` → `"HUDGems"` to match prefab filenames + audit window labels. Functional necessity: `KitforgeCatalogPrefabResolver.PickBestMatch` requires filename↔DisplayName equality for HUDCurrency-typed prefabs (both HUDCoins.prefab and HUDGems.prefab carry the `HUDCurrency` component; without filename disambiguation both cells would resolve to the alphabetically-first prefab — HUDCoins — incorrectly mapping HUDGems cell to HUDCoins prefab on drag). Other DisplayNames retain designer-friendly spacing (`"Daily Login"` / `"Game Over"` / `"Loading Screen"` / `"Notification Toast"`) — single-component-per-prefab case lets `firstComponentMatch` resolve correctly without filename anchor; cross-tool divergence (Hub "Daily Login" vs audit "DailyLoginPopup") accepted as audience-appropriate (designer Hub vs engineering audit). USS additions: `kfh-catalog-detail-instruction` (light-blue actionable banner), `kfh-catalog-detail-fields` (collapsed column with bullet rows), `kfh-catalog-detail-snippet-box` (header + dark code-box read-only TextField, 80px min-height). **Out of scope (deferred):** thumbnail preview rendering via `PrefabSnapshotCapture` (M5.3.B-bis post-M5.3, lower daily ROI per workshop-drawer ROI re-evaluation 2026-05-10); copy-to-clipboard button on snippet (UIToolkit Button with `EditorGUIUtility.systemCopyBuffer = snippet`, ~5 LOC, defer to M5.3.D polish if user feedback); search / group filter input (defer until catalog grows beyond 17); in-cell drag-cursor visual cue (defer); per-cell prefab-built indicator badge (defer — drag instruction in detail pane covers the case). Workshop-drawer test pass: drag-to-scene = the daily action (designer's lunes 9am gesture), DTO snippet = copy-paste integration into game code, label fix = cross-tool consistency. Static review: `DragAndDrop.PrepareStartDrag` / `DragAndDrop.objectReferences` / `DragAndDrop.StartDrag` are stable Unity Editor APIs (UIToolkit-compatible — same drag infrastructure across IMGUI + UIElements); `MouseDownEvent` / `MouseLeaveEvent` / `MouseUpEvent` are UIToolkit events; `TextField.isReadOnly` + `TextField.multiline` are stable properties; reflection via `BindingFlags.Public | BindingFlags.Instance` on Type matches the existing DTO `[Serializable]` + public-fields contract validated empirically in M5.1 R-U-C2 audit. Function lengths all ≤ 15 lines (`BuildFieldList` split into `BuildFieldList` + `MakeFieldsHeader` + `PopulateFieldsBox` + `MakeFieldsEmptyLabel` + `MakeFieldRow` quintet to honor Unity rule).

- **M5.3.A — Catalog Browser foundation (registry + grid + selection + detail panel placeholder).** New `Editor/Hub/Catalog/` folder with 3 files. **`KitforgeCatalogEntry.cs`** — public POCO + `KitforgeSpawnPattern` enum (Popup / Toast / Screen / HUD); 5 properties (ComponentType, DisplayName, Group, Pattern, Description) + computed `Key` property (`{FullName}|{DisplayName}` for HUDCoins/HUDGems disambiguation since both share `HUDCurrency` component type per audit dedup pattern). **`KitforgeCatalogRegistry.cs`** — public static; hardcoded list of 17 entries via `BuildAll()` split into 5 group helpers (`AppendGroupA` 4 entries · `AppendGroupB` 5 entries · `AppendGroupC` 5 entries · `AppendGroupD` 1 entry · `AppendGroupE` 2 entries). Honors D3 LOCKED ("17 kit-only en v1.0.0-rc; `[KitforgeCatalogEntry]` attribute API en v1.1.0") — registry IS the source of truth for v1.0.0-rc; attribute-driven extensibility deferred to v1.1.0 once `PrefabSnapshotCapture` API stabilizes. Type discovery via `typeof()` (compile-time safety vs string-based reflection); 13 sub-namespace usings imported (Confirm/Pause/Tutorial/Toasts/Reward/Shop/NotEnough/HUD/DailyLogin/LevelComplete/GameOver/Settings/Screens). **`KitforgeCatalogBrowser.cs`** — internal sealed; UIToolkit pane builder. Layout: 60% width grid (left) + flex-grow detail panel (right). Grid wraps cells via `flex-wrap` USS (160×110 cells, 4-cell rows on default 900-wide window). Each cell renders group badge (top-left, dark pill) + display name (bold) + spawn pattern (italic, bottom). Click selects entry → persists `KitforgeHubState.SelectedCatalogKey` → highlights cell via `kfh-catalog-cell--selected` (blue border + tinted bg) → refreshes detail pane. Detail pane: empty hint when no selection ("Select an element from the grid to see details") OR title + meta line (`{Pattern} · Group {Group} · {ComponentType.Name}`) + description + status badge ("Thumbnail · drag-to-scene · DTO snippet — coming in M5.3.B / M5.3.C"). Selection is type-only (cell click never instantiates a prefab); drag-to-scene + thumbnails + DTO snippet ship in M5.3.B (snapshots) and M5.3.C (drag + side panel actions). **State extension**: `KitforgeHubState._selectedCatalogKey` (`[SerializeField] string`, default empty) + `SelectedCatalogKey` property; both setters refactored to call shared `Persist()` helper (replaces 4 lines of duplicated `EditorUtility.SetDirty` + `AssetDatabase.SaveAssetIfDirty` per setter — keeps both setters at ≤ 7 lines each per Unity rule). Stale-key guard: `ResolveSelectedEntry` returns null if persisted key doesn't match any registry entry (e.g. after package upgrade renames a type) → detail pane reverts to hint cleanly. **Hub wiring**: `KitforgeHubWindow.BuildPaneFor` adds `if (tab == HubTab.Catalog) return new KitforgeCatalogBrowser(_state).Build();` (1-line addition above placeholder fallback). Catalog tab now ships with real content; remaining Theme / Test / Help tabs continue to render placeholder until M5.4 / M5.5 / M5.6. **Out of scope (deferred):** prefab thumbnail rendering via `PrefabSnapshotCapture` 1080×1920 → cell preview image (M5.3.B); drag-from-cell-to-Hierarchy / drag-from-cell-to-Scene gesture handlers (M5.3.C); DTO Inspector snippet preview (M5.3.C); copy-to-clipboard `popupManager.Show<T>(...)` snippet button (M5.3.C); group filter / search input (M5.3.C if needed); per-group color stripe on cells (M5.3.B polish). Workshop-drawer test pass: organizes the cajón (designer's lunes 9am — opens Hub → Catalog tab → 17 elements visible at-a-glance with what-when-pattern triple, no spec reading required); does NOT pin the marketing letrero (no marketing-quality screenshots, no Asset Store hero shots — that scope stays closed per phase doc). Static review: 0 new asmdef refs (uses existing runtime asmdef via `KitforgeLabs.MobileUIKit.Catalog.*` sub-namespaces); function lengths all ≤ 15 lines (`BuildCell` split into 4 helpers via `BuildCellGroupBadge` + `BuildCellName` + `BuildCellPattern`; `BuildDetailContent` split into 4 helpers similarly); class order preserved.

- **M5.2.C — Auto-open on import + M5.2.B debt absorbed.** New `Editor/Hub/KitforgeHubAutoOpen.cs` (~22 LOC, internal static, `[InitializeOnLoad]`): static constructor checks `Application.isBatchMode` (skip CI / headless builds) + `EditorPrefs.GetBool("kf.hub.first_open_done", false)` first-time marker. On first install (marker false): immediately writes marker to `true` (mark-before-schedule prevents re-open during domain-reload cascade — D-A.2 race mitigation per roadmap risk #3) + registers `EditorApplication.delayCall += DeferredOpen`. `DeferredOpen` unsubscribes self + calls `KitforgeHubWindow.Open()`. Marker is per-machine (`EditorPrefs`) so each developer machine gets one auto-open per kit install; subsequent reloads / project-reopens skip auto-open. Manual Hub open via `Tools/Kitforge/Hub` always available regardless of marker state. Resolves P7 persistent-context pillar (designer doesn't hunt for the menu first time + doesn't get nagged after). **M5.2.B debt absorbed in same chunk** (2 FIX SOON + 1 NOTE from prior `as dev` + `as qa` checker round, single-source convergent + extras): (1) `KitforgeSetupWizard.InvokeMenu(string path)` helper wraps all `EditorApplication.ExecuteMenuItem` calls — checks bool return + emits `Debug.LogError` with actionable message ("Menu 'X' not found. Package may be partially installed; reinstall the Kitforge Mobile UI Kit") when menu path missing (fires if Bootstrap Defaults / Build Group A Sample renamed or package partially installed); silent no-op replaced by visible diagnostic. (2) `RequiredAnimPresetCount` constant **removed** (was hardcoded `10`); `IsStep1Done` now returns `guids.Length >= 1` (lenient — semantic = "has Bootstrap been run at least once" matches the workshop-drawer test acceptance). Description text retains "Generates 10 default UIAnimPreset assets" for buyer clarity (descriptive contract) — validation tracks bootstrap-was-run, not preset-count-exact. Single-source-of-truth refactor (option B — expose `DefaultUIAnimPresetsCreator.PresetCount` const referencing the 10-call list) deferred — would breach M5.2.C cap (cross-file edit on Generators surface) without proportional gain (current lenient validation handles user-deletes-presets case via idempotent re-run). (3) **NOTE absorbed** — `RunStep2` early-return-and-ping idempotency: extracted `TryPingExistingBinder` (returns true + pings existing binder if one is already in scene) + `InstantiateNewKitforgeRoot` (existing logic). Buyer's "click again on Done step" instinct now selects + pings the existing root instead of creating a duplicate scene root (which would emit Unity's multiple-EventSystems warning). Bonus cleanup: dead `statusLabel.userData = "status"` line removed (vestigial — `RefreshStep` queries by className, not userData). `BuildStepCard` split into `BuildStepCard` + `BuildCardHeader` to keep both ≤ 15 lines (Unity rule); `Func<bool> isDone` parameter dropped from `BuildStepCard` signature (was unused — status updates flow through `RefreshStep`'s className query). **Out of scope (deferred):** `Build Everything` button delegating to existing builders A-E + M4.1 sequential rebuild (M5.2.D); Catalog/Theme/Test/Help tab content (M5.3-M5.6); auto-open trigger when `KitforgeRoot.prefab` is detected in a scene (smart contextual open — speculative until user feedback). Static review: `[InitializeOnLoad]` static constructor pattern, `EditorApplication.delayCall +=`, `EditorPrefs.GetBool/SetBool`, `Application.isBatchMode` are all stable Unity 6 APIs; no new asmdef refs (uses existing `KitforgeLabs.MobileUIKit.Animation` for `UIAnimPreset` + `KitforgeLabs.MobileUIKit.Bootstrap` for `KitforgeThemeBinder`); function lengths all ≤ 15 lines; locality respected (auto-open lives in package Editor/Hub/, marker lives in user's EditorPrefs).

- **M5.2.B — Setup wizard 3-step content + M5.2.A debt absorbed.** Setup tab content lands as a 3-card vertical wizard (`Editor/Hub/Setup/KitforgeSetupWizard.cs`, internal sealed). Each card: number badge + title + description (1-2 lines, no jargon) + status badge ("Done" / "Pending" with `--done` USS modifier) + action button. **Step 1 (Initialize Project)**: invokes `EditorApplication.ExecuteMenuItem("Tools/Kitforge/UI Kit/Bootstrap Defaults")`; validation = `AssetDatabase.FindAssets("t:UIAnimPreset")` count ≥ 10 (matches Bootstrap Defaults output contract). **Step 2 (Add Scene Root)**: loads `KitforgeRoot.prefab` from the package path + `PrefabUtility.InstantiatePrefab` into active scene + `Undo.RegisterCreatedObjectUndo` + `Selection.activeGameObject` + `EditorGUIUtility.PingObject`; validation = `FindObjectsByType<KitforgeThemeBinder>(FindObjectsInactive.Include, FindObjectsSortMode.None)` returns ≥ 1. **Step 3 (Hello World)**: invokes `EditorApplication.ExecuteMenuItem("Tools/Kitforge/UI Kit/Build Group A Sample")` (4-popup demo: Confirm/Pause/Tutorial/NotificationToast); validation = `LoadAssetAtPath<SceneAsset>("Assets/Catalog_GroupA_Demo/Catalog_GroupA_Demo.unity")` non-null. Validation runs live on every `CreateGUI` + on every action-button click (no cached state — filesystem is the source of truth, so manual deletion of presets/scene auto-reverts step status). When all 3 done → `kfh-wizard-summary` label renders "Setup complete — switch to Catalog to browse the 17 elements." All actions idempotent; re-running is safe. **M5.2.A debt absorbed in same chunk** (3 FIX SOON from prior `as dev` + `as qa` checker round): (1) `KitforgeHubWindow.GetTabLabel(HubTab)` switch decouples display labels from enum identifiers — future enum rename (e.g. `Setup` → `Onboarding`) does not silently change UI text; (2) `ApplyStylesheet()` extracted with `Debug.LogWarning` when `LoadAssetAtPath<StyleSheet>` returns null — post-package-refactor regression now visible in Console instead of silent unstyled window; (3) `KitforgeHubState.GetOrCreate()` recovery — split into `MarkPersisted()` + `TryCreateOrFallback()` helpers (each ≤9 lines per Unity rule); post-`CreateAsset` re-`LoadAssetAtPath` verifies persistence; on failure (readonly Assets/, VCS lock) emits actionable `Debug.LogError` + returns in-memory SO with `IsPersisted=false`. New `KitforgeHubState.IsPersisted` non-serialized property (`[NonSerialized] private bool _isPersisted`) drives `BuildPersistenceBanner()` — displays warning banner ("Hub state is in-memory only…") at window top when persistence failed; banner hidden in normal flow. **Out of scope (deferred):** auto-open on import via `[InitializeOnLoad]` + `EditorApplication.delayCall` + `EditorPrefs` first-time marker (M5.2.C); `Build Everything` button delegating to existing builders A-E + M4.1 (M5.2.D); Catalog/Theme/Test/Help tab content (M5.3-M5.6); inline screenshot/preview of resulting scene state (M5.3 Catalog Browser thumbnails); auto-advance to next tab on Setup completion (debatable UX — defer to user feedback). Static review: 0 new asmdef refs (uses existing `KitforgeLabs.MobileUIKit` runtime ref for `KitforgeThemeBinder` + `UIAnimPreset` types); function lengths all ≤ 15 lines per Unity rule; class order serialized → private → properties → static factory → private helpers preserved.

- **M5.2.A — Kitforge Hub Editor window skeleton (UIToolkit, top-level menu front door).** New `Editor/Hub/` folder with 3 files: `KitforgeHubWindow.cs` (UIToolkit `EditorWindow`, `[MenuItem("Tools/Kitforge/Hub")]` — promoted to top level under `Tools/Kitforge/`, NOT under `UI Kit/` because Hub IS the front door per pillar P1), `KitforgeHubState.cs` (sealed `ScriptableObject` persisting `ActiveTab` field; auto-creates `Assets/Settings/Kitforge/HubState.asset` on first window open via `GetOrCreate()` lazy pattern + `EnsureFolders()` mirror of `CatalogM41ThemeBuilder`), `KitforgeHubWindow.uss` (BEM-style `kfh-*` class names, distinct from `audit-*` to avoid stylesheet collision; vertical 180px sidebar layout per F-UX-1 spec). Window structure: header (title + 1-line tagline) + body row (sidebar + content area). Sidebar enumerates 5 tabs from `KitforgeHubState.HubTab` enum: Setup / Catalog / Theme / Test / Help. Click switches `_state.ActiveTab` (persisted via `EditorUtility.SetDirty` + `AssetDatabase.SaveAssetIfDirty`), re-renders content pane, applies `kfh-sidebar-tab--active` class. Content pane is placeholder this milestone: per-tab subtitle (1-line description) + milestone status badge ("Coming in M5.X"). Wiring complete for M5.2.B onward to swap placeholders for real panes without touching the shell. Editor-only (asmdef `KitforgeLabs.MobileUIKit.Editor`, `includePlatforms: ["Editor"]`); zero Runtime impact, zero new asmdef references. **Out of scope (deferred):** Setup wizard 3-step content (M5.2.B); auto-open on import via `[InitializeOnLoad]` + `EditorApplication.delayCall` + `EditorPrefs` first-time marker (M5.2.C); `Build Everything` button delegating to existing builders A-E + M4.1 (M5.2.D); actual Catalog / Theme / Test / Help content panes (M5.3 / M5.4 / M5.5 / M5.6). **Workshop-drawer test pass:** organizes the cajón (single front door — designer's lunes 9am hits ONE menu instead of hunting `Tools/Kitforge/UI Kit/[Audit|Bootstrap Defaults|Build Group X Sample × 5|Build M4.1 Theme Presets]` 8-deep menu). Pre-Path-B-cheatsheet content remains accessible at its existing location; Hub will eventually surface key entry points without removing the deep menu.

- **M5.1 — `Documentation~/CHEATSHEET.md` (one-page reference for game designers).** New top-level documentation file at `Documentation~/CHEATSHEET.md` (~145 lines, 7 sections): quick start (drop KitforgeRoot.prefab + Play + Build Group X Sample) · 3 spawn patterns (popup / screen / toast 1-liner code) · catalog at-a-glance table for the 17 elements (popup name + when to use + key DTO fields + events to wire — covers Group A 4 popups + Group B 3 popups + 2 HUDs + Group C 3 popups + 2 HUDs + Group D 1 popup + Group E 2 screens + 1 toast = 17) · theme swap + customize (single-field via `KitforgeThemeBinder._theme` + `SetTheme(theme)` runtime API + `Create → Kitforge → Theme` for unlimited custom themes — clarifies Default/Casual/Premium are shipped presets, NOT a 3-theme cap) · settings persistence table (5 settings auto-persisted via `PlayerPrefsPlayerDataService` + 3 dailylogin keys + game-state buyer-owned) · top 9 friction points (HUD `--` no service · music slider silent · prefab not registered · theme not reskinning · multiple EventSystems · regen hidden · timer `--:--` · no animation · DailyLogin first launch) · going deeper links to CATALOG.md / Specs/ / Services/ / sample builders / audit gate / Bootstrap Defaults. Resolves M3c audit B2 finding "designer Friday-demo gap — currently must read 17 specs to know which popup spawns when". Pre-Hub deliverable (M5.6 Help tab will render this content live in-Editor); cheatsheet content is the ground truth that the Help tab consumes. **Sourced from runtime files** (not memory) — read each `*PopupData.cs` DTO + grepped `public event Action` across `Runtime/Catalog/` to lock event signatures empirically; spec markdown not read (cheatsheet is "what" reference, specs are "why" reference). Workshop-drawer test pass: organizes the cajón (designer finds spawn 1-liner + key fields without reading 17 separate specs); does NOT pin the marketing letrero. Out of scope: in-Editor Help tab UIToolkit panel rendering this markdown (M5.6); embedded code-snippet copy buttons (M5.3 Catalog Browser side panel); per-element preview thumbnails (M5.3 PrefabSnapshotCapture).

- **M5.1 — `KitforgeRoot.prefab` + `KitforgeThemeBinder` (drop-and-play scene root, single-field theme distribution).** `Runtime/Bootstrap/KitforgeRoot.prefab` is the first-class entry-point asset for buyers: drop into a scene → 4 managers wired + Canvas + 3 RectTransform roots + global PopupBackdrop + EventSystem, all preconfigured with `Theme_Default` from the package. Replaces the pre-M5.1 ritual of creating 4 manual GameObjects + assigning `_themeConfig` to UIManager + PopupManager + ToastManager separately (audit U-A.2 + U-A.3 — game designer hype-casual workflow lost ~25 min/proto on this on Monday morning). New `Runtime/Bootstrap/KitforgeThemeBinder.cs` (~37 LOC, `[DefaultExecutionOrder(-200)]`, sealed, `[DisallowMultipleComponent]`): single `_theme` Inspector field + 3 manager refs (`_uiManager` / `_popupManager` / `_toastManager`); `Awake()` distributes the theme to all 3 managers via existing `SetTheme()` API; `public void SetTheme(UIThemeConfig)` runtime swap re-distributes (re-Initializes cached instances per the M4.X runtime swap API). **Structural contract** (buyer-facing — load-bearing for prefab integration):
  - **Built-in EventSystem**: prefab ships with its own `EventSystem` + `StandaloneInputModule` GameObject. If buyer's scene already has an EventSystem, drop one (the prefab's) to avoid Unity's duplicate-EventSystem warning. Default decision documented; reassess in M5.6 buyer-friendliness audit.
  - **Built-in global PopupBackdrop**: a `PopupRoot/PopupBackdrop` GameObject ships with `Image` (black α 0.55, `RaycastTarget=1`), `SetActive=false` initially. `PopupManager.Update` toggles it via `_activeStack.Count > 0` (M3.1ter sibling-order fix consumes this). `RaycastTarget=1` means any active popup blocks scene input — desired for modal popup pattern; opinionated default the kit chose.
  - **Manager GameObjects as siblings of Canvas (not children)**: `KitforgeRoot/UIManager`, `KitforgeRoot/PopupManager`, `KitforgeRoot/ToastManager`, `KitforgeRoot/EventSystem` are all top-level children of the root, NOT under Canvas. Canvas children are only the 3 RectTransform roots (ScreenRoot / PopupRoot / ToastRoot). Keeps non-UI logic out of the UI hierarchy.
  - **`_themeConfig` deliberately null on the 3 managers**: the prefab does NOT pre-wire `UIManager._themeConfig` / `PopupManager._themeConfig` / `ToastManager._themeConfig`. KitforgeThemeBinder distributes the theme in Awake via `[DefaultExecutionOrder(-200)]` (runs before UIServices `-100`, before managers default `0`). Buyers swap themes by changing the binder's single `_theme` field — never touch the 3 manager fields again.
  - **`UIServices` slots all null by default**: 8 service ref slots stay empty until buyer wires their economy / playerData / progression / shopData / ads / time / audio / localization implementations. KitforgeRoot is theme-self-sufficient; service-dependent popups (Reward / Shop / GameOver / etc.) emit actionable `LogError` until the relevant service is wired.
  - **CanvasScaler set to `Scale With Screen Size`, reference 1080×1920 portrait**: matches `CATALOG.md § 3.1 MUST 6` canonical canvas reference for the 17 catalog elements.
  
  Generated as authored YAML (not via in-Editor compose) with stable fileIDs (1000000-10000003 range) and refs to `Theme_Default.asset` (guid `a3fdf09a2ee383b4cb83fe4eb5d821f6`) + `KitforgeThemeBinder.cs` (guid `4f7904bff34ba1b40b9a979213d4c9d8`) + Unity built-ins (`CanvasScaler` `0cd44c1031e13a943bb63640046fad76` · `GraphicRaycaster` `dc42784cf147c0c48a680349fa168899` · `Image` `fe87c0e1cc204ed48ad3b37840f39efc` · `EventSystem` `76c392e42b5098c458856cdf6ecaaaa1` · `StandaloneInputModule` `4f231c4fb786f3946a6b90b886c48677`). Unity import logged 10ms PrefabImporter success on first refresh; auto-generated `.meta` `effca62da40d5fb438ed0791c1495d8c`. Pending validation: smoke test (drop into empty scene → Play → no LogError on Console).

- **M4.7-bis.QA — QA Suite editor tool — sequential structural integrity scanner for demo content.** New `Tools → Kitforge → UI Kit → QA Suite` window (UI Toolkit, `Editor/QA/`) auto-discovers all demo scenes under `Assets/Catalog_*/` + their `Prefabs/` subfolders + builder MenuItems matching `Tools/Kitforge/UI Kit/Build *` pattern (via `TypeCache.GetMethodsWithAttribute<MenuItem>`). Pluggable check architecture: `IQAIntegrityCheck` interface auto-discovered via `TypeCache.GetTypesDerivedFrom`. Three checks ship in v0.9.0-alpha: `MissingReferenceCheck` (broken serialized refs detected via `objectReferenceInstanceIDValue != 0 && objectReferenceValue == null` — distinguishes broken-link from intentionally-unassigned), `MissingScriptCheck` (null MonoBehaviours via `GameObjectUtility.GetMonoBehavioursWithMissingScriptCount`), `UIKitManagerCheck` (wraps existing `UIKitValidator.ValidateActiveScene` for scene targets). Toolbar buttons: `Discover` / `Rebuild Samples` / `Rebuild + Run All` / `Run All` / `Run Selected` / `Clear` — `Rebuild + Run All` closes the regenerate-then-validate loop in 1 click (executes the 6 builder MenuItems sequentially via `EditorApplication.ExecuteMenuItem` + `AssetDatabase.Refresh` + re-discover + scan; replaces the manual `Build Group X Sample` ritual × 6 + manual scene-by-scene eyeball). Progress bars for both rebuild and scan phases; final log `[QASuite] Run complete · scanned N · X errors · Y warnings`. Validates 23 targets clean on M4.7-bis.B.5 baseline (6 scenes A-E + ThemePresets + 17 prefabs across `Assets/Catalog_*/Prefabs/`). Adapt-easy: drop a new `IQAIntegrityCheck` impl into `Editor/QA/Checks/` → `TypeCache` auto-discovers without window/runner changes; new `[MenuItem("Tools/Kitforge/UI Kit/Build *")]` builder appears automatically in rebuild sweep. Sibling edit: `UIKitValidator.ValidationReport` exposes `IReadOnlyList<string> Errors / Warnings` properties (2-line addition) so the QA check can adapt validation output without parsing `Format()` strings — no API removal, no buyer breakage. Editor-only, zero Runtime impact. Pattern saved to ecosystem `tools_patterns_library.md` as `qa-suite-integrity-scanner`. Out of scope v0.9.0-alpha (deferred M4.X cluster): PlayMode runner for runtime exception capture during popup spawn cycles (current scope is EditMode-static structural integrity only); JSON report export to `Library/QA/Reports/` for CI integration; `ThemeBindingCheck` to detect `ThemedImage._image` / `ThemedText._fontSlot/_colorSlot/_sizeSlot` consistency drift; per-target exclusion manifest if granular skip is needed (auto-discovery is sufficient for v1).

- **M4.1 — Theme presets (assets + infra prep; contract validation deferred to M4.7).** 4 additive `UIThemeConfig` color slots (`TertiaryColor`, `MutedColor`, `TextOnPrimary`, `TextOnAccent`) — research-aligned with Material 3 paired On-color tokens + Apple HIG 4-tier label ladder. Closes the slot-coverage gap audit (3 buyer-facing CTAs in GameOver + LevelComplete + MainMenu reused `SecondaryColor` for tertiary buttons; on-color tokens were missing for label contrast on Primary / Accent backgrounds). `Samples~/Catalog_M4_ThemePresets/` ships `ThemeSwitcherSample.cs` + `Tools/Kitforge/UI Kit/Build M4.1 — Theme Presets` builder. Builder generates `Theme_Casual.asset` (bright/saturated palette, Royal Match / Disney Getaway Blast vibe) + `Theme_Premium.asset` (dark/desaturated palette, Hades / Genshin menu vibe) by cloning `UIThemeConfig_Default`'s sprite / font / audio refs and overriding only the 16 color slots. `ThemePresetsDemo.unity` materializes a 3-option dropdown + Show GameOver / Show LevelComplete buttons wired through the `ThemeSwitcherSample` MonoBehaviour. The `_themes` field is a `List<ThemeOption>` so buyers add a 4th theme by dragging a new `UIThemeConfig.asset` to the Inspector list — no script edits required. Sample asmdef `KitforgeLabs.MobileUIKit.Samples.CatalogM41ThemePresets` references `KitforgeLabs.MobileUIKit` + `KitforgeLabs.MobileUIKit.Catalog` + TextMeshPro. Requires Group B + Group C samples imported + built (stubs + GameOver / LevelComplete prefabs).

### Known issues

- **M4.X cluster — items deferred post-`v1.0.0-rc` pending buyer feedback (speculative slot proliferation rejected per Decision Protocol §4 Contrarian + ecosystem §5 no-over-engineering).** None of the items below has a current consumer asking for it; ship `v1.0.0-rc` without inventing slots and add only when 2nd consumer or real complaint surfaces. (a) **`ActiveStateColor` slot** — `PrimaryColor` is currently reused for widget active states (Slider Fill + Toggle Check in Group D, BtnPlay in Group E). Decoupling would require a new color token + 3 palette decisions (Default/Casual/Premium) + 1 enum entry + 1 arm in `ThemedImage.TryResolveColor`. (b) **`HandleColor` slot** — Slider thumb is `Color.white` inline today (no theme slot semantically maps to "draggable thumb"). (c) **`LoadingBg` / `MainMenuBg` dedicated slots** — both screens reuse `BackgroundDark` (Group E B.4 collapse). If buyer wants distinct LoadingScreen tone vs MainMenuScreen tone, separate slots needed. (d) **`SliderTrack` / `ToggleBg` / `ButtonSecondaryDark` slots** — Group D widgets + Group E secondary buttons reuse `MutedColor`. Documented mild stretch (Material 3 `Muted` was designed as inert background, not as widget chrome). (e) **`HUDCurrency.MaxCapLabel` migration** — "/5" suffix uses raw `HUDCapLabelColor` static; HUD shell is intentionally non-themed (HUDBackgroundColor semi-transparent black). Migrating would require new `MutedOnDark` slot (single-consumer, violates §5 promote-when-2nd-consumer) or `TextSecondary` reuse with readability Δ ~0.4 on dark bg.

- **`HUDCurrency` audit dedup** — closed M4.X (HUDCoins + HUDGems now both surface as catalog targets via filename-disambiguated labels — see `### Fixed` entry).

- **Bootstrap Defaults palette research — `Theme_Default._secondaryColor` Δ 0.5 vs builder fallback** — D2 deferral from M4.7-bis.A still open: Default `_secondaryColor (0.35, 0.40, 0.50)` slate-blue does not match builder `ButtonSecondaryColor (0.85, 0.86, 0.90)` light gray fallback. Reconciliation requires cross-theme palette decision (which color is canonical for Casual + Premium too). Defer to M3c UX audit (visual review milestone — buyer-facing palette decisions belong there, not in M4.X). No buyer-visible regression today: ThemedImage runtime read produces `theme._secondaryColor` correctly; the gap is editor-time visual delta only.

### Notes

- **Migration: zero buyer action required.** The 4 new `UIThemeConfig` slots are additive `[SerializeField]` fields; existing `UIThemeConfig.asset` files retain prior behavior with the new properties auto-populated to research-aligned defaults (`TertiaryColor #738EBF` slate-blue · `MutedColor #6B727F` blue-tinted gray · `TextOnPrimary #FFFFFF` · `TextOnAccent #1A1A1F`). Properties exposed via `Theme.TertiaryColor` / `MutedColor` / `TextOnPrimary` / `TextOnAccent` getters. Catalog popups that today reuse `SecondaryColor` for third-tier buttons (`GameOver.Restart`, `LevelComplete.MainMenu`) will progressively migrate to `TertiaryColor` over M4.7+ once builders wire `ThemedImage` on every `Image`.
- **Doc drift fix (text-only edits).** `README.md` § Status now reflects the re-sequenced path (M3a → M4 → M3c → M5/RC); `Documentation~/Specs/CATALOG.md` § 9 milestone table updated to the 7-milestone path.
- **Migration M5.1 — `Theme_Default.asset` path change (buyer action required for upgrades from `v0.9.0-alpha` or earlier).** Buyers with an existing `Assets/Settings/UI/UIThemeConfig_Default.asset` (created by Bootstrap Defaults pre-M5.1) must delete that orphan file post-update. The package now ships `Theme_Default` at `Packages/com.kitforgelabs.mobile-ui-kit/Runtime/Theme/Presets/Theme_Default.asset` and all kit builders + `KitforgeThemeBinder` reference the package path. The orphan file in `Assets/Settings/UI/` is no longer consumed by anything; safe to delete. If the buyer customized colors / sprites in the orphan asset, copy values to the new package asset (or to a personal Theme variant) before deletion. Bootstrap Defaults will NOT duplicate-create at the old buyer path on next run — it now writes only to the package path. **DTO refactor was scoped out post-discovery** — empirical audit of all 13 DTOs (Confirm/Pause/Tutorial/NotificationToast/Reward/Shop/NotEnough/DailyLogin/LevelComplete/GameOver/Settings/Loading/MainMenu) found them already Inspector-friendly (`[Serializable]` + public fields + parameterless ctor + sensible defaults; grep `new \w+PopupData\(` returned 0 hits with positional args — all callsites use object-initializer syntax). The audit's R-U-C2 "constructor-style → Inspector-friendly" framing was an inherited assumption that did not match the current codebase state; refactor cancelled. Theme path move is the only buyer-impacting change in this milestone.

### Audit

- **M3c UX audit closed 2026-05-09 — pure audit, zero code, zero tag.** 4 audit blocks, 130 findings total: B1 4-roles original plan (audit history, lente reframed mid-Block); B2 50 user findings as game designer hype-casual workflow (1 prototipo/semana, no técnico); B3 50 UX specialist findings + Kitforge Hub design from scratch; B4 30 dev validation findings + plan aterrizaje 7 sub-milestones. Output: `~/.claude/memory/kitforge_ux_audit_2026-05.md`.
- **7 UX Pillars LOCKED for the package** (rectores permanentes): P1 Single front door · P2 Show, don't tell · P3 Progressive disclosure · P4 No dead-ends · P5 Unity-native · P6 Predictable defaults · P7 Persistent context.
- **7 Hub features driven** (deliverable central v1.0.0-rc): F-UX-1 Kitforge Hub Editor window (UIToolkit, 5 tabs vertical sidebar Setup/Catalog/Theme/Test/Help, dockable, auto-open on import) · F-UX-2 Setup wizard 3-step persistente · F-UX-3 Catalog Browser (visual grid 17 prefabs + thumbnails + drag-to-scene + side panel) · F-UX-4 Theme Studio (live preview + slot categorías + presets dropdown + Capture All) · F-UX-5 Popup Test Launcher (Play-mode-only · DTO mock-fill · Force scenarios) · F-UX-6 Communication actionable (LogError + ShowNotification toasts + health badge) · F-UX-7 Polish post-RC (P1).
- **5 Premises Runtime non-Hub** (M5.1 critical path): R-U-C1 `KitforgeRoot.prefab` + `KitforgeThemeBinder` MonoBehaviour (theme single-field) · R-U-C2 DTO refactor BREAKING 17 files (Inspector-friendly) · R-U-C3 `HUDSimple`/`HUDEnergySimple`/`HUDTimerSimple` (sin servicios) · R-U-C5 Theme presets move a Runtime + multi-font (3 slots) · R-U-C7 `SafeAreaFitter` MonoBehaviour minimal (**inverts Non-goal #7** — `Screen.safeArea` applied automatically; mobile reality 2026).
- **4 critical decisions LOCKED**: D1 Hub in v1.0.0-rc (split por sub-milestones internos) · D2 UIToolkit (no IMGUI) — Unity 6 dirección + dockable + USS theming + future-proof · D3 17 kit-only catalog en v1.0.0-rc + `[KitforgeCatalogEntry]` API en v1.1.0 · D4 `EditorWindow.ShowNotification` text-only standard + Hub auto-foreground (no in-toast button per ShowNotification API limit).
- **M5/RC plan revised from 1-2 sessions to ~10-11 sessions split in 7 sub-milestones M5.1-M5.7**: M5.1 Premises Runtime → M5.2 Hub shell + Setup tab → M5.3 Catalog Browser → M5.4 Theme Studio → M5.5 Test + Communication + audit checks (UIServicesRefsCheck + ThemeReskinDeltaCheck + ThemeConsistencyCheck) → M5.6 HUDSimple + SafeAreaFitter + Help tab + Tools menu jerarquía + legacy soft-deprecate → M5.7 final triple gate + tag `v1.0.0-rc`. Opción A LOCKED (full scope, no Hub-light split).
- **New rule for all future audits/recommendations on this kit**: workshop-drawer test (`~/.claude/memory/feedback_audit_priority_tool_first.md`). Tool reliability + prefab practicality + reorganization come FIRST. Asset Store / demos / buyer-facing docs are LAST until user explicitly opens that scope. Applied retroactively to Block 1 of this audit (50% findings store/demos/buyer-evaluator framing dropped to P2).

## [0.8.1-alpha] — Unreleased (M3b — UIAnim per-element polish, FOUNDATIONAL REVIEW IN PROGRESS)

> **2026-05-06 — M3b sessions 1+2 reverted.** Both polish implementations (DailyLogin item 1/8 cell cascade + LevelComplete item 2/8 stars cascade & score rollup) were built on a misdiagnosed "DOTween spawn-frame race" premise locked in session 1. When item 2/8 first samples-green failed, escalating workarounds (warmup constant → `_polishInitialDelay` field → coroutine-deferred yield) were stacked over the same wrong premise rather than re-questioning fundamentals. User push-back surfaced that `_tween` ecosystem (production-tested) handles the same domain with simple Activate/Instantiate + OnEnable Show + `_initialDelay`-when-needed — no warmups, no yields. If the alleged race were a real DOTween-level behavior, `_tween` patterns would suffer it equally. They do not. **Reverted scope:** `UIAnimPopupBase` 3-hook surface + `SpawnFrameWarmup` constant + convention block; `UIAnimDailyLoginPopup` cascade implementation; `UIAnimLevelCompletePopup` cascade + rollup implementation; `CatalogGroupCBuilder.BuildDailyLoginPopup` 7 demo cells + helpers + animator wiring; `CatalogGroupCBuilder.BuildLevelCompletePopup` animator wiring; `LevelCompletePopup.OnShow` SetScoreTarget seam. **Pending before re-attempt:** foundational review of kit's `UIAnimPopupBase` lifecycle vs `_tween` `UIAnimBase` lifecycle (PlayShow-from-OnShow vs Show-from-OnEnable, Sequence vs Tweener, manual SetLink vs RegisterTween auto-link); empirical Debug.Log instrumentation comparing cold-spawn vs warm-spawn behavior in both ecosystems; revisit polish strategy across all 8 items + all 5 Catalog groups (A-E) to align with `_tween` validated patterns. Lesson captured in `feedback_workflow.md` → "Inherited framing requires empirical re-verification before propagation". No content lands in this Unreleased section until the foundational review concludes and a re-validated polish pattern is locked.

## [0.8.0-alpha] — 2026-05-06 BREAKING

> M3 — Group E catalog (LoadingScreen + MainMenuScreen) + OnUpdate infra dispatch fix. Adds 2 screen elements, `UIAnimScreenBase` shared animator base, 8 new `UIThemeConfig` slots, `CatalogGroupEBuilder` (with Group C-built guard) + `GroupE_BootDemo`, and `GroupEDemoHost` sample with boot chain (Loading → MainMenu → DailyLogin auto-trigger via `DailyLoginFlow.ShowIfDue`). Closes latent `OnUpdate()` infra gap (`PopupManager` dispatch) — `RewardPopup.AutoClaim` and `DailyLoginPopup` countdown now functional via infra dispatch (no per-popup `Update()` workaround). **292 EditMode tests green** on fresh compile (+31 since `v0.7.0-alpha`: 14 LoadingScreen + 17 MainMenuScreen). Closes Milestone M3 of the path to `v1.0.0-rc`. Next: M4 hardening + docs final → `v1.0.0-rc`.

### Fixed

- **`PopupManager._backdrop` sibling-order — fixed.** `PopupManager.ApplySiblingOrder()` now calls `_backdrop.transform.SetAsFirstSibling()` after reordering the active stack, so the global backdrop renders BENEATH every popup as intended (line 108, `Runtime/Core/PopupManager.cs`). Closes the structural blocker that previously made the backdrop intercept every click meant for the popup or for any UI behind it — visible symptom in the Group E demo: `DailyLoginPopup` auto-spawning over `MainMenuScreen` locked input on both layers (Continue / Claim / Watch-Ad on the popup, all 4 actions on the screen). Verification: re-ran `Build Group X Sample` + first-button-click + popup-dismiss for all 5 groups A→E — green; 292 EditMode tests stayed green on fresh compile. The fix lives in `ApplySiblingOrder()` itself (called from every `Show` via `InsertActiveByPriority`); `Dismiss` and `TryEvictLowerPriority` did not need a redundant call because removing a popup never raises the backdrop above remaining popups (the backdrop's index 0 stays index 0 when later siblings disappear).
- `PopupManager.Update()` dispatch added — iterates `_activeStack` and calls `OnUpdate()` on each active popup. Closes latent infra gap present since v0.1.0: `UIModuleBase.OnUpdate()` was declared as `public virtual` but never invoked by any manager. Previously affected `RewardPopup.AutoClaim` (silently broken since v0.5.0) and `DailyLoginPopup` countdown (workaround applied locally since v0.6.0).
- `RewardPopup.AutoClaim` now functional in production — timer ticks via infra dispatch. No code change in `RewardPopup.cs`; repair is a consequence of the `PopupManager` fix above.
- `DailyLoginPopup` local `Update()` workaround removed — double-tick risk eliminated. `OnUpdate()` override drives `AdvanceCountdown` exclusively via infra dispatch.
- `HUDTimer` / `HUDEnergy` — `OnUpdate()` indirection collapsed: private method renamed inline into `Update()`. Docstrings updated to remove stale workaround notice. No behavioral change.
- `GroupEDemoHost.TryAutoTriggerDailyLogin` no longer instantiates `DailyLoginPopup` manually via `Instantiate` + `Initialize` — routes through `DailyLoginFlow.ShowIfDue(PopupManager, UIServices, ...)`. Manual instantiation bypassed `PopupManager` (no backdrop, no stack tracking, broken back-button); the flow helper preserves all three. Aligns the sample with the kit's "always spawn through `PopupManager`" contract.
- **All 5 group sample builders (`CatalogGroupABuilder` through `CatalogGroupEBuilder`) now generate a `Main Camera` GameObject (with `Camera` + `AudioListener`) in their demo scenes.** Closes regression present since `v0.4.0-alpha` (Group A): every `Build Group X Sample` produced a scene without a camera, triggering Unity's *"Display 1: No cameras rendering"* warning that dominated the Game View on Play — buyer perceived as black screen. Canvas was technically rendering (`ScreenSpaceOverlay`) but the warning hid it; audio context was also missing (no `AudioListener`). Fix introduces shared helper `EditorSceneFactory.CreateMainCamera(Scene)` (`Editor/Generators/EditorSceneFactory.cs`) invoked by all 5 builders before canvas creation. Re-import the sample of any group and re-run its `Build Group X Sample` to regenerate the scene. New samples-green gate added to ecosystem (`feedback_workflow.md` → "Verify-all-samples gate") to prevent recurrence.
- `GroupEDemoHost.BuildDailyLoginTemplate` now sets `CloseOnBackdrop = true` on the `DailyLoginPopupData` template. Restores by-design tap-out dismiss intent for the demo template (without the field set, `BackdropButton.interactable` stays false and the internal backdrop button consumes taps as no-ops). Note: `DailyLoginFlow.ShowIfDue` is intentionally one-shot per launch (guarded by `_shownThisLaunch`) AND one-shot per UTC day (guarded by `IProgressionService.GetDailyLoginState().AlreadyClaimedToday`) — re-running `Boot Demo` after a successful claim will not re-spawn the popup. This is by-design kit behavior (DailyLogin is a launch-window prompt, not a recurring HUD) and is not a bug.

### Added

- `LoadingScreen` spec + code + tests delivered — `Runtime/Catalog/Screens/Loading/LoadingScreen.cs` + `UIAnim_LoadingScreen.cs` + `Tests/Editor/LoadingScreenTests.cs` (14 tests). Buyer-driven `SetProgress(float)`, optional spinner + progress bar tween, `OnProgressComplete` / `OnMinDisplayTimeElapsed` advisory events, no required services.
- `UIAnimScreenBase` new internal base class (`Runtime/Catalog/_Internal/UIAnimScreenBase.cs`) — shared base for all screen fade animators; mirrors `UIAnimPopupBase` pattern for screens.
- `MainMenuScreen` spec written — `Documentation~/Specs/Catalog/MainMenuScreen.md`. Entry-point screen with 4 hide-able action buttons, C# event model, daily-dot polling, optional HUD child slots.
- `CATALOG_GroupE_DELTA.md` written — 8 new `UIThemeConfig` slots, `Screens/` folder convention, Group E builder spec, `GroupE_BootDemo` acceptance criteria.

- `MainMenuScreen` code + tests delivered — `Runtime/Catalog/Screens/MainMenu/MainMenuScreen.cs` + `UIAnim_MainMenuScreen.cs` + `Tests/Editor/MainMenuScreenTests.cs` (17 tests). 5 C# events, daily-dot 30s poll + silent degrade, HUD child slots, `UIAnim_MainMenuScreen` with panel slide-from-bottom + configurable `_staggerItems` entry array.
- `UIThemeConfig` — 8 new Group E slots added: `BackgroundMainMenu`, `LogoMainMenu`, `IconPlay`, `IconShop`, `IconSettings`, `IconDaily`, `IconLoading` (Sprite) + `LoadingBarColor` (Color).
- `CatalogGroupEBuilder` — `Editor/Generators/CatalogGroupEBuilder.cs`, MenuItem `Tools/Kitforge/UI Kit/Build Group E Sample`. Builds `LoadingScreen.prefab` + `MainMenuScreen.prefab`, wires both into `UIManager._screenPrefabs`, builds `GroupE_BootDemo.unity` with full UIRoot (UIManager + PopupManager + UIRouter + ToastManager + UIServices + InMemoryProgressionService from Group C sample). Wires `DailyLoginPopup.prefab` from `Catalog_GroupC_Demo` if present.
- `CatalogGroupEBuilder.CheckGroupCBuilt()` precondition guard — aborts the Group E build with a buyer-actionable LogError if `Assets/Catalog_GroupC_Demo/DailyLoginPopup.prefab` is missing (Group C sample not built / not imported). Prevents silent half-wired demos where `GroupEDemoHost.TryAutoTriggerDailyLogin` would fail at runtime due to missing popup registration. Buyer fix: import Group C sample + run `Build Group C Sample` before `Build Group E Sample`.
- `GroupEDemoHost` sample — `Samples~/Catalog_GroupE_Screens/GroupEDemoHost.cs` + asmdef. Boot coroutine: Loading (simulated progress) → Replace to MainMenu → DailyLogin auto-trigger after MainMenu show animation completes (subscribed via new `MainMenuScreen.OnShown` event). 3 `[ContextMenu]` QA triggers.
- `MainMenuScreen.OnShown` event — fires after `OnShow` completes (animator's `PlayShow` callback, or immediately if no animator wired). Hosts subscribe to chain post-show actions like auto-popups (e.g., `GroupEDemoHost` chains DailyLogin auto-trigger). Removes the prior race condition where popups spawned during MainMenu's slide+stagger entry animation, causing visual overlap.
- `GroupE_BootDemo` scene — `CatalogGroupEBuilder` now generates a fixed in-scene Instructions Text panel anchored top of the UICanvas: `"DEMO: Right-click GroupEDemoHost in Hierarchy → ContextMenu (Boot Demo / Show MainMenu / Trigger DailyLogin). Button clicks log to Console (Window → General → Console)."`. Discoverability fix for buyers evaluating the kit who would otherwise not find the `[ContextMenu]` triggers nor know that button clicks emit Debug.Log guidance to the Console (instead of visible UI feedback) — Group E demo intentionally keeps Settings/Shop/Play/Back as no-op handlers per kit contract (`§3.MUST 9 — service decoupling, host wires chains`).

_(GroupE_BootDemo.unity + prefabs ship via builder — compile + run `Build Group E Sample` to generate. M3a tag ready after import + smoke test.)_

## [0.7.0-alpha] — 2026-05-04 BREAKING

> Group D — Player Data catalog. Adds 1 catalog element (SettingsPopup with 5 controls), 2 new service interfaces (`IPlayerDataService` rewritten + new `IUILocalizationService`), 1 new helper (`RewardFlow.GrantAndShow` single — capability-gate re-audit promoted from Group C deferral), 1 Runtime impl (`PlayerPrefsPlayerDataService`), 1 Runtime helper (`DailyLoginPersistence` for cross-session DailyLogin streak), `Build Group D Sample` builder + 1 prefab + demo scene + 9 `[ContextMenu]` scenarios, 2 in-memory sample stubs (`InMemoryPlayerDataService` + `InMemoryLocalizationService`), DailyLogin retro-fit (`InMemoryProgressionService` opt-in `_playerDataServiceRef`), and Game Wiring sample REVIVED (parked since v0.4.1-alpha) with 8 `Stub*` services + VContainer-gated asmdef. **261 EditMode tests green** on fresh compile (+55 since `v0.6.0-alpha`). Closes Milestone M2 of the path to `v1.0.0-rc`. **8 canonical kit-side keys** frozen at `v1.0.0-rc` (5 settings + 3 dailylogin) under `kfmui.<scope>.<name>` namespace.

### Breaking changes — migration guide

**`IPlayerDataService` rewritten — primitive key-value surface replaces stub-era profile API:**

The previous interface (`GetProfile`/`SaveProfile`/`AddXp`/`OnProfileChanged`) was a speculative stub that never reached production consumers — no popup, helper, or sample referenced it beyond the parked `Samples~/GameWiring/` stub. The new surface mirrors `PlayerPrefs` (12 primitive-typed methods) and is the canonical persistence path for all kit popups starting at M2.

```diff
- playerData.GetProfile()
- playerData.SaveProfile(profile)
- playerData.AddXp(50)
- playerData.OnProfileChanged += handler
+ playerData.GetFloat("kfmui.settings.musicVolume", 1f)
+ playerData.SetFloat("kfmui.settings.musicVolume", 0.8f)
+ playerData.GetInt("kfmui.dailylogin.streak", 0)
+ playerData.Save()  // explicit flush; Unity lifecycle auto-flushes on app pause/quit
```

`PlayerProfile` DTO removed. Future profile-style data (avatar, level, XP) — if needed — composes via primitive keys (`kfmui.profile.avatarId`, `kfmui.profile.level`, etc.).

**Migration impact: zero for production consumers** — the old surface was unused. Only the parked Game Wiring sample referenced it; that sample is revived later in M2 against the new contract.

### Added

- `IPlayerDataService` (primitive surface): `GetInt/SetInt/GetFloat/SetFloat/GetString/SetString/GetBool/SetBool/Has/Delete/Save/Reload`. Method-per-type avoids reflection and Newtonsoft dependency. Spec lockfile: `Documentation~/Specs/Services/IPlayerDataService.md`.
- `PlayerPrefsPlayerDataService` (Runtime, default impl): `MonoBehaviour` wrapping `UnityEngine.PlayerPrefs`. `GetBool` ↔ `GetInt(0|1)` mapping. `Reload()` is no-op (PlayerPrefs has no in-memory cache distinct from disk).
- `FakePlayerDataService` (Tests/Editor/Helpers): in-memory test seam. Dictionary-backed. Type-mismatch on read throws `InvalidCastException` (fail-loud during dev). Used by all `IPlayerDataService` tests.
- `IPlayerDataServiceTests`: 16 EditMode tests covering surface roundtrip + edge cases (default fallback, `Has` after `Delete`, `Reload` clears, type mismatch throws, null-key throws).
- `IUILocalizationService`: 4-member dispatch interface for active language + change events (`CurrentLanguage`, `AvailableLanguages`, `OnLanguageChanged`, `SetLanguage`). Decoupled from `IPlayerDataService` — buyer wires "PlayerData → SetLanguage" boot sync themselves. Kit does NOT translate strings (Non-goal #2 — BYO localization); the service is the dispatch hub for buyer's localization layer (Unity Localization Package, I2, custom). Spec lockfile: `Documentation~/Specs/Services/IUILocalizationService.md`.
- `FakeLocalizationService` (Tests/Editor/Helpers): in-memory test seam. Constructor fail-loud on empty/null `availableCodes` or `currentCode` not in available. `SetLanguage` same code = no-op no event; unknown code throws `ArgumentException`.
- `IUILocalizationServiceTests`: 10 EditMode tests covering constructor validation, surface coverage, and event subscription edge cases (same-code no-op, multi-subscriber, unsubscribe).
- `UIServices.Localization` slot + `_localizationServiceRef` Inspector field + `SetLocalization()` setter. `Validate` ContextMenu now reports 8/8 services (was 7/7).
- `SettingsPopup` runtime: `UIModule<SettingsPopupData>` with 5 standard settings (music volume, sfx volume, language picker, notifications toggle, haptics toggle) backed by `IPlayerDataService` for persistence and `IUILocalizationService` for live re-skin trigger. 5 events (`OnMusicVolumeChanged`, `OnSfxVolumeChanged`, `OnLanguageChanged`, `OnNotificationsChanged`, `OnHapticsChanged`) + `OnDismissed`. Slider drag emits real-time events for live audio preview. `OnHide` belt-and-braces `PlayerData.Save()` flush. `IsDismissing` guard suppresses control changes during hide animation. Spec lockfile: `Documentation~/Specs/Catalog/SettingsPopup.md`.
- `SettingsPopupData` DTO + `LanguageOption` POCO. 5 `Show<X>` visibility flags (default all true) + `Title` / `CloseLabel` strings + buyer-supplied `LanguageOptions[]` (auto-hides picker if null/empty).
- `UIAnimSettingsPopup` 3-line stub extending `UIAnimPopupBase` (Group B/C convention — no bespoke choreography).
- 5 canonical PlayerData keys exposed as `SettingsPopup.Key<X>` constants: `kfmui.settings.musicVolume`, `kfmui.settings.sfxVolume`, `kfmui.settings.language`, `kfmui.settings.notifications`, `kfmui.settings.haptics`. Frozen at `v1.0.0-rc` per `IPlayerDataService.md` D8 migration policy.
- `SettingsPopupTests`: 19 EditMode tests covering DTO defaults, control change events + PlayerData writes (5 controls), during-dismiss suppression (4 control types), `OnHide` Save flush, re-Bind clears subscriptions, close/backdrop/back-press dismiss flow, saved-language fallback warning.
- `FakePlayerDataService.SaveCallCount` + `ReloadCallCount` test seam (internal counters) for assertions on flush/reload calls.
- `DailyLoginPersistence` static helper (`Runtime/Services/DailyLoginPersistence.cs`): canonical 3-key persistence (`kfmui.dailylogin.currentDay` int, `kfmui.dailylogin.lastClaimUtc` ISO 8601 string, `kfmui.dailylogin.doubledToday` bool). `Load(IPlayerDataService, ref DailyLoginState)` + `Save(IPlayerDataService, DailyLoginState)` static API. Buyer with custom `IProgressionService` impl reuses the same persistence pattern. ISO 8601 round-trip handles `DateTime` over the primitive surface (no Newtonsoft dep needed). Null-`IPlayerDataService` no-ops both methods (graceful degradation).
- `DailyLoginPersistenceTests`: 5 EditMode tests covering null-PlayerData no-op (load + save), full state round-trip, default `LastClaimUtc` ↔ empty-string mapping, partial state load.
- `InMemoryProgressionService` (Samples~) retro-fitted with optional `_playerDataServiceRef` Inspector field. When wired, daily login state hydrates from `IPlayerDataService` on Awake and persists on every `MarkDailyLoginClaimed` / `MarkDailyLoginDoubled`. When null, falls back to in-memory backing (original v0.6.0-alpha behavior — graceful degradation). Total canonical kit-side keys at `v1.0.0-rc`: 8 (5 Settings + 3 DailyLogin).
- **Sample migration note for PACHINKO and other v0.6.0-alpha early-adopters**: after re-importing the v0.7.0-alpha sample via Package Manager, manually delete `Assets/Samples/Kitforge Mobile UI Kit/0.6.0-alpha/` to avoid duplicate-type compile errors (`InMemoryProgressionService` exists in both versioned folders under the same namespace until the older folder is removed).
- `RewardFlow.GrantAndShow` (single) helper: `GrantAndShow(PopupManager popups, IEconomyService economy, RewardPopupData reward, Action<CurrencyType, int> onClaimed = null)`. Shows ONE `RewardPopup`, credits economy on claim (skipping `ItemCurrencySentinel` for Item/Bundle kinds — host resolves), forwards `(currency, amount)` to optional `onClaimed` callback. Same shape as sibling `GrantAndShowSequence` (`PopupManager`-based). Capability-gate re-audit (Group D 2026-05-04, `/_checker as user` lens): promoted from `v0.6.0-alpha` deferral after `CatalogGroupBDemo` was found to repeat the equivalent manual pattern 3 times (`SpawnReward` L260, `SpawnChainReward` L167, `HandleWatchAdRequested` L244). Silent-bug risk (forgetting item-sentinel skip credits items as currency) makes this helper a buyer-ergonomics win.
- `RewardFlowTests` +5 EditMode tests for `GrantAndShow`: null `popups` / null `economy` / null `reward` validation no-ops, Coins reward credits economy + fires `onClaimed`, Item sentinel does NOT credit but still fires `onClaimed` (host-resolved).
- **Demo update OUT-of-scope at M2 close** — the manual `SpawnPopup<RewardPopup>` pattern in `CatalogGroupBDemo.cs` is intentionally pedagogical (shows under-the-hood mechanics for buyers using non-`PopupManager`-registered prefabs). The new `RewardFlow.GrantAndShow` is buyer-facing API for `PopupManager`-registered prefab flows. M4 QUICKSTART will document both paths under "Pattern: 1-line reward grant" + cross-reference.
- `CatalogGroupDBuilder` (Editor menu `Tools/Kitforge/UI Kit/Build Group D Sample`): generates `Assets/Catalog_GroupD_Demo/` with `SettingsPopup.prefab` (1 prefab — Title + 5 control rows + Close button + backdrop, refs wired via `SerializedObject`) + `Catalog_GroupD_Demo.unity` scene (UICanvas + EventSystem + UIServices wired with `PlayerPrefsPlayerDataService` for real cross-session persistence + `InMemoryLocalizationService` Sample stub + Demo MonoBehaviour with all `[ContextMenu]` triggers).
- Group D sample (`Samples~/Catalog_GroupD_PlayerData/` + `package.json` `samples[]` entry): `CatalogGroupDDemo.cs` MonoBehaviour with 9 `[ContextMenu]` scenarios (3 Settings spawn variants + 2 PlayerData diagnostic prints + 1 reset-defaults + 3 language-switch triggers), `Stubs/InMemoryPlayerDataService.cs` (Dictionary-backed MonoBehaviour for repeat-Play stateless demos — buyer alternative to PlayerPrefs), `Stubs/InMemoryLocalizationService.cs` (Inspector-editable language list + initial-language validation in Awake; raises `OnLanguageChanged` on `SetLanguage`), `KitforgeLabs.MobileUIKit.Samples.CatalogGroupD.asmdef`.
- **Game Wiring sample REVIVED** (parked since `v0.4.1-alpha`, restored in M2 2026-05-04). `Samples~/GameWiring/` re-added to `package.json` `samples[]` ("Game Wiring (VContainer)"). All 8 `Stub*` services (`StubEconomyService` / `StubPlayerDataService` / `StubProgressionService` / `StubShopDataProvider` / `StubAdsService` / `StubTimeService` / `StubAudioRouter` / `StubLocalizationService`) updated to compile against current interface contracts (M1 `IEconomyService` v2 + M2 `IPlayerDataService` 12-method primitive surface + M2 `IUIAudioRouter` + `IUILocalizationService`). `GameWiringLifetimeScope.cs` registers all 8 services via VContainer `Register<IInterface, ImplClass>(Lifetime.Singleton)`. `README.md` rewritten: documents the 8 service contracts + buyer migration notes (v0.4.1 → v0.7.0) + `UIServicesBinder` pattern that bridges VContainer-resolved services to the kit's `UIServices` MonoBehaviour container (kit popups read via `UIServices`, not VContainer directly — preserves "VContainer optional" philosophy).
- **GameWiring asmdef gated behind VContainer presence**: `defineConstraints: ["KFMUI_HAS_VCONTAINER"]` + `versionDefines` mapping `jp.hadashikick.vcontainer >= 1.0.0` to `KFMUI_HAS_VCONTAINER`. Result: buyer who imports the sample WITHOUT VContainer in their project no longer hits compile errors — the asmdef is silently excluded from compilation. Buyer who installs VContainer (UPM scoped registry `https://package.openupm.com` → `jp.hadashikick.vcontainer`) gets the asmdef compiled in. Catches the v0.7.0-alpha-dev verification failure where importing the sample into a non-VContainer host (PACHINKO) broke compilation.

### Removed

- Old `IPlayerDataService` (`GetProfile/SaveProfile/AddXp/OnProfileChanged`) — see migration above.
- `PlayerProfile` DTO struct (`Runtime/Services/DTO/PlayerProfile.cs`) — superseded by primitive keys.

## [0.6.0-alpha] — 2026-05-04 BREAKING

> Group C — Progression catalog. Adds 5 catalog elements (DailyLogin / LevelComplete / GameOver popups + HUD-Energy / HUD-Timer), 1 of 3 planned helpers (`RewardFlow.GrantAndShowSequence` — sibling helpers deferred to Group D per capability-gate), `Build Group C Sample` builder + 5 prefabs + demo scene + 6 chain `[ContextMenu]` scenarios, 2 in-memory service stubs (`InMemoryProgressionService` + `InMemoryTimeService`), `IEconomyService` v1→v2 BREAKING migration with `CurrencyType.Energy` added, and `HUDCurrency` parameterized base replacing the `HUDCoins`/`HUDGems` sibling classes. **206 EditMode tests green** on fresh compile. Closes Milestone M1 of the path to `v1.0.0-rc`.

### Breaking changes — migration guide

**Block 1 — `IEconomyService` v1 → v2 method renames (sed/regex friendly):**

```diff
- economy.GetCoins()
+ economy.Get(CurrencyType.Coins)

- economy.GetGems()
+ economy.Get(CurrencyType.Gems)

- economy.SpendCoins(amount)
+ economy.Spend(CurrencyType.Coins, amount)

- economy.SpendGems(amount)
+ economy.Spend(CurrencyType.Gems, amount)

- economy.AddCoins(amount)
+ economy.Add(CurrencyType.Coins, amount)

- economy.AddGems(amount)
+ economy.Add(CurrencyType.Gems, amount)

- economy.OnCoinsChanged += handler
+ economy.OnChanged += (currency, value) => { if (currency == CurrencyType.Coins) handler(value); }

- economy.OnGemsChanged += handler
+ economy.OnChanged += (currency, value) => { if (currency == CurrencyType.Gems) handler(value); }
```

**Block 2 — Why no `[Obsolete]` shim.** `IEconomyService` v1 is removed cleanly — no transitional forwarders ship. Rationale: the parameterized API IS the contract; preserving v1 alongside v2 doubles the surface, masks the migration intent, and would bleed into v0.7+. Buyer pays the migration cost once at v0.6.0.

**Block 3 — Currency extension limit.** `CurrencyType` is a closed `public enum` with three sealed values (`Coins = 0`, `Gems = 1`, `Energy = 2`). Buyers cannot extend the enum from outside the package. Buyers with custom currencies (Tickets, Stars, Hearts) have three options: (a) fork the package and add enum values, (b) wait for an extension mechanism in a future version (no roadmap commitment), (c) reuse `Coins` / `Gems` / `Energy` as semantic aliases for their domain. A `(CurrencyType)42` cast at runtime returns 0 from `Get` / `CanAfford` and is a silent no-op for `Spend` / `Add` (no exception). Documented honestly so the buyer doesn't discover this in production.

**Block 4 — Prefab migration.** `HUDCoins.prefab` / `HUDGems.prefab` from the v0.5.0 Group B builder reference deleted classes (`HUDCoins`, `HUDGems`). After upgrade, the imported sample prefabs show "Missing Mono Script" in Inspector. Buyer steps:

1. Delete the Group B demo folder (`Assets/Catalog_GroupB_Demo/` if generated locally; `Assets/Samples/Kitforge Mobile UI Kit/0.5.0-alpha/` if imported via Package Manager).
2. Re-run `Tools/Kitforge/UI Kit/Build Group B Sample` (transitional — the Group B builder is updated in this release to produce `HUDCurrency`-based prefabs) OR `Tools/Kitforge/UI Kit/Build Group C Sample` (Phase B deliverable, ships with the v0.6.0-alpha tag).
3. Existing scenes referencing the old prefabs by GUID will break — buyer re-wires references manually (prefab GUIDs change because file paths change).

**Block 5 — Coupling: economy v2 + HUDCurrency are an inseparable migration.** v0.6.0-alpha bundles two breaking changes that cannot be reverted independently: (a) `IEconomyService` v1 → v2, and (b) `HUDCoins`/`HUDGems` classes → `HUDCurrency` parameterized base. A buyer who reverts (b) by restoring `HUDCoins.cs`/`HUDGems.cs` from git will get compile errors — the restored files reference v1 surface (`OnCoinsChanged`, `GetCoins`, ...) which v2 deleted. Reverting requires either reverting BOTH migrations together, or manually rewriting the restored HUDs to v2 (`OnChanged + filter`). Do NOT assume `git restore HUDCoins.cs` is enough.

### Added (Phase B — DailyLogin element 1/5)

- **`Runtime/Catalog/Popups/DailyLogin/`** — 5 files:
  - `DailyLoginPopupData.cs` — DTO with calendar-driven streak state. Fields: `Title` ("Daily Reward"), `RewardEntries: DailyLoginRewardEntry[]`, `CurrentDay` (1-based), `LastClaimUtc`, `AlreadyClaimedToday`, `DoubledToday` (FQ3), `MaxStreakGapDays` (1), `ClaimLabel` ("Claim"), `WatchToDoubleLabel` ("Watch ad to double"), `CloseOnBackdrop` (false). DTO defaults asserted in `DTO_Defaults_Match_Spec_Contract` test (FD3).
  - `DailyLoginRewardEntry.cs` — per-day calendar slot. `Rewards: RewardPopupData[]` (multi-reward day support per D8), `IsBigReward`, `AllowDouble`, `Label`.
  - `DailyLoginPopup.cs` — `UIModule<DailyLoginPopupData>`. Two CTAs (`Claim`, `Watch ad to double`) mutually exclusive per D5; back-press = silent dismiss; backdrop tap honors `CloseOnBackdrop`. Already-claimed state (D7) renders a HH:MM:SS countdown to UTC midnight, ticking via `Services.Time` once per `OnUpdate`. FQ1 auto-transition: when countdown crosses zero, popup re-queries `Services.Progression.GetDailyLoginState()` and flips to claim-ready inline (no close/re-open). FQ3: watch-to-double CTA stays visible in already-claimed state when `DoubledToday=false` and ad is ready. CurrentDay-beyond-RewardEntries.Length wraps modulo per D2/spec edge cases. Service binding follows § 4.5: `ITimeService` REQUIRED → null logs actionable LogError + aborts OnShow; `IAdsService` optional → null hides watch-to-double silently; `IProgressionService` optional (FQ1 auto-transition only) → null skips transition silently. Empty/null RewardEntries also aborts OnShow with actionable error.
  - `DailyLoginFlow.cs` — static helper `ShowIfDue(popups, services, configTemplate, onClaimed = null)` for D6 auto-trigger. Validates `PopupManager`/`IProgressionService`/`configTemplate` (null on any → § 4.5 LogError + return false). Queries `IProgressionService.GetDailyLoginState()`; returns false when `AlreadyClaimedToday=true`. Idempotent within app launch (FQ2): internal `_shownThisLaunch` flag prevents re-showing in same session even when due. Test helpers `ShownThisLaunchForTests` / `ResetForTests` / `ForceShownForTests` exposed via `internal` for unit coverage.
  - `UIAnimDailyLoginPopup.cs` — `: UIAnimPopupBase {}` shell (Group B precedent). Cell-cascade stagger animation deferred to builder/`_tween` polish session.
- **Test helpers** under `Tests/Editor/Helpers/`:
  - `FakeTimeService.cs` — `ITimeService` fake with `SetNow` / `Advance` setters. Default 2026-01-15 12:00 UTC.
  - `FakeAdsService.cs` — `IAdsService` fake with `SetRewardedReady` / `SetInterstitialReady` setters; `ShowRewardedAd` invokes callback synchronously with current ready state.
- **EditMode tests +26** under `Tests/Editor/`:
  - `DailyLoginPopupTests.cs` (19 tests) — Bind defaults / DTO contract (FD3) / Claim+Watch+Backdrop+Back paths / spam guard / already-claimed lockout / FQ1 auto-transition / FQ3 watch-double in already-claimed state / null-time service abort (§ 4.5) / empty-entries abort / countdown tick math / modulo wrapping.
  - `DailyLoginFlowTests.cs` (7 tests) — null-popups + null-services + null-progression + null-template § 4.5 paths / already-claimed short-circuit / FQ2 idempotency / test helper round-trip.
- **Spec update** — `Documentation~/Specs/Catalog/DailyLoginPopup.md`: `Services consumed` section renamed to `Service binding` and cites `CATALOG_GroupC_DELTA.md` § 4.5; QA scenarios 17-18 added covering null-required-service abort (popup) and null-progression return-false (helper).

### Added (Phase B — LevelComplete element 2/5)

- **`Runtime/Catalog/Popups/LevelComplete/`** — 3 files:
  - `LevelCompletePopupData.cs` — DTO with stars/score/best-score/CTA visibility flags and a `Rewards: RewardPopupData[]` array consumed by host via `RewardFlow.GrantAndShowSequence` post-dismiss (L3). Fields: `Title` ("Level Complete!"), `LevelLabel` (""), `Stars` (0, clamped `[0,3]` on Bind), `Score` (0), `BestScore` (0), `IsNewBest` (false), `Rewards` (null), `NextLabel` ("Next"), `RetryLabel` ("Retry"), `MainMenuLabel` ("Main Menu"), `ShowNext` (true), `ShowRetry` (true), `ShowMainMenu` (false), `CloseOnBackdrop` (false). Defaults asserted in `DTO_Defaults_Match_Spec_Contract` test (FD3).
  - `LevelCompletePopup.cs` — `UIModule<LevelCompletePopupData>`. Three CTAs (`Next`/`Retry`/`MainMenu`) gated by their `Show*` flags; back-press routes to retry per L7; backdrop-tap routes to retry when `CloseOnBackdrop=true`. `IsDismissing` guard on every CTA path. Star reveal via direct sprite swap from `Theme.StarFilledSprite`/`StarEmptySprite` (Theme slots from pre-flight); cascade tween + score rollup tween deferred to `_tween` polish session per Group B precedent. `IsNewBest=true` plays `UIAudioCue.Success` on Show. No required services — only optional `IUIAudioRouter`. Foot-gun guard: all-CTAs-hidden bind logs error and forces `ShowRetry=true` (mirrors NotEnoughCurrencyPopup pattern). Stars-out-of-range warning + clamp to `[0,3]`. Service binding follows § 4.5 (no required service to validate; LogError pattern unused but cited in spec).
  - `UIAnimLevelCompletePopup.cs` — `: UIAnimPopupBase {}` shell (Group B precedent). Star cascade + CTA stagger animation deferred to `_tween` polish session.
- **EditMode tests +16** under `Tests/Editor/LevelCompletePopupTests.cs` — Bind defaults / DTO contract (FD3) / Next+Retry+MainMenu+Backdrop+Back paths / per-CTA `Show*=false` lockout / spam guard / stars-clamp warnings (positive + negative) / all-CTAs-hidden foot-gun guard / event listener reset on re-Bind / event-arg DTO carries `Rewards` array intact for `RewardFlow.GrantAndShowSequence` integration.
- **Spec update** — `Documentation~/Specs/Catalog/LevelCompletePopup.md`: `Services consumed` section renamed to `Service binding` and cites `CATALOG_GroupC_DELTA.md` § 4.5.

### Added (Phase B — GameOver element 3/5)

- **`Runtime/Catalog/Popups/GameOver/`** — 5 files:
  - `GameOverPopupData.cs` — DTO for level-failure announcement with two-mode Continue (ad / currency / both) decoupled from ad / economy services. Fields: `Title` ("Game Over"), `Subtitle` (""), `Score` (-1, GO8 sentinel hides score block), `ContinueMode` (`Ad`), `ContinueCurrency` (`Gems`), `ContinueAmount` (5), `ContinuesUsedThisSession` (0), `MaxContinuesPerSession` (1), `ContinueAdLabel` ("Continue"), `ContinueCurrencyLabel` ("Continue ({amount})"), `RestartLabel` ("Restart"), `MainMenuLabel` ("Main Menu"), `ShowRestart` (true), `ShowMainMenu` (true), `BackPressBehavior` (`Restart`), `CloseOnBackdrop` (false). Defaults asserted in `DTO_Defaults_Match_Spec_Contract` test (FD3).
  - `ContinueMode.cs` — enum `None`/`Ad`/`Currency`/`AdOrCurrency` (GO1). Drives Continue CTA visibility; `AdOrCurrency` shows both side-by-side (player picks).
  - `BackPressBehavior.cs` — enum `Restart`/`MainMenu`/`Ignore` (GO7). Drives back-button routing.
  - `GameOverPopup.cs` — `UIModule<GameOverPopupData>`. Five CTAs (`ContinueAd` / `ContinueCurrency` / `Restart` / `MainMenu` / `Backdrop`) with strict service decoupling per GO4 — popup never calls `IEconomyService.Spend` nor `IAdsService.ShowRewardedAd`; host wires via emitted events. **Affordability gate (GO3) is a TWO-LAYER design**: (a) `IEconomyService=null` → currency button `interactable=false` (click is a no-op, matches spec edge case "Click emits no event"); (b) service present + can't afford → button stays interactable AND visually grayed via `image.color.a=0.5f`, click re-queries `CanAfford` and emits `OnContinueAffordCheckFailed(currency, amount)` WITHOUT dismissing — host wires to `ShopPopup` / `NotEnoughCurrencyPopup`. Click handler always re-queries `CanAfford` even when the Bind snapshot was true, covering external economy mutation between Bind and click. Ad readiness gated via `Button.interactable = !IsRewardedAdReady` set at Bind only (no re-check on click — host owns ad-failure recovery per FQ4). **Continue session limit** (`ContinuesUsedThisSession >= MaxContinuesPerSession`) hides BOTH Continue CTAs regardless of `ContinueMode` (GO2). **Foot-gun guard**: when Continue is hidden by limit AND `ShowRestart=false` AND `ShowMainMenu=false`, `Bind` logs error and forces `ShowMainMenu=true` (vs LevelComplete's `ShowRetry=true` — game-over biases toward exit, GO7 default). Score block hides silently when `Score < 0` (GO8). Header tint applies `Theme.FailureColor` (Group C pre-flight slot). Currency icon resolved from `Theme.IconCoin`/`IconGem`/`IconEnergy` by `ContinueCurrency`. `ContinueCurrencyLabel` placeholder `{amount}` replaced with `ContinueAmount.ToString()`. Service binding follows § 4.5 (Ads, Economy, Audio all OPTIONAL for GameOver — degrade visible+disabled, no abort). On Show plays `UIAudioCue.PopupOpen` then `UIAudioCue.Error` — no `Failure` cue exists in the enum, `Error` is the closest semantic match for the failure-feedback cue called for in spec; a dedicated `Failure` cue would expand cross-cutting audio surface beyond element-3 scope cap.
  - `UIAnimGameOverPopup.cs` — `: UIAnimPopupBase {}` shell (Group B precedent). CTA cascade stagger + Cinematic-preset Failure-color particle accents deferred to `_tween` polish session.
- **EditMode tests +18** under `Tests/Editor/GameOverPopupTests.cs` — Bind defaults / DTO contract (FD3) / ContinueAd happy path + spam guard / ContinueCurrency afford → success event + dismiss / ContinueCurrency NOT-afford → `OnContinueAffordCheckFailed` without dismiss (GO3 contract assertion) / ContinueCurrency with null `IEconomyService` → no-op (Bind sets interactable=false, handler also early-returns at `economy == null`) / Restart + MainMenu CTAs / `BackPressBehavior` Restart + MainMenu + Ignore branches (GO7) / session limit hides Continue (direct click invocations no-op) / `ContinueMode=None` hides Continue / foot-gun guard forces `ShowMainMenu=true` with error log assertion / Bind resets event listeners / backdrop click routes to `BackPressBehavior` when `CloseOnBackdrop=true`, ignored when false. Test target was ~16 from spec; delivered 18 covering the affordability matrix + back-press matrix + DTO defaults + backdrop edge cases. Visual-state setup scenarios (QA 1-5, 7) deferred to in-Editor demo verification — not testable in EditMode without prefab Refs assignments.
- **Spec citation** — `Documentation~/Specs/Catalog/GameOverPopup.md`: § 4.5 citation already applied in 2026-05-03 batch sweep; this delivery did not touch the spec.

### Added (Phase B — HUD-Energy element 4/5)

- **`Runtime/Catalog/HUD/HUDEnergy.cs`** extended from sealing-only stub to full element delivery. Adds `[Serializable] private struct EnergyRefs` foldout (`RegenCountdownLabel: TMP_Text`, `MaxCapLabel: TMP_Text`, `EnergyBarFill: Image` type Filled), serialized format strings (`_capFormatString = "/{0}"`, `_regenFormatString = "+1 in {0}"`, `_regenReadyText = "+1 ready"`), and at-max flash configuration (`_atMaxFlashEnabled` + `_atMaxFlashColor` defaulting to ≈SuccessColor + `_atMaxFlashDuration = 0.30f`). Subscribe override registers an auxiliary `HandleEconomyChangedForRegen` filtered to Energy that triggers `PollAndApplyRegenState()` on every economy event (spec E3 — "polled on every OnChanged event"); base.Subscribe still handles the value/punch via `HandleEconomyChanged`. 1Hz polling driven by a local `Update() → OnUpdate() → TickRegenPoll()` chain (workaround for `UIModuleBase.OnUpdate()` infra dispatch gap, anchored with literal `// OnUpdate-workaround-M3-sweep` per `/_checker` mejora F — M3 entry-criteria greps the anchor and sweeps atomically when infra dispatch lands). MaxCapLabel renders the `/{Max}` suffix only (CountLabel inherited from HUDCurrency carries Current); when `IsFull` or `Max <= 0` the suffix label is hidden via `SetActive(false)`. RegenCountdownLabel hidden when `IsFull`; shows `_regenReadyText` when `NextRegenUtc <= ITimeService.GetServerTimeUtc()`; otherwise `string.Format(_regenFormatString, "HH:MM:SS")` (manual TimeSpan format, DailyLogin precedent). EnergyBarFill `fillAmount = Mathf.Clamp01((float)Current / Max)`; hidden when `Max <= 0`. At-max flash (E7) fires when Current crosses up to Max via DOTween `Sequence().Append(label.DOColor(_atMaxFlashColor, half).SetEase(Ease.OutQuad)).Append(label.DOColor(_capLabelOriginalColor, half).SetEase(Ease.InQuad)).SetLink(gameObject)`, original color captured once on first OnEnable. Service binding follows § 4.5: `IProgressionService=null` → cap+regen+bar `SetActive(false)` silently (no LogError); `ITimeService=null` → regen label shows `_regenReadyText` (no countdown computation); `IEconomyService=null` → base `HUDCurrency.Refresh` logs actionable error and label shows `"--"`. Empty pulse (E8) and overcap tint deferred to M3 polish — both are nice-to-have feedback that don't block buyer demo, and overcap tint requires Theme access not currently wired into `HUDCurrency`.
- **EditMode tests +10** under `Tests/Editor/HUDEnergyTests.cs` — `Refresh_With_Service_Reads_Initial_Energy_State`, `Refresh_With_Null_ProgressionService_Hides_Energy_UI_Silently` (§ 4.5 silent-degrade contract — assert no LogError + all 3 energy UI elements `activeSelf=false`), `Cap_Label_Shows_Suffix_When_Bounded`, `Cap_Label_Hides_When_IsFull` (E4), `Cap_Label_Hides_When_Max_Is_Zero` (E4), `Regen_Label_Shows_Ready_Text_When_NextRegen_In_Past` (E5), `Regen_Label_Hides_When_IsFull` (E5), `EnergyBarFill_Sets_FillAmount_From_Current_Max` (3/5 → 0.6), `Sealed_To_Energy_Even_If_Inspector_Currency_Set_To_Coins` (E1 LOCKED Option A — ResolveCurrency seal proof), `OnChanged_Energy_Triggers_Regen_Poll_Refresh` (auxiliary handler proof — Max change picked up immediately, not on next 1Hz tick). Internal test API additions: `LastEnergyValueForTests`, `CapLabelTextForTests`, `CapLabelVisibleForTests`, `RegenLabelTextForTests`, `RegenLabelVisibleForTests`, `BarFillAmountForTests`, `BarFillVisibleForTests`, `SetEnergyRefsForTests`, `ForceRegenPollForTests`. Total tests: **189/189 verde sobre fresh compile** (179 baseline + 10 HUDEnergy).
- **Spec update** — `Documentation~/Specs/Catalog/HUD-Energy.md`: § Status flipped Code/Tests from `⏳ pending` to `✅ delivered 2026-05-03`; § E5 placeholder format corrected from `"{hh:mm:ss}"` to single-placeholder `"+1 in {0}"`; § E6 click behavior corrected (no rename — inherits agnostic `_onClickEvent` from FD4-cleaned base); § Files corrected (no separate `EnergyRegenState.cs` — struct lives inside `IProgressionService.cs`).

### Added (Phase B — HUD-Timer element 5/5)

- **`Runtime/Catalog/HUD/TimerMode.cs`** — `public enum TimerMode { CountdownToTarget = 0, CountupSinceTarget = 1, LocalStopwatch = 2 }`. Explicit values for stable serialization across upgrades.
- **`Runtime/Catalog/HUD/HUDTimer.cs`** — `[DisallowMultipleComponent] public sealed class HUDTimer : UIHUDBase`. Single class parameterized by `TimerMode`; mode is immutable post-OnEnable per FQ6 (buyer swaps modes via disable→assign→enable cycle, no `SetMode()` runtime helper). `[Serializable] private struct TimerRefs` foldout (`Label: TMP_Text`, optional `IconImage: Image`, optional `ClickButton: Button`). SerializeFields cover the entire surface: `_mode`, `_tickRateHz` (default 1f, buyer raises to 30f for stopwatch precision), `_formatString` (default `"mm\\:ss"`, validated in OnEnable with try/catch FormatException + LogError once + fallback), `_targetUtcIso` (optional ISO 8601 string parsed in OnEnable via `DateTime.TryParse` with `DateTimeStyles.AssumeUniversal | AdjustToUniversal`), `_expiredLabel` (default `"00:00"`), `_hideOnExpire` (default false), `_warningThresholdSeconds` (float, default 0 = disabled — TimeSpan can't be SerializeField), `_pausesWithTimeScale` (LocalStopwatch only; UTC modes ignore it), `_expiryStyle` (private enum `Success`/`Failure`, picks expiry flash color), `_warningColor`/`_expirySuccessColor`/`_expiryFailureColor` (defaults approximate Theme.WarningColor/SuccessColor/FailureColor — HUDTimer does NOT couple to `UIThemeConfig`, same precedent as HUDEnergy at-max flash decision), `_warningTweenDuration` (0.40s half-cycle), `_expiryFlashDuration` (0.30s in+out), `_onTimerClickEvent: UnityEvent`. Public C# events `OnExpired` and `OnWarningEntered` (cleared via `ResetTransientFlags` on `SetTarget(...)` and `Reset()` per spec § Events). Public methods `SetTarget(DateTime utc)` (resets expiry + warning state, ticks once force=true), `Reset()` (zeroes stopwatch state + clears events + ticks), `SetPaused(bool)` (LocalStopwatch only — early-returns for UTC modes; pause accumulates `_stopwatchAccumulated += CurrentRealTime() - _stopwatchStartRealTime`, unpause sets new start). `OnEnable` bypasses `base.OnEnable` when `Services == null && _mode == LocalStopwatch` (stopwatch doesn't need services — unique to HUDTimer; HUDCurrency/HUDEnergy always require Economy). 1Hz polling via local `Update() → OnUpdate() → tick rate throttle` chain (anchor `// OnUpdate-workaround-M3-sweep` per `/_checker` mejora F — same M3 sweep target as HUDEnergy + DailyLogin). Warning pulse: `_warningTween = label.DOColor(_labelOriginalColor, dur).SetLoops(-1, LoopType.Yoyo).SetEase(Ease.InOutSine).SetLink(gameObject)` — base color set to `_warningColor` on entry, restored to original on exit (re-target moves out of warning zone). Expiry flash: `_expiryTween = DOTween.Sequence().Append(label.DOColor(color, half)).Append(label.DOColor(originalColor, half)).SetLink(gameObject)` — kills warning tween first. Service binding follows § 4.5: `ITimeService = null` in UTC mode → label shows `"--:--"` SILENTLY, **drift fix vs older spec § Edge cases wording** (`"logs error"`); `IUIAudioRouter = null` → audio cues no-op. Stopwatch mode operates with zero services. Audio cues: warning enters `UIAudioCue.Error` (drift fix — `UIAudioCue.Failure` doesn't exist in enum, `Error` is closest semantic match per GameOver precedent); expiry plays `UIAudioCue.Success`. Internal `RealTimeProviderForTests: Func<float>` allows EditMode tests to inject a controllable clock without depending on `Time.unscaledTime` (prevents flaky tests). Production fallback: `_pausesWithTimeScale ? Time.time : Time.unscaledTime`.
- **EditMode tests +10** under `Tests/Editor/HUDTimerTests.cs` — `Countdown_Initial_Refresh_Computes_Remaining_From_Target` (5min target → `"05:00"`), `Countdown_Tick_Decrements_Remaining` (advance 30s → `"04:30"`), `Countdown_Expired_Fires_Event_Once_And_Stops_Ticking` (target in past → fireCount=1 + label=`"00:00"` + IsExpired=true; subsequent ticks within cycle do NOT re-fire), `Countdown_Warning_Threshold_Fires_Event_Once_When_Crossed` (target=now+15s, threshold=10s, advance 6s → fireCount=1; re-tick within zone does NOT re-fire), `Countdown_Hide_On_Expire_Hides_Label` (T5 — `_hideOnExpire=true`), `Countup_Initial_Refresh_Computes_Elapsed` (start=now-30s → `"00:30"`), `Stopwatch_OnEnable_Starts_From_Zero` (clock injected, format `"mm\\:ss\\.ff"` → `"00:00.00"`), `Stopwatch_SetPaused_Freezes_Counter` (clock at 0→5→pause→10→unpause→13 → label progression `00:05`→`00:05`→`00:08`), `Stopwatch_Reset_Sets_To_Zero` (advance to 10s, Reset → `"00:00"`), `Mode_UTC_Without_TimeService_Shows_Dashes_Silently` (§ 4.5 drift fix proof — `_services.SetTime(null)`, label = `"--:--"`, no LogError). Total tests: **199/199 verde sobre fresh compile** (189 baseline + 10 HUDTimer).
- **Spec update** — `Documentation~/Specs/Catalog/HUD-Timer.md`: § Status flipped Code/Tests from `⏳ pending` to `✅ delivered 2026-05-04`; § Edge cases UTC null-time wording aligned to § 4.5 silent (drift fix); § T6 warning tint clarified as `_warningColor` SerializeField (HUDTimer does NOT read Theme directly — consistency with HUDEnergy); § Service binding audio cue corrected from `Failure` to `Error` (drift fix — `UIAudioCue.Failure` does not exist).

### Added (Phase B — Helpers, capability-gate verdict 2026-05-04)

- **`Runtime/Catalog/Popups/Reward/RewardFlow.cs`** — `public static class RewardFlow` with the single shipped helper `GrantAndShowSequence(PopupManager popups, IEconomyService economy, IEnumerable<RewardPopupData> rewards, Action onSequenceComplete = null)`. Chains N `RewardPopup` instances via `OnDismissed → next.Show()` and credits `IEconomyService.Add(currency, amount)` on each non-sentinel claim. Item/Bundle rewards (sentinel `(CurrencyType)(-1)` per `RewardPopup.ItemCurrencySentinel`) surface via `OnClaimed` but are NOT auto-credited — host resolves them. Validation per `CATALOG_GroupC_DELTA.md` § 4.5 helper policy: null `popups` / null `economy` / null `rewards` / empty `rewards` → `Debug.LogError` with mandatory format + early no-op return. `onSequenceComplete` fires only after the LAST reward dismisses successfully — validation errors and queue-saturation early-aborts do NOT invoke it (contract is "all rewards claimed", not "sequence attempted"). Snapshots `IEnumerable` to `RewardPopupData[]` once on entry to insulate against buyer-side post-call mutation. Internal recursion via `ShowNext(index)` + `SubscribeChainHandlers` keeps each function ≤15 lines per UNITY_RULES. No static state cross-call (each invocation independent — distinct from sibling `DailyLoginFlow._shownThisLaunch` idempotent pattern).
- **EditMode tests +7** under `Tests/Editor/RewardFlowTests.cs` — 4 validation paths (null popups, null economy, null rewards, empty collection — each asserts LogError + `onSequenceComplete` does NOT fire) + 3 happy paths (single Coins reward credits + completes / 3 mixed rewards chain crediting Coins+Gems+Coins=150+5 then completes once / 3 mixed including Item + Bundle sentinels filters correctly crediting only the Coins). Test infra uses real `PopupManager` + `RewardPopup` prefab + reflection on private `_popupPrefabs`/`_popupRoot` SerializeFields (Group B PopupManagerTests precedent) + `PreWarmRewardPopupWithNullAnimator` to inject NullAnimator into the cached popup instance before the helper invokes it (UIAnimRewardPopup short-circuits on `_card == null` so this is defensive — both real animator + NullAnimator complete the chain synchronously in EditMode).
- **`Tests/Editor/LevelCompletePopupTests.Event_Data_Carries_Original_Rewards_For_Sequence_Helper`** migrated per `/_checker` mejora H — renamed to `Event_Data_Carries_Original_Rewards_And_RewardFlow_Credits_Economy_Per_Reward`. Original assertion (`Assert.AreSame(rewards, d.Rewards)` for L3 array carry-through contract) preserved; test now ALSO sets up local `PopupManager` + `RewardPopup` prefab + `FakeEconomyService` and invokes `RewardFlow.GrantAndShowSequence(manager, economy, d.Rewards, () => sequenceCompleteCount++)` from inside the `OnNextRequested` handler. Two `dummy.OnBackPressed()` calls drive the chain through both rewards (Coins=100 + Gems=5). Asserts economy credited correctly + `sequenceCompleteCount == 1`. Closes the "test verifies intent but not integration" gap flagged in the post-GameOver triple `/_checker` round.
- **Capability-gate verdict (Group C 2026-05-04)** — sibling helpers `RewardFlow.GrantAndShow` (single) and `ShopFlow.OpenWithPurchaseChain` deferred to Group D. Counted callsites: `GrantAndShow` single = 1 (DailyLogin OnWatchAdRequested 2× amount; host can use `GrantAndShowSequence` with single-element collection in the meantime), `ShopFlow` chain = 1 (Group B demo trigger; GameOver does NOT use this chain — its continues are direct Ad/Currency, not Shop-routed). Spec preserved in `RewardPopup.md` and `ShopPopup.md` § Convenience helpers — contract-stable, no event surface churn at Group D build time.
- **Spec updates** — `Documentation~/Specs/Catalog/RewardPopup.md` § Convenience helpers and `Documentation~/Specs/Catalog/ShopPopup.md` § Convenience helpers: status flipped from "deferred — Group C decision" to capability-gate verdict (Sequence shipped, single + Shop deferred Group D).

### Added (B.4.0 — sample stub infrastructure prep, NOT tag-ready)

> **Intermediate progress entry.** B.4.0 ships ONLY the in-memory progression stub + sample asmdef. No prefabs, no builder, no demo scene. **`v0.6.0-alpha` is NOT tag-ready** until B.4.1 (builder skeleton) → B.4.2-B.4.5 (popup/HUD prefab generators) → B.4.6 (demo scene + chain demo MonoBehaviour) all land. `package.json` `samples[]` entry deliberately NOT added in B.4.0 — adding it now would expose a half-finished sample to buyers via Package Manager UI (`/_checker` 2026-05-04 mejora F1: shipping the entry premature breaks the buyer-facing completeness expectation set by Group A and Group B). The samples[] entry lands at B.4.6 closure together with the buyer-facing demo. Done criteria checkbox for the helper line above is the LAST flip before tag — all subsequent B.4 sub-steps must land before flipping it.

- **`Samples~/Catalog_GroupC_Progression/Stubs/InMemoryProgressionService.cs`** — `[DisallowMultipleComponent] [DefaultExecutionOrder(-100)]` (Awake-before-OnEnable per L4 lesson) `MonoBehaviour, IProgressionService`. Inspector-editable `_dailyLoginState` and `_energyRegenState` SerializeFields with documented defaults: `_dailyLoginState.LastClaimUtc = default` (DateTime.MinValue per `/_checker` mejora B — DailyLogin auto-trigger fires on buyer's first Play because today != MinValue.Date), `_dailyLoginState.CurrentDay = 1` / `MaxStreakGapDays = 1`, `_energyRegenState.Current = Max = 5` / `IsFull = true` / `NextRegenUtc = default` ("fresh first run"). `_regenSeconds = 60f` Inspector-tunable. `_levels` seeded with 5 LevelData entries (Id 0 Available, Ids 1-4 Locked) on first Awake. Economy injection via `[SerializeField] MonoBehaviour _economyServiceRef` cast to `IEconomyService` in Awake — no cross-sample `RequireComponent` dependency (buyer can wire any `IEconomyService` impl, not only Group B's `InMemoryEconomyService`). Public `SetEconomyForTests(IEconomyService)` for test/builder direct injection. `Update()` ticks regen via `DateTime.UtcNow` (not via `ITimeService` — keeps stub self-contained, mirrors mobile-game convention; buyer can fork stub for server time): when not full and `UtcNow >= NextRegenUtc` → `Current++` (capped at Max), `IsFull` recomputed, `NextRegenUtc = UtcNow + _regenSeconds` (or `default` if full), then `_economy?.Add(CurrencyType.Energy, 1)` so HUD-Energy's `IEconomyService.OnChanged(Energy, _)` subscription stays in sync without extra wiring. `GetDailyLoginState()` returns a struct copy with `AlreadyClaimedToday` computed at call time (`LastClaimUtc.Date == DateTime.UtcNow.Date`) — the SerializeField stores the raw timestamp, the computed bool is never written back. Public state-mutation API for buyer host code: `MarkDailyLoginClaimed()` (sets `LastClaimUtc=UtcNow` + increments `CurrentDay` + clears `DoubledToday`), `MarkDailyLoginDoubled()`, `SetEnergyValue(int)` (clamped 0..Max with NextRegenUtc recomputed). Five `[ContextMenu]` debug triggers per `CATALOG_GroupC_DELTA.md` § 2 acceptance: "Reset Daily To Day 1", "Set Daily To Day 7 Ready", "Mark Today Claimed", "Set Energy To Zero", "Refill Energy". `IProgressionService.CompleteLevel` clamps stars to `[0,3]`; `UnlockLevel` returns false if already unlocked. All public functions ≤15 lines per UNITY_RULES.
- **`Samples~/Catalog_GroupC_Progression/KitforgeLabs.MobileUIKit.Samples.CatalogGroupC.asmdef`** — sample-side asmdef mirroring Group B precedent (`KitforgeLabs.MobileUIKit.Samples.CatalogGroupB`). References: `KitforgeLabs.MobileUIKit`, `KitforgeLabs.MobileUIKit.Catalog`, `Unity.TextMeshPro`. Asmdef name LOCKED per `/_checker` mejora F3 — B.4.1+ builder will resolve the stub via `System.Type.GetType("...InMemoryProgressionService, KitforgeLabs.MobileUIKit.Samples.CatalogGroupC")` reflection (Group B precedent). Naming consistency keeps the future builder code symmetrical to `CatalogGroupBBuilder.TryAttachStubServices`.

### Added (B.4.1 — builder skeleton, NOT tag-ready)

> **Intermediate progress entry.** B.4.1 ships the `CatalogGroupCBuilder` static class scaffold with the `Tools/Kitforge/UI Kit/Build Group C Sample` MenuItem wired and `EnsureFolders` creating the `Assets/Catalog_GroupC_Demo/Prefabs/` output tree. **No prefabs are generated yet** — `BuildAll` only logs a "skeleton ready" diagnostic and exits. **`v0.6.0-alpha` is NOT tag-ready** until B.4.2-B.4.5 (per-element `BuildXxx` generators) + B.4.6 (demo scene + chain demo + `samples[]` entry) all land. `package.json` `samples[]` entry deliberately NOT added in this sub-step — same rationale as B.4.0 (premature buyer-facing exposure of a half-finished sample).

- **`Editor/Generators/CatalogGroupCBuilder.cs`** (~140 lines) — `static class CatalogGroupCBuilder` mirroring `CatalogGroupBBuilder` precedent. Includes:
  - 3 const strings: `OutputRoot = "Assets/Catalog_GroupC_Demo"`, `PrefabsFolder = OutputRoot + "/Prefabs"`, and `ProgressionServiceTypeName` (LOCKED reflection target string for B.4.6: `"KitforgeLabs.MobileUIKit.Samples.CatalogGroupC.InMemoryProgressionService, KitforgeLabs.MobileUIKit.Samples.CatalogGroupC"` — typo-proofs the future `Type.GetType` call against the asmdef name LOCKED in B.4.0).
  - 5 color tokens used by the helpers (`BackdropColor`, `CardColor`, `ButtonPrimaryColor`, `ButtonSecondaryColor`, `TextDarkColor`) — additional palette tokens (Cell / HeaderTint / Success / HUDBackground / TextLight / Failure) deferred to the specific `BuildXxx` sub-step that first needs them, per scope-cap discipline (avoids `CS0414` unused-readonly warnings + keeps the file growing in lockstep with capability delivery).
  - 8 layout helpers + 3 transitive deps copied verbatim from `CatalogGroupBBuilder` (decision: copy-paste — see "Helper-sharing strategy" below): `CreateRoot`, `CreateBackdrop`, `CreateCard`, `CreateChild`, `AddImage`, `CreateText`, `CreateButton`, `CreatePrimaryButton`, `CreateSecondaryButton`, `StretchInside`, `AnchorTopOfCard`. Prefab-saving + animator-wiring helpers (`SaveAsPrefab`, `WireAnimatorCard`, `ForceButtonHeight`) deferred to the specific sub-step that first needs them.
  - `[MenuItem("Tools/Kitforge/UI Kit/Build Group C Sample")] public static void BuildAll()` body: `EnsureFolders(); Debug.Log(skeleton-ready diagnostic that cites the locked reflection target).` No prefab generation, no scene generation, no service stub attachment. Output the user can verify: a console log line + the empty `Assets/Catalog_GroupC_Demo/Prefabs/` folder appearing in the Project view.
- **Helper-sharing strategy decision** (drift checker `/_checker drift` 2026-05-04, after evaluating 3 options): **copy-paste during M1-M3**, extract `CatalogBuilderBase` at M4 hardening sweep. Rationale: composability criterion (philosophy #5) requires 2+ consumers PROVEN, not anticipated. Touching `CatalogGroupBBuilder.cs` (taggeado en `v0.5.0-alpha`) for an extraction now opens BREAKING blast radius post-tag and falls outside the B.4.1 cap. M4 already has "builder destination overwrite/skip prompt" hotfix scheduled — extraction lands in the same sweep when 4 builders (A/B/C/D) exist. Documented in roadmap "Diferidos / próximos hotfixes".

### Added (B.4.2 — DailyLoginPopup prefab generator, NOT tag-ready)

> **Intermediate progress entry.** B.4.2 ships the `BuildDailyLoginPopup` private generator + `DailyLoginPopup.prefab` materialized at `Assets/Catalog_GroupC_Demo/Prefabs/DailyLoginPopup.prefab`. **`v0.6.0-alpha` is NOT tag-ready** until B.4.3 (LevelComplete) → B.4.4 (GameOver) → B.4.5 (HUD-Energy + HUD-Timer) → B.4.6 (DemoScene + chain demo + `samples[]` entry) all land.

- **`Editor/Generators/CatalogGroupCBuilder.cs`** — `BuildDailyLoginPopup()` private static added (~70 LOC mirroring Group B `BuildRewardPopup` pattern). Generates a 900×1200 card with: title (TMP, top-anchored), 4-column `GridLayoutGroup` `DayCellContainer` (cell template authoring is buyer/runtime concern per spec — builder ships the empty container only), `CountdownLabel` (TMP, hidden by default — popup `ApplyVisibility` toggles based on `AlreadyClaimedToday`; color = local `WarningColor` orange, buyer can attach `ThemedText` for theme override), primary `ClaimButton` (`SuccessTintColor` green tint + white "Claim" label), secondary `WatchToDoubleButton` (gray + dark "Watch ad to double" label, hidden by default — popup activates when `ShouldShowWatchToDouble()` evaluates `AllowDouble` + `IsRewardedAdReady` + `!DoubledToday`). All 8 `_refs` wired via `SerializedObject.FindProperty("_refs.XxxLabel/Button")` matching `DailyLoginPopup.Refs` private struct.
- **Animator wiring**: `root.AddComponent<UIAnimDailyLoginPopup>()` first (mirroring Group B AddComponent ordering), then `root.AddComponent<DailyLoginPopup>()` (auto-adds animator via `[RequireComponent]` if absent — explicit-first prevents the redundant fallback path), then `WireAnimatorCard(animator, card)` sets `_card` SerializeField. `UIAnimDailyLoginPopup` is the v0.5.0 Group B precedent stub `: UIAnimPopupBase {}`.
- **`BuildAll` updated**: now loads `UIThemeConfig_Default.asset` + opens "Bootstrap Defaults missing" dialog if absent (mirroring Group B), calls `BuildDailyLoginPopup()` + `AssetDatabase.SaveAssets()` + `Refresh()`, logs "Group C — 1/5 prefabs generated. Pending: B.4.3 / B.4.4 / B.4.5 / B.4.6".
- **Constants/colors added incrementally per cap-strict discipline**: `DailyLoginPath` const + `DefaultThemePath` const + `SuccessTintColor` + `WarningColor` + `TextLightColor` static readonly. Anticipatory tokens for B.4.3-B.4.5 (LevelComplete star colors, GameOver header tint, HUD background) still deferred — added when each `BuildXxx` first uses them. Helpers `SaveAsPrefab<T>` + `WireAnimatorCard` added (composability criterion now satisfied — 4 future consumers in B.4.3-B.4.5 will reuse).
- **Usings added**: `KitforgeLabs.MobileUIKit.Catalog.DailyLogin` + `KitforgeLabs.MobileUIKit.Theme`.

### Added (B.4.3 — LevelCompletePopup prefab generator, NOT tag-ready)

> **Intermediate progress entry.** B.4.3 ships the `BuildLevelCompletePopup` private generator + `LevelCompletePopup.prefab` materialized at `Assets/Catalog_GroupC_Demo/Prefabs/LevelCompletePopup.prefab`. **`v0.6.0-alpha` is NOT tag-ready** until B.4.4 (GameOver) → B.4.5 (HUD-Energy + HUD-Timer) → B.4.6 (DemoScene + chain demo + `samples[]` entry) all land.

- **`Editor/Generators/CatalogGroupCBuilder.cs`** — `BuildLevelCompletePopup()` private static added (~120 LOC). Generates a 900×1300 card with: title (TMP "Level Complete!" 48pt, top-anchored), optional `LevelLabel` subtitle (28pt italic, hidden when empty per popup `ApplyTexts`), `StarsRow` (`HorizontalLayoutGroup` with 3 child `Image` slots 160×160 — initial sprite = `Theme.StarEmptySprite` if Theme present; runtime overrides via `ApplyStars`), `ScoreLabel` (64pt bold center), `BestScoreLabel` (26pt italic gray below score), `NewBestBanner` (full-width strip with `SuccessTintColor` background + white "NEW BEST!" label, hidden by default — popup `ApplyVisibility` toggles via `IsNewBest`), `Buttons` container with `VerticalLayoutGroup` holding 3 stacked CTAs: `NextButton` (primary blue, "Next" label, 96h), `RetryButton` (secondary gray, "Retry" label, 92h), `MainMenuButton` (secondary gray, "Main Menu" label, 80h, hidden by default per `ShowMainMenu=false`). All 13 `_refs` wired via `SerializedObject.FindProperty`, including the `StarImages` array via `arraySize=3` + per-index `objectReferenceValue` assignment.
- **Animator wiring**: `root.AddComponent<UIAnimLevelCompletePopup>()` first, then `root.AddComponent<LevelCompletePopup>()`, then `WireAnimatorCard(animator, card)`. `UIAnimLevelCompletePopup` is the v0.5.0 Group B precedent stub `: UIAnimPopupBase {}` (Group C precedent for stub-only animator scripts).
- **`BuildAll` updated**: `BuildLevelCompletePopup()` call added after `BuildDailyLoginPopup()`. Log message updated to "2/5 prefabs generated. Pending: B.4.4 / B.4.5 / B.4.6".
- **Constants added incrementally per cap-strict discipline**: `LevelCompletePath` const. No new color tokens needed (existing `SuccessTintColor` covers banner; `ButtonPrimaryColor`/`ButtonSecondaryColor`/`TextDarkColor`/`TextLightColor` cover CTAs; star colors come from `Theme.StarFilledSprite`/`StarEmptySprite` at runtime). Helper `ForceButtonHeight` added (Group B precedent — 3 future consumers in B.4.4-B.4.5 will reuse for stacked CTA layouts).
- **Usings added**: `KitforgeLabs.MobileUIKit.Catalog.LevelComplete`.

### Added (B.4.4 — GameOverPopup prefab generator, NOT tag-ready)

> **Intermediate progress entry.** B.4.4 ships the `BuildGameOverPopup` private generator + `GameOverPopup.prefab` materialized at `Assets/Catalog_GroupC_Demo/Prefabs/GameOverPopup.prefab`. **`v0.6.0-alpha` is NOT tag-ready** until B.4.5 (HUD-Energy + HUD-Timer) → B.4.6 (DemoScene + chain demo + `samples[]` entry) all land.

- **`Editor/Generators/CatalogGroupCBuilder.cs`** — `BuildGameOverPopup()` private static added (~130 LOC). Generates a 900×1300 card with: `HeaderTint` strip (full-width 14h band at top with `FailureColor` placeholder — popup `ApplyTint` overwrites runtime via `Theme.FailureColor` at Bind time), title (TMP "Game Over" 48pt bold), optional `Subtitle` (28pt italic, hidden when DTO Subtitle empty per `ApplySubtitle`), `ScoreBlock` GameObject wrapping `ScoreLabel` (64pt bold, popup hides block when `Score < 0` per GO8), `Buttons` container with `VerticalLayoutGroup` holding 4 stacked CTAs: `ContinueAdButton` (primary blue, "Continue" white 30pt, 96h), `ContinueCurrencyButton` (secondary gray, internal absolute-positioned `CurrencyIcon` Image + "Continue (5)" label — popup `ApplyIcon` resolves `Theme.IconCoin/Gem/Energy` runtime by `_data.ContinueCurrency`; popup `ApplyContinueCurrencyGating` sets alpha 0.5 when can't afford), `RestartButton` (secondary gray, "Restart" 28pt, 92h), `MainMenuButton` (secondary gray, "Main Menu" 26pt, 80h). All 15 `_refs` wired via `SerializedObject.FindProperty` including `HeaderTint` and `ScoreBlock`.
- **Animator wiring**: `root.AddComponent<UIAnimGameOverPopup>()` first, then `root.AddComponent<GameOverPopup>()`, then `WireAnimatorCard`. `UIAnimGameOverPopup` is the v0.5.0 Group B precedent stub `: UIAnimPopupBase {}`.
- **`BuildAll` updated**: `BuildGameOverPopup()` call added after `BuildLevelCompletePopup()`. Log message updated to "3/5 prefabs generated. Pending: B.4.5 / B.4.6".
- **Constants added incrementally per cap-strict discipline**: `GameOverPath` const + `FailureColor` static readonly (`#E5392B` mirroring `Theme.FailureColor` default for placeholder fidelity in Prefab Mode; popup runtime overwrites). No new helpers needed (all existing helpers reused).
- **Usings added**: `KitforgeLabs.MobileUIKit.Catalog.GameOver`.
- **Theme runtime hook validation**: `BuildGameOverPopup` deliberately leaves `HeaderTint.color`, `ContinueCurrencyIcon.sprite`, and `ContinueCurrencyLabel.text` at placeholder values — popup `ApplyTint`/`ApplyIcon`/`ApplyTexts` overwrite at every `Bind` call from current `Theme` and DTO. This is the **canonical pattern for live-Theme reskin** (contrast with `BuildLevelCompletePopup.NewBestBanner` which hardcodes `SuccessTintColor` build-time — captured as M3 polish deuda in `kitforge_mobile_ui_kit_roadmap.md` § Diferidos).

### Added (B.4.5 — HUDEnergy + HUDTimer prefab generators, NOT tag-ready)

> **Intermediate progress entry.** B.4.5 ships `BuildHUDEnergy()` + `BuildHUDTimer()` — the final 2 of the 5 Group C catalog prefabs. Materializes `Assets/Catalog_GroupC_Demo/Prefabs/HUDEnergy.prefab` and `Assets/Catalog_GroupC_Demo/Prefabs/HUDTimer.prefab`. **All 5 catalog prefabs now generated by the builder** (DailyLogin / LevelComplete / GameOver popups + HUDEnergy + HUDTimer HUDs). **`v0.6.0-alpha` is NOT tag-ready** until B.4.6 (DemoScene + chain demo `CatalogGroupCDemo` MonoBehaviour wiring `UIServices` + 5 prefabs + `[ContextMenu]` triggers per mejora I + `package.json` `samples[]` entry) lands.

- **`Editor/Generators/CatalogGroupCBuilder.cs`** — Two new private static functions added (~140 LOC combined):
  - `BuildHUDEnergy()` (~70 LOC) — generates a 280×100 horizontal HUD shell: `HUDBackgroundColor` semi-transparent panel as Button targetGraphic, left-anchored Icon Image (64×64, sprite = `Theme.IconEnergy` if available), `CountLabel` ("3" 32pt white bold) + `MaxCapLabel` ("/5" 24pt gray bold) inline at top, `RegenCountdownLabel` ("+1 in 04:30" 16pt `WarningColor` orange) at bottom-left, `EnergyBarFill` Image (Filled/Horizontal type, `SuccessTintColor` green, fillAmount=0.6f preview) as thin 8h strip at bottom edge. Wires 7 SerializedObject refs: `_currency=Energy` (informational — `HUDEnergy.ResolveCurrency()` seals via virtual override regardless), `_refs.IconImage`, `_refs.CountLabel`, `_refs.ClickButton`, `_energyRefs.RegenCountdownLabel`, `_energyRefs.MaxCapLabel`, `_energyRefs.EnergyBarFill`. **No `_services` wiring at build time** — buyer or `BuildDemoScene` (B.4.6) attaches `UIServices` reference at runtime via `WireHUDServices` precedent.
  - `BuildHUDTimer()` (~50 LOC) — generates a 220×64 horizontal HUD shell: `HUDBackgroundColor` panel as Button targetGraphic, left-anchored Icon Image (40×40, sprite = `Theme.IconClock` if available), `Label` ("00:00" 36pt white bold) stretched right of icon. Wires 3 SerializedObject refs: `_refs.Label`, `_refs.IconImage`, `_refs.ClickButton`. Mode/format/expiry SerializeFields keep their script-level defaults (`_mode=CountdownToTarget`, `_formatString="mm\\:ss"`, `_targetUtcIso=""`) — buyer configures per-instance via Inspector or `SetTarget(DateTime)` runtime API.
- **`BuildAll` updated**: added `BuildHUDEnergy()` + `BuildHUDTimer()` calls after `BuildGameOverPopup()`. Log message updated to **"5/5 prefabs generated. Pending: B.4.6 (DemoScene + chain demo + samples[] entry)"** — final builder closure milestone.
- **Constants added incrementally per cap-strict discipline**: `HUDEnergyPath` const + `HUDTimerPath` const + `HUDBackgroundColor` static readonly (`#00000073` semi-transparent black, mirrors Group B precedent for HUD shells) + `HUDCapLabelColor` static readonly (`#D9D9E5` light gray for "/5" suffix readability against background). No new helpers needed (HUDs are simpler than popups — no card, no backdrop, no animator, just RectTransform + Image bg + Button + child labels/icon).
- **Usings added**: `KitforgeLabs.MobileUIKit.Catalog.HUD` (for `HUDEnergy`/`HUDTimer` types) + `KitforgeLabs.MobileUIKit.Services` (for `CurrencyType.Energy` enum reference in `_currency` SerializedObject assignment).
- **HUDs do NOT have `IUIAnimator` components** (per spec — HUDs are persistent UI without Show/Hide lifecycle). Builder consequently skips `WireAnimatorCard` for these two prefabs. Inline tweens for at-max flash, empty pulse, warning pulse, expiry flash are stored in HUD-side `Tween` fields (`HUDEnergy._capFlashTween`, `HUDTimer._warningTween`/`_expiryTween`) with `SetLink(gameObject)` per UNITY_RULES.

### Added (B.4.6 — Demo scene + chain demo + samples[] entry → tag-ready)

> **Tag-ready closure.** B.4.6 ships the buyer-facing closure of Group C: `BuildDemoScene` materializes `Catalog_GroupC_Demo.unity` with Canvas + EventSystem + UIServices container + 5 wired in-memory stubs (Economy / Shop / Ads from Group B asmdef + Time / Progression from Group C asmdef via reflection) + 2 HUD instances + Demo MonoBehaviour wired to all prefabs. `CatalogGroupCDemo.cs` host script ships with 6 chain `[ContextMenu]` scenarios per mejora I (`/_checker as user/pm/dev` 2026-05-03 GameOver close) + 9 individual element triggers + multi-timer reset per HUD-Timer QA scenario 11. `package.json` `samples[]` entry added making the Group C sample discoverable in Package Manager UI. CHANGELOG done-criteria checkboxes flipped — **only `v0.6.0-alpha` BREAKING tag remains** (B.5: bump version + README install URL + fresh-compile final verification + `git tag`).

- **`Samples~/Catalog_GroupC_Progression/Stubs/InMemoryTimeService.cs`** (NEW, ~30 LOC) — `[DisallowMultipleComponent] [DefaultExecutionOrder(-100)]` `MonoBehaviour, ITimeService`. Returns `DateTime.UtcNow + _offsetSeconds`. `_offsetSeconds` is Inspector-editable for QA testing (skip 1 hour / 1 day / reset ContextMenu helpers). Wired into UIServices' `_timeServiceRef` field by `TryAttachStubServices`. Required dependency for `DailyLoginPopup` (countdown + day-boundary computation) and `HUDTimer` (UTC modes). Discovered as a gap by pre-flight finding ("InMemoryTimeService NOT exists in Samples~/") — included in B.4.6 cap per Option A approval.
- **`Samples~/Catalog_GroupC_Progression/CatalogGroupCDemo.cs`** (NEW, ~280 LOC) — host MonoBehaviour with 9 `[SerializeField]` refs (`_popupParent` RectTransform, `_theme` UIThemeConfig, `_services` UIServices, 3 popup prefabs + RewardPopup prefab from Group B + 2 HUD scene instances). Mirrors `CatalogGroupBDemo` precedent: instantiates popups via `SpawnPopup<T>` helper (Instantiate + GetComponent + Initialize(theme, services)), subscribes to popup events, dismisses by destroying the GameObject on `OnDismissed`. Includes:
  - **`Start()` initializer** (post-checker C1 fix 2026-05-04): seeds `_hudTimer.SetTarget(DateTime.UtcNow.AddMinutes(5))` so first-Play shows a live 5-minute countdown instead of the default `_targetUtc = MinValue` that would expire immediately and display "00:00" out-of-the-box. Buyer's first impression is a working ticking timer.
  - **9 individual ContextMenu triggers**: DailyLogin (day 1 fresh / day 5 watch-to-double / already claimed); LevelComplete (3 stars new best / last level Next-hidden); GameOver (Ad / Currency / AdOrCurrency).
  - **6 HUD ContextMenu triggers**: Add 1 energy / Spend 1 energy / Empty energy (set to 0); Set timer 5min / Set timer 10s warning zone / Set timer 3min countdown (with multi-timer setup hint).
  - **5 chain ContextMenu scenarios per mejora I + honest categorization**: LevelComplete → Reward sequence (3 chained rewards via `OnDismissed`-based recursion in `SpawnNextRewardInSequence`); GameOver Continue Ad (subscribes `OnContinueWithAdRequested` → `_services.Ads.ShowRewardedAd`); GameOver Continue Currency (subscribes → `_services.Economy.Spend` + log); DailyLogin auto-trigger (manual SpawnDailyLogin since the MonoBehaviour-less `DailyLoginFlow.ShowIfDue` requires `PopupManager` which the demo deliberately doesn't use, mirroring Group B's `Instantiate`-based pattern); Energy regen tick (manual +1 add, HUD reflects via `OnChanged` subscription). **Sixth mejora I scenario (multi-timer) reclassified** post-checker C2 fix as a HUD ContextMenu helper rather than a Chain scenario — it sets a single timer plus logs the manual-instantiation guidance for QA scenario 11; "chain" is reserved for true popup → event → another-popup flows. The 6 mejora I scenarios are still all addressable; only the categorization changed for honest naming.
  - **Watch-to-double flow** (`HandleAdThenDoubledRewards`): subscribes to `OnWatchAdRequested(day, rewards)` → calls `_services.Ads.ShowRewardedAd` → on success, doubles first reward amount per spec D5 → spawns single doubled reward via `SpawnRewardSequence`.
  - **`HandleRewardClaimed`** filters out `RewardPopup.ItemCurrencySentinel` (Item/Bundle kinds) before crediting economy, matching Group B precedent.
- **`Editor/Generators/CatalogGroupCBuilder.cs`** — Three new private static functions added (~210 LOC combined):
  - `BuildDemoScene()` — creates new empty scene, loads 5 Group C prefabs + Group B `RewardPopup` prefab + Theme, builds Canvas (ScreenSpaceOverlay, ScaleWithScreenSize 1080×1920, matchWidthOrHeight 0.5), `Popups` child (full stretch) + `HUD` child (top-right anchored 300×240 with `VerticalLayoutGroup`), `EventSystem` GameObject, `UIServices` GameObject, calls `TryAttachStubServices(servicesGO, services)`, instantiates HUDEnergy + HUDTimer prefabs into HUD parent and calls `WireHUDServices` on each, instantiates Demo GameObject with `CatalogGroupCDemo` component (resolved via reflection against `DemoMonoBehaviourTypeName` — `LogWarning` if Group C sample asmdef not compiled), wires all 9 SerializedObject fields on Demo, saves scene to `Catalog_GroupC_Demo/Catalog_GroupC_Demo.unity`. Logs `LogError` for missing critical prefabs (theme + 5 Group C prefabs), `LogWarning` for missing optional `RewardPopup` prefab (chain partial without it but other scenarios still work).
  - `TryAttachStubServices(host, services)` — reflects against TWO asmdefs (`GroupBStubAsmdef` for Economy/Shop/Ads + `GroupCStubAsmdef` for Time/Progression). Adds 5 stub MonoBehaviours to the UIServices host GameObject, wires `InMemoryProgressionService._economyServiceRef` to the Economy stub instance (so regen tick can call `_economy.Add(Energy, 1)`), then wires all 5 service refs on UIServices (`_economyServiceRef`, `_shopDataProviderRef`, `_adsServiceRef`, `_timeServiceRef`, `_progressionServiceRef`). Logs separate warnings per missing asmdef so buyer knows whether Group B or Group C sample needs importing.
  - `WireHUDServices(hud, services)` — sets `_services` SerializedObject field on HUD instance (mirrors Group B precedent verbatim).
- **`BuildAll` updated**: now calls `BuildDemoScene()` after `SaveAssets()` + `Refresh()` (mirroring Group B precedent — scene generation requires prefabs already saved on disk for `LoadAssetAtPath`). Final user-facing dialog replaces the prior diagnostic Debug.Log: "Group C built at Assets/Catalog_GroupC_Demo. 5 prefabs + 1 scene generated. Open the scene, press Play, right-click the Demo GameObject and pick a Context Menu scenario (try 'Chain — LevelComplete → Reward sequence (3 rewards)' to see the post-level chain)." Same UX pattern as `Build Group B Sample`.
- **Constants added incrementally**: `ScenePath` const + `GroupBRewardPath` const + `GroupBStubAsmdef` const + `GroupCStubAsmdef` const + `TimeServiceTypeName` const + `DemoMonoBehaviourTypeName` const. The 4 reflection-target consts typo-proof every `Type.GetType` call against the exact namespace + asmdef strings (locked at B.4.0 + B.4.6).
- **Usings added**: `UnityEditor.SceneManagement` (`EditorSceneManager.NewScene` / `SaveScene`) + `UnityEngine.SceneManagement` (`SceneManager.MoveGameObjectToScene`).
- **`package.json` `samples[]` entry added**: 4th entry for `"Catalog — Group C — Progression"` describing the 5 catalog elements + 2 stubs + Group B dependency for the chain demo. Discoverable in Package Manager UI under Samples after upgrade. **This entry is the buyer-facing closure of Group C** — it's what makes the kit promise "drag and drop a popup that already works" tangible for the catalog's 5 progression elements.

### Triple-round `/_checker as user/dev/qa` (pre-tag audit) — 2026-05-04

Pre-tag audit on B.4.0 → B.4.6 + 5 prefabs + demo scene + samples[] entry. 12 findings collapsed via mental-pass to 2 FIX NOW (C1 + C2 — text-only edits applied this commit) + 8 NOTE deferred to M3/M4 polish (already captured in `kitforge_mobile_ui_kit_roadmap.md` § Diferidos or this CHANGELOG).

- **C1 — HUDTimer first-Play UX bug** (rol `as user` U2 + `as qa` Q2): default `_targetUtc = MinValue` made HUDTimer expire immediately on Play → "00:00" out-of-the-box. Fixed by adding `Start()` initializer in `CatalogGroupCDemo` that seeds 5-minute countdown so first impression shows a live ticking timer.
- **C2 — Multi-timer ContextMenu misleading name** (rol `as user` U3 + `as qa` Q5): "Chain — Multi-timer reset (3 min countdown)" was categorized as Chain but only set a single timer; spec QA 11 expects 3 instances of HUDTimer. Fixed by renaming + recategorizing as `"HUD Timer — Set 3min countdown (multi-timer requires extra prefab instances)"` with log message guiding manual instantiation. Honest naming aligned with what the helper actually does. The 6th mejora I scenario is still addressable — buyer can read the log and add extra HUDTimer prefab instances; the kit's responsibility ends at the contract documentation.

**8 NOTE deferred** (captured for `/_close` → memoria, no changes this session): N1 2-step build dependency (documented), N2 build dialog Group B prerequisite hint (M3), N4 cross-sample scene reference fragility (documented), N5 60s regen tick QA-slow (M3 ContextMenu helper), N7 DailyLoginFlow.ShowIfDue not in Play demo (M3), N8 InMemoryProgressionService stub-economy wiring fragility (documented), N9 demo no anti-spam guard (Group B precedent), N10 watch-to-double doubles only `rewards[0]` (limitation documented in spec D5).

### Spec citations (Phase B § 4.5 batch sweep)

- **`Documentation~/Specs/Catalog/GameOverPopup.md`** — `Services consumed` renamed to `Service binding`, cites § 4.5.
- **`Documentation~/Specs/Catalog/HUD-Energy.md`** — `Services consumed` renamed to `Service binding`, cites § 4.5 with HUD-degrade qualifier.
- **`Documentation~/Specs/Catalog/HUD-Timer.md`** — `Services consumed` renamed to `Service binding`, cites § 4.5 with HUD-degrade qualifier.

### Added (pre-flight infrastructure)

- **`HUDCurrency.cs`** under `Runtime/Catalog/HUD/` — parameterized HUD displaying a single currency from `IEconomyService`, replaces the v0.5.0 `HUDCoins`/`HUDGems` sibling classes. `[SerializeField] private CurrencyType _currency` set at prefab time only (tooltip warns against runtime changes — the HUD does not re-subscribe). Subscribes to `IEconomyService.OnChanged` and filters foreign currencies via `if (currency != ResolveCurrency()) return` (FD4 contract — sentinel `(CurrencyType)(-1)` used by RewardPopup for Item/Bundle is naturally excluded). `protected virtual CurrencyType ResolveCurrency() => _currency` — extension hook for subclass sealing.
- **`HUDEnergy.cs : HUDCurrency`** — sealed subclass with `protected override CurrencyType ResolveCurrency() => CurrencyType.Energy`. The inherited `_currency` Inspector field has no effect (read-once-via-virtual-method pattern); buyer cannot break the energy binding via Inspector mistake. Pre-flight ships the **sealing scaffold only** — Phase B HUD-Energy element delivery extends with regen countdown UI, max-cap label, energy bar fill, and 1Hz `IProgressionService.GetEnergyRegenState()` poll.
- **`IProgressionService` extension** in `Runtime/Services/IProgressionService.cs`: 2 new methods `GetDailyLoginState()` / `GetEnergyRegenState()` + 2 supporting `[Serializable]` structs. `DailyLoginState` carries `CurrentDay`, `LastClaimUtc`, `AlreadyClaimedToday`, `DoubledToday` (FQ3), `MaxStreakGapDays`. `EnergyRegenState` carries `Current`, `Max`, `NextRegenUtc`, `IsFull`. Backward compatible — existing level-progression members preserved.
- **`UIThemeConfig` slots (5 new)**: `IconEnergy`, `IconClock`, `StarFilledSprite`, `StarEmptySprite` (sprites under `[Header("Sprites")]`) + `FailureColor` (color under `[Header("Colors")]`, default `#E53935` red 600). Used by Phase B elements (HUD-Energy, HUD-Timer, LevelComplete star reveal, GameOver header tint). Existing Theme assets get the new slots auto-populated as null/default — buyer fills them via Bootstrap Defaults Editor menu or manually.
- **`Tests/Editor/Helpers/FakeProgressionService.cs`** — minimal `IProgressionService` test fake. Setters: `SetCurrentLevelIndex`, `SetDailyLoginState`, `SetEnergyRegenState`. Phase B element tests will use this for behavior coverage.
- **`Tests/Editor/HUDCurrencyTests.cs`** — replaces `HUDCoinsTests` + `HUDGemsTests` with parameterized `[TestCase(CurrencyType.Coins)]` + `[TestCase(CurrencyType.Gems)]` covering the 3 currency-relevant paths (initial value, change handler, large-value formatting). Adds standalone `OnChanged_With_Foreign_Currency_Does_Not_Trigger_ApplyValue` (FD4 currency-filter contract test). Null-service path keeps its dedicated test.
- **Documentation**: `Documentation~/Specs/Catalog/CATALOG_GroupC_DELTA.md` runbook with 8 sections — Theme slots / IProgressionService extension / HUDCurrency event filtering (FD4) / IEconomyService v2 / Q2 helpers contract / null-service fallback policy (§ 4.5) / HUDCurrency+HUDEnergy implementation specs (§ 4.6) / pre-flight follow-ups + 4-block migration table spec (§ 3) + 8 FIX-NEXT items absorbed from `as qa`/`as dev` mental pass.

### Changed (BREAKING)

- **`IEconomyService` v2** in `Runtime/Services/IEconomyService.cs` — typed surface removed (`GetCoins`, `GetGems`, `SpendCoins`, `SpendGems`, `AddCoins`, `AddGems`, `OnCoinsChanged`, `OnGemsChanged`). Replaced by parameterized: `int Get(CurrencyType)`, `bool Spend(CurrencyType, int)`, `void Add(CurrencyType, int)`, `bool CanAfford(CurrencyType, int)` (already parameterized in v1, signature unchanged), `event Action<CurrencyType, int> OnChanged`. See migration block 1 above for buyer-side renames.
- **`CurrencyType` enum** in `Runtime/Services/DTO/ShopItemData.cs`: `Energy = 2` value added; existing `Coins = 0`, `Gems = 1` preserved with explicit values for stable serialization.
- **`Runtime/Catalog/Popups/Shop/ShopPopup.cs`** — single `economy.OnChanged` subscription replaces previous double subscription to `OnCoinsChanged` + `OnGemsChanged`. Handler signature: `(CurrencyType _, int __) => RefreshAffordability()` — affordability check is currency-agnostic and fires once per any currency change.
- **`Tests/Editor/Helpers/FakeEconomyService.cs`** — implements v2 surface; `SetCoins` / `SetGems` / `AddCoins` / `AddGems` retained as **test-only helpers** internally calling `Add` / `SetCurrency` (now public). HUDCurrencyTests + non-economy fixtures do not need to change.
- **`Samples~/Catalog_GroupB_Currency/Stubs/InMemoryEconomyService.cs`** + the imported copy under `Assets/Samples/Kitforge Mobile UI Kit/0.5.0-alpha/...` — both rewritten to v2. Adds `_energy = 30` field + `Debug — Add 50 energy` ContextMenu so the Group B sample remains runnable post-upgrade for buyers who stay on Group B prefabs.
- **`Samples~/Catalog_GroupB_Currency/Stubs/InMemoryShopDataProvider.cs`** + imported copy — `_economy.SpendCoins/SpendGems` collapsed to `_economy.Spend(item.PriceCurrency, item.PriceAmount)`; `_economy.AddCoins(N)` → `_economy.Add(CurrencyType.Coins, N)`.
- **`Samples~/Catalog_GroupB_Currency/CatalogGroupBDemo.cs`** + imported copy — 9 v1 callsites migrated to v2. `HandleRewardClaimed` simplified from typed if/else chain to single `_services.Economy.Add(currency, amount)` (sentinel still filtered before the call).
- **`Editor/Generators/CatalogGroupBBuilder.cs`** — `BuildHUDCoins`/`BuildHUDGems` produce `HUDCurrency`-typed prefabs with `_currency` set via `SerializedObject.FindProperty("_currency").enumValueIndex`. Demo scene wiring uses `GetComponent<HUDCurrency>()`. `Build Group B Sample` continues to work post-migration — the menu item is preserved as a transitional path until `Build Group C Sample` ships in Phase B.

### Removed (BREAKING)

- **`Runtime/Catalog/HUD/HUDCoins.cs`** + `.meta` — replaced by `HUDCurrency` parameterized base.
- **`Runtime/Catalog/HUD/HUDGems.cs`** + `.meta` — same.
- **`Tests/Editor/HUDCoinsTests.cs`** + `.meta` — replaced by parameterized `HUDCurrencyTests` + FD4 filter test.
- **`Tests/Editor/HUDGemsTests.cs`** + `.meta` — same.
- **`IEconomyService` v1 surface**: 8 typed members removed (`GetCoins`, `GetGems`, `SpendCoins`, `SpendGems`, `AddCoins`, `AddGems`, `OnCoinsChanged`, `OnGemsChanged`). See Changed `IEconomyService v2` and migration block 1 above.

### Architecture decisions (locked in pre-flight)

- **Null-service fallback policy** (`CATALOG_GroupC_DELTA.md` § 4.5): HUDs degrade silently (render plain value or last-known); popups `Debug.LogError` actionable + abort `OnShow` so PopupManager queue advances; helpers `Debug.LogError` + return false. Mandatory error format: `"[ElementName]: I[ServiceName] not registered on UIServices. Wire it before opening this popup. See Quickstart § Service binding."`. Phase B element specs cite § 4.5 in their "Service binding" subsection. QA scenarios `Show_With_Null_Required_Service_Logs_Error_And_Aborts` per popup; `Bind_With_Null_Service_Renders_Plain_Value_Silently` per HUD.
- **Sealing pattern for HUD subclasses** (`CATALOG_GroupC_DELTA.md` § 4.6): `protected virtual CurrencyType ResolveCurrency() => _currency` in base; subclasses override to seal currency unconditionally. Prevents Inspector-mistake breakage. Validated on HUDEnergy.

### Pending (Phase B — required for `v0.6.0-alpha` tag cut)

- **5 catalog elements** under `Runtime/Catalog/`: `Popups/DailyLogin/`, `Popups/LevelComplete/`, `Popups/GameOver/`, `HUD/HUDEnergy.cs` extension (regen UI + IProgressionService poll), `HUD/HUDTimer.cs`. Specs already locked in `Documentation~/Specs/Catalog/`.
- **3 helpers** (Q2 lock — capability-gated for Group C): `RewardFlow.GrantAndShow` (+ DTO overload F2), `RewardFlow.GrantAndShowSequence` (with `onSequenceComplete` callback F5), `ShopFlow.OpenWithPurchaseChain`.
- **`Editor/Generators/CatalogGroupCBuilder.cs`** — skeleton DONE in B.4.1 + 5 `BuildXxx` generators DONE in B.4.2-B.4.5 + `BuildDemoScene` + `TryAttachStubServices` + `WireHUDServices` DONE in B.4.6 (above). **Builder complete** — `Build Group C Sample` MenuItem now generates 5 prefabs + demo scene end-to-end.
- **`Samples~/Catalog_GroupC_Progression/CatalogGroupCDemo.cs`** (B.4.6) — chain demo with 6 `[ContextMenu]` scenarios (LevelComplete → Reward sequence, GameOver Continue paths, DailyLogin auto-trigger, Energy regen tick, multi-timer per HUD-Timer QA scenario 11) + builder-wired prefab references.
- **`package.json` `samples[]` entry** for "Catalog — Group C — Progression" (deliberately NOT added in B.4.0 per `/_checker` mejora F1 — adding it without B.4.6 demo would expose a half-finished sample to buyers; lands at B.4.6 closure).
- **`Samples~/Catalog_GroupC_Progression/`** stub + asmdef shipped in B.4.0 (above) — buyer-facing demo wiring lands at B.4.6.

### Done criteria (for `v0.6.0-alpha` tag)

Pre-flight (this batch — done 2026-05-03):
- [x] `UIThemeConfig` 5 new slots compile; gate green
- [x] `IProgressionService` extended; `FakeProgressionService` ships; gate green
- [x] `IEconomyService` v2 BREAKING migration applied (Runtime + Tests + Samples~ source + imported `Assets/Samples` copy); gate green
- [x] `HUDCurrency` parameterized base + `HUDEnergy` minimal sealing subclass shipped; `HUDCurrencyTests` covers parameterized cases + FD4 filter test; gate green
- [x] `CatalogGroupBBuilder` migrated to produce HUDCurrency-based prefabs (back-compat for Group B sample post-upgrade)
- [x] All 119 baseline tests pass on fresh compile after each delta
- [x] CHANGELOG migration table present with 5 mandatory blocks (this section)

Phase B (in progress):
- [x] DailyLogin element 1/5 — DTO + behavior + Flow helper + UIAnim wrapper + 25 EditMode tests + § 4.5 spec citation
- [x] LevelComplete element 2/5 — DTO + behavior + UIAnim wrapper + 16 EditMode tests + § 4.5 spec citation. Animation polish (star cascade, score rollup, CTA stagger) deferred to `_tween` session.
- [x] GameOver element 3/5 — DTO + `ContinueMode`/`BackPressBehavior` enums + behavior + UIAnim wrapper + 18 EditMode tests + § 4.5 spec citation. Animation polish (CTA cascade, Cinematic preset Failure-color accents) deferred to `_tween` session.
- [x] HUD-Energy element 4/5 — extended with regen UI (countdown + cap suffix + bar fill) + 1Hz `IProgressionService` poll per § 4.6 + auxiliary OnChanged-driven poll per E3 + 10 EditMode tests + § 4.5 silent-degrade contract proof + anchor `// OnUpdate-workaround-M3-sweep` per mejora F. Empty pulse (E8) + overcap tint deferred to M3 polish.
- [x] HUD-Timer element 5/5 — `HUDTimer` + `TimerMode` enum (3 modes) + warning/expiry tween infra + pause/resume + format validation + 10 EditMode tests + § 4.5 silent-degrade drift fix (UTC null-Time → `"--:--"` silently) + `UIAudioCue.Error` for warning (Failure cue doesn't exist, drift fix) + anchor `// OnUpdate-workaround-M3-sweep` per mejora F. Multi-timer test (QA scenario 11) + server-time-jump-backward (QA 14) deferred to B.4 builder/demo coverage.
- [x] Helpers — capability-gate verdict 2026-05-04: ✅ `RewardFlow.GrantAndShowSequence` shipped (2 callsites); ⏳ `RewardFlow.GrantAndShow` single + ⏳ `ShopFlow.OpenWithPurchaseChain` deferred Group D (1 callsite each — capability-gate failed). 7 EditMode tests + 1 integration test (LevelCompletePopupTests via mejora H).
- [x] `Build Group C Sample` builder + chain demo (B.4.1-B.4.6 closed 2026-05-04 — 5 prefabs + demo scene + 6 chain ContextMenu scenarios + `samples[]` entry)
- [x] `InMemoryProgressionService` + `InMemoryTimeService` sample stubs shipped under `Samples~/Catalog_GroupC_Progression/Stubs/` (B.4.0 + B.4.6 closed 2026-05-04)
- [x] Tag `v0.6.0-alpha` BREAKING (single bump per group) — closed 2026-05-04, M1 done

## [0.5.0-alpha] — 2026-05-02

> Group B — Currency catalog. Adds 3 popups (Reward / Shop / NotEnoughCurrency), 2 HUD elements (Coins / Gems), 3 in-memory service stubs (Economy / Shop / Ads), one demo scene chaining the full monetization loop, and an `UIAnimPopupBase` consolidating the per-popup animator boilerplate.

### Added
- **5 catalog elements** under `Runtime/Catalog/`:
  - `Popups/Reward/` — `RewardKind`, `RewardPopupData`, `RewardPopup`, `UIAnimRewardPopup`. Supports `Coins`/`Gems`/`Item`/`Bundle` reward kinds, optional `AutoClaimSeconds`, optional backdrop-tap-to-claim. Emits `OnClaimed(CurrencyType, int)` + `OnDismissed`. Item/Bundle use sentinel `(CurrencyType)(-1)` to signal "host-resolved" amount. Popup never mutates economy — host wires the credit.
  - `Popups/Shop/` — `ShopPopupData`, `ShopCategoryFilter`, `ShopItemView` (cell), `ShopPopup`, `UIAnimShopPopup`. Clone-from-template grid; subscribes to `IEconomyService.OnCoinsChanged`/`OnGemsChanged` and re-evaluates affordability without rebuild. Calls `IShopDataProvider.Purchase(itemId)` only — never `IEconomyService.Spend*`. Disables the cell of an item that returned `InsufficientFunds` until the next currency event refreshes it (prevents repeat-fire spam).
  - `Popups/NotEnough/` — `NotEnoughCurrencyPopupData`, `NotEnoughCurrencyPopup`, `UIAnimNotEnoughCurrencyPopup`. Three CTAs: BuyMore / WatchAd / Decline. Queries `IAdsService.IsRewardedAdReady()` on Bind to gray out Watch Ad when no ad is available. Emits `OnBuyMoreRequested(CurrencyType, int)` / `OnWatchAdRequested(CurrencyType, int)` / `OnDeclined`. Logs warning if all CTAs hidden + backdrop disabled (foot-gun guard for iOS-only builds without hardware back).
  - `HUD/HUDCoins.cs` + `HUD/HUDGems.cs` — Live counters reactive to `IEconomyService` typed events. Punch-scale tween on every change (kill-before-create, `SetLink(gameObject)`). Default format `"N0"` (thousand separators). Optional `UnityEvent` click hook for "open shop" wiring. `"--"` fallback + actionable error log when `IEconomyService` is unavailable.
- **`UIAnimPopupBase`** under `Runtime/Catalog/_Internal/`. Consolidates the show / hide / preset / sequence / Snap / ResetToShowStart logic that was previously duplicated across each popup's animator. New popups now inherit and add zero code (e.g., `public sealed class UIAnimRewardPopup : UIAnimPopupBase { }`). Group B animator files reduced from 3 × 110 lines to 1 base + 3 × 3-line stubs.
- **3 in-memory stubs** under `Samples~/Catalog_GroupB_Currency/Stubs/`: `InMemoryEconomyService` (250 coins / 5 gems seed, debug ContextMenus), `InMemoryShopDataProvider` (4 hardcoded items spanning Currency / Consumable / Cosmetic categories, delegates `Spend` to the economy service via `[RequireComponent]`), `InMemoryAdsService` (1s simulated rewarded ad). All deterministic — no network, no save, no persistence.
- **`Build Group B Sample` editor menu** (`CatalogGroupBBuilder.cs`): generates 5 prefabs + 1 demo scene under `Assets/Catalog_GroupB_Demo/`. Pre-flight check warns if Theme or stubs are missing. Wires UIServices + 3 stubs + HUD instances + Demo MonoBehaviour with all prefab references in one click.
- **Demo MonoBehaviour** (`CatalogGroupBDemo.cs`) with 19 `[ContextMenu]` triggers across 5 sections: HUD debug (5 — Add/Spend coins/gems), Reward variations (7 — Coins/Gems/Item/Bundle/Auto/Empty/Backdrop), Shop variations (3 — All/Currency/Cosmetics), NotEnough variations (3 — Coins/Gems/Decline-only), and 1 **`Chain — Shop → NotEnough → Ad → Reward`** end-to-end demonstration. Chain trigger pre-flight-checks all three popup prefabs are assigned before opening the Shop.
- **Specs** under `Documentation~/Specs/Catalog/`: `RewardPopup.md`, `ShopPopup.md`, `NotEnoughCurrencyPopup.md`, `HUD-Coins.md`, `HUD-Gems.md`. Each opens with a "Decisions to confirm" table promoting per-element implementation choices to spec contract.
- **EditMode tests +37**: 8 RewardPopup, 9 ShopPopup (including `InsufficientFunds_Disables_The_Matching_Cell_Until_Affordability_Refreshes`), 10 NotEnoughCurrencyPopup, 4 HUDCoins, 4 HUDGems (including `Coins_Changed_Does_NOT_Update_Gems_HUD` for typed-event isolation). Total catalog tests: 90+.
- **Test helpers** under `Tests/Editor/Helpers/`: `NullAnimator.cs` and `FakeEconomyService.cs` shared across the 5 new test fixtures. Replaced 3× duplicate `NullAnimator` and 2× duplicate `FakeEconomyService` private nested classes.

### Changed
- **`UIHUDBase`**: added `protected void SetServicesInternal(UIServices)` to enable test injection without reflection. HUDCoins/HUDGems `SetServicesForTests` now a one-line forward instead of `BindingFlags.NonPublic` field hacking.
- **Sample registration**: `package.json` `samples[]` now lists 3 entries (Quickstart, Catalog Group A, Catalog Group B).
- **`NotEnoughCurrencyPopupData` defaults aligned to spec N5**: `ShowDecline` `true` → **`false`** (decline button is opt-in; backdrop/back already cover the implicit decline path). `CloseOnBackdrop` `false` → **`true`** (forgiving UX; tap-out cancels the offer). DTO was internally inconsistent with `Documentation~/Specs/Catalog/NotEnoughCurrencyPopup.md` § N5; tests `Default_DTO_Hides_Decline_Button` and `CloseOnBackdrop_True_By_Default` codified the spec contract. **No buyer-visible regression** (no tagged version shipped the previous defaults).

### Fixed
- **`Samples~/Catalog_GroupB_Currency/CatalogGroupBDemo.cs`**: removed duplicate `using KitforgeLabs.MobileUIKit.Catalog.NotEnough` directive and duplicate `[SerializeField] private GameObject _notEnoughPrefab` field declaration (CS0102 compile error). Group B sample asmdef failed to compile, which silently cascaded — `Build Group B Sample` resolved demo + stub types via `System.Type.GetType` and got `null` from a non-compiling assembly, producing a demo scene with no `CatalogGroupBDemo` component, no in-memory stubs, and no HUD service wiring. The 19 ContextMenu triggers were not exercisable until this fix landed.
- **`Tests/Editor/Helpers/FakeShopDataProvider.cs`** (new — mirrors `FakeEconomyService` pattern): minimal `IShopDataProvider` test fake exposing `SetItems` / `QueuePurchaseResult`. `ShopPopupTests.SetUp` now creates a `UIServices` + injects the fake via `services.SetShopData(_shopData)` + calls `_popup.Initialize(null, services)`. Without this, 7 of 8 ShopPopup tests false-failed on a fresh test run because `ShopPopup.Bind` → `RebuildGrid` → `ResolveItems` emits `[ShopPopup] No IShopDataProvider available` `LogError`, and Unity's Test Runner treats unexpected `LogError` as a failure. **Symptom only — no runtime change**; the LogError is intentional buyer-facing diagnostic when wiring is forgotten.
- **`UIServices`**: added `[DefaultExecutionOrder(-100)]` so its `Awake` (where serialized service refs are resolved into typed properties: `Economy`, `ShopData`, `Ads`, …) is guaranteed to run before any consumer's `OnEnable`. Without this, Unity does NOT guarantee Awake-before-OnEnable across GameObjects in the same scene; HUDCoins/HUDGems `OnEnable → Refresh` could read `Services.Economy == null` and emit the actionable buyer-facing `LogError` even when wiring was correct in the Inspector. Caught only during the in-Editor Group B chain demo verification — the 119 EditMode tests injected services manually via `SetServicesForTests` and could not exercise this code path. **Buyer impact:** silently fixes any UIServices+HUD scene where script execution order was previously undefined.

### Deferred (Group C)
- `RewardFlow.GrantAndShow` / `RewardFlow.GrantAndShowSequence` / `ShopFlow.OpenWithPurchaseChain` — capability-gate fails for Group B (1 chain callsite). Will land when 3+ callsites materialize at Group C kickoff (DailyLogin / LevelComplete / GameOver). Spec stub written in `RewardPopup.md` and `ShopPopup.md` to lock the event surface that the helpers will consume.
- `IEconomyService` v1 (typed `OnCoinsChanged`/`OnGemsChanged`) → v2 (parameterized `OnChanged(CurrencyType, int)`) migration — open question for Group C kickoff before HUD-Energy lands. Tradeoff table documented in `HUD-Gems.md` and `kitforge_mobile_ui_kit_roadmap.md`.

### Done criteria
- [x] Group B 5 elements + tests + specs + Build Group B Sample editor menu + demo scene + chain trigger.
- [x] All popups respect MUSTN'T #1 (no service mutation, no popup-to-popup spawn calls).
- [x] HUD-Coins and HUD-Gems coexist in the same scene with isolated punch tweens (no cross-channel leakage).
- [x] Chain demo wires the full Shop → NotEnough → Ad → Reward → HUD update loop using only public events.

## [0.4.1-alpha] — 2026-05-02

### Removed
- **`Game Wiring` sample parked**: removed from `package.json` `samples[]` so Package Manager no longer offers it for import. Source files stay under `Samples~/GameWiring/` (`GameWiringLifetimeScope.cs`, `StubServices.cs`, `UIRouterStub.cs`, `KitforgeLabs.MobileUIKit.GameWiring.asmdef`, `README.md`) for revival once service contracts stabilize at end of Group D. **Why**: pre-existing buyer-facing P0 caught during v0.4.0 verification — sample required VContainer (opt-in, not installed in PACHINKO host) and the 5 stub services had ~30 unimplemented interface members added during v0.3.0 G3 work. Importing the sample blocked the entire host project from compiling. Path to revival = update stubs after Group D ships `IPlayerDataService` + `SettingsPopup`, then re-add the entry to `samples[]`.

### Changed
- **README**: VContainer prereq note rewritten — no longer mentions `Samples~/GameWiring`; instead points buyers to `UIServices` MonoBehaviour setters as the DI integration surface. Architecture decision #1 + #8 updated. Phase 1 done-criteria checkbox for `Samples~/GameWiring` removed (no longer shipped). Install URL bumped to `v0.4.1-alpha`.
- **Quickstart README**: 3 cross-references to `Game Wiring` rewritten as "parked under `Samples~/`, returns once Group D ships".
- **Catalog Group A README**: "See also" link to GameWiring annotated as parked.

### Done criteria
- [x] `package.json` `samples[]` lists Quickstart + Catalog Group A only (2 entries).
- [x] No README references guide buyers to import a broken sample.
- [x] `Samples~/GameWiring/` source preserved on disk for future revival.

## [0.4.0-alpha] — 2026-05-02

> Group A — Pure UI catalog. Tag cut after Unity Editor verification of the 7-item Done criteria below: Bootstrap Defaults runs, `Build Group A Sample` produces 4 prefabs + scene with all 5 references wired (Theme + 4 prefabs), 4 prefabs open clean (36/36 catalog refs + 4/4 animator refs), 12 ContextMenu entries spawn correctly in Play, EditMode Test Runner reports 86/86 green, Editor.asmdef compiles after the Catalog reference addition, `git status` aligned with the deliverables list.

### Group A closure (this batch — specs, extra tests, severity polish, prefab generator, Theme contract honoring)
Resolves the per-element `Pending` lines flagged in the four "Group A — element N/4" entries below. Closes the TA checker gap "Theme sprite slots not consumed by generated prefabs" — the core "skin it once" pitch now actually works for catalog prefabs.

- **Specs written** under `Documentation~/Specs/Catalog/`: `NotificationToast.md`, `PausePopup.md`, `TutorialPopup.md`. Each opens with a **"Decisions to confirm"** block (D1-D8 / D1-D7 / D1-D8 respectively) promoting per-element implementation choices to spec contract. `ConfirmPopup.md` already shipped.
- **EditMode tests +25**: PausePopup +9 (intent + shortcut + toggle events for Restart / Home / Quit / Settings / Shop / Help / Sound / Music / Vibration), TutorialPopup +9 (3 backdrop modes / 2 StartIndex clamping / `CompleteTutorial` API / re-Bind StartIndex / 2 empty-Steps no-op), NotificationToast +7 (3 tap paths / 3 severity mappings / re-bind safety). Total catalog tests: 53. Total package tests: **86** (33 framework + 53 catalog).
- **TutorialPopup early-return guards**: `GoNext` and `GoPrevious` now `if (StepCount == 0) return` — prevents `OnNext(0)` firing with empty Steps list (defensive; `Refs` UI hides those buttons but programmatic calls would have leaked the event).
- **NotificationToast severity mapping upgraded**:
  - `Warning` → `WarningColor` (new Theme field) — was `AccentColor` (semantic neighbor; not distinct enough for a premium asset).
  - `Info` / `Warning` / `Error` → `IconInfo` / `IconWarning` / `IconError` (new Theme slots) — were `null` (only `Success` had `IconCheck`).
  - `SeverityIcon` hide path: `gameObject.SetActive(false)` instead of `Image.enabled = false` — `LayoutGroup` now reflows the row when icon slot is null instead of leaving a gap.
  - `SeverityToColor` / `SeverityToIcon` / `SeverityToCue` helpers: `private static` → `internal static` to allow direct unit testing without prefab Refs setup.
- **`UIThemeConfig`**: added `_warningColor` (default `(1.00f, 0.60f, 0.15f, 1f)` saturated orange), `_iconInfo`, `_iconWarning`, `_iconError` sprite slots. Removed legacy `_transitionSpeed` and `_bounceStrength` fields (zero consumers since v0.3.0 — animation tuning lives entirely in `UIAnimPreset` SO now). **BREAKING for assets that wrote those slots** — re-save existing Theme assets after upgrade.
- **Access modifier changes (testability)**: 9 `Handle*` in `PausePopup`, 1 in `TutorialPopup` (`HandleBackdrop`), 1 in `NotificationToast` (`HandleTap`) promoted `private` → `internal`. Catalog test asmdef has `InternalsVisibleTo` since v0.3.0; this matches the precedent set by `SetAnimatorForTests`.
- **`PausePopupTests` SetUp** now creates a mock `UIThemeConfig` + `UIAnimPreset` + `UIServices` and calls `popup.Initialize(theme, services)` before each test — silences the cosmetic `[PausePopup] Theme not initialized` and `[PausePopup] No animation preset resolved` warnings the four `OnShow*` tests were emitting. The other three Catalog fixtures don't call `OnShow` so they don't need this.
- **New Editor generator** `Tools/Kitforge/UI Kit/Build Group A Sample` (`Editor/Generators/CatalogGroupABuilder.cs`): single-click materialization of `ConfirmPopup.prefab` + `PausePopup.prefab` + `TutorialPopup.prefab` + `NotificationToast.prefab` + `Catalog_GroupA_Demo.unity` into `Assets/Catalog_GroupA_Demo/`. Demo scene wires a `Canvas` (1080×1920 portrait CanvasScaler) + `EventSystem` + `UIServices` + the `CatalogGroupADemo` host MonoBehaviour with 12 `[ContextMenu]` triggers (4 Confirm tones / 2 Pause variants / 2 Tutorial variants / 4 Toast severities). Idempotent: re-running overwrites.
- **`CatalogGroupABuilder` asset-reference persistence fix + `CatalogGroupADemo` field-type refactor** (caught during v0.4.0 verification, two complementary changes):
  - **Builder**: original implementation (a) passed `PrefabUtility.SaveAsPrefabAsset` return values directly into `BuildDemoScene`'s `SerializedProperty.objectReferenceValue` assignments — Component-on-prefab refs serialized as `{fileID: 0}`; (b) loaded `UIThemeConfig` BEFORE `EditorSceneManager.NewScene(EmptyScene, Single)` — the asset reference survived in memory but Unity's scene serializer wrote it as `{fileID: 0}` after the scene replacement. Diagnostic logs proved both pre-Apply and post-Apply re-reads of the SerializedProperty held a valid reference (instanceID present, name resolvable), yet `SaveScene` persisted null. Root cause for both: asset references obtained outside the active scene's lifetime become "stale" for the scene serializer even though they remain valid C# references. Fix: `BuildAll` now does `SaveAssets()` + `Refresh()` between prefab creation and `BuildDemoScene()`. `BuildDemoScene` calls `EditorSceneManager.NewScene` FIRST, then loads all 5 assets via `AssetDatabase.LoadAssetAtPath` (Theme as `UIThemeConfig`, prefabs as `GameObject`) — references obtained against the active scene context persist correctly. `BuildAll` no longer threads prefab refs through parameters.
  - **Sample**: `CatalogGroupADemo` 4 prefab fields changed from `ConfirmPopup`/`PausePopup`/`TutorialPopup`/`NotificationToast` Component types → `GameObject`. Decouples the asset persistence story from Component-vs-GameObject reference type (orthogonal cleanup). SpawnXxx methods now do `Instantiate(prefab).GetComponent<T>()` at runtime with explicit null check (logs an error and destroys the orphan if a buyer assigns a prefab missing the expected component). New private `SpawnPopup<T>` helper deduplicates the 3 popup spawn paths; `SpawnToast` stays inline (different parent + base type). Trade-off: Inspector drag-drop accepts any prefab, not just those carrying the typed Component — runtime check enforces the contract instead. Documented via `[Header]` tooltip.
- **New Sample** registered in `package.json`: **"Catalog — Group A — Pure UI"** (`Samples~/Catalog_GroupA_PureUI/`). Ships host script (`CatalogGroupADemo.cs`), asmdef, README. Two-step OOTB flow: `Bootstrap Defaults` → `Build Group A Sample`.
- **`Editor.asmdef`**: now references `KitforgeLabs.MobileUIKit.Catalog` + `Unity.TextMeshPro` (required by the builder).
- **README §Status rewritten** with a 6-group table (0 / A / B / C / D / E) showing per-group ship status. Latest tag updated to `v0.3.0-alpha`. Total tests count surfaced.
- **README §Architecture decisions** new entry #11: a single `UIThemeConfig` asset feeds `UIManager` + `PopupManager` + `ToastManager`. Closes the documentation gap flagged by the close_context.
- **`Documentation~/Specs/CATALOG.md`** row links: ConfirmPopup / PausePopup / TutorialPopup / NotificationToast all link to their per-element specs.
- **`package.json` description** updated to mention the catalog (4 elements shipped in `v0.4` + group-by-group roadmap).
- **NEW Theme contract: `IThemedElement` + `ThemedImage` + `ThemedText`** in `Runtime/Theme/`:
  - `IThemedElement.ApplyTheme(UIThemeConfig)` is the public contract; any component implementing it gets called when a popup or toast is initialized.
  - `ThemedImage` (slots: `ThemeSpriteSlot` enum {`PanelBackground` / `ButtonPrimary` / `ButtonSecondary` / `Backdrop` / `Divider`} + `ThemeColorSlot` enum 10 entries including new `WarningColor`).
  - `ThemedText` (slots: `ThemeFontSlot` enum {`FontHeading` / `FontBody` / `FontCaption`} + `ThemeColorSlot` + `ThemeFontSizeSlot`).
  - `None` enum value on every slot = "leave the authored value untouched" — supports literal-color text on themed buttons (white-on-primary).
- **`UIModuleBase.Initialize` and `UIToastBase.Initialize`** now walk children and call `ApplyTheme` on every `IThemedElement` found. `protected ApplyThemeToChildren(theme)` available for manual re-apply if buyer ever needs it (e.g., theme swap mid-session). One-time on instantiate; no perf cost in steady state.
- **`CatalogGroupABuilder` wires `ThemedImage` / `ThemedText`** on Card backgrounds, primary/secondary/danger button backgrounds, Title (FontHeading / TextPrimary), Body / Subtitle / Progress (FontBody or FontCaption / TextSecondary), button labels (FontBody / TextPrimary or untouched). Confirm / Pause / Tutorial / Toast all benefit. Backdrop, Toast `SeverityTint`, ConfirmPopup `ToneStrip` and Toggle visuals stay non-themed by design (their tint/visuals are dynamic per-data, not Theme-driven).
- **Sample README** new sections: **Theme contract** (table of which slot drives which element) + **Hierarchy stability** (safe vs not-safe edits to prefab variants) + clarified re-run instructions (rename output folder before customizing).

### OUT of scope for v0.4.0-alpha
Explicit limits — these do NOT ship in this tag and are not promised by `v0.4.0-alpha`:

- **Catalog Groups B / C / D / E** (RewardPopup, ShopPopup, NotEnoughCurrencyPopup, DailyLoginPopup, LevelCompletePopup, GameOverPopup, SettingsPopup, LoadingScreen, MainMenuScreen, HUD-Coins, HUD-Gems, HUD-Energy, HUD-Timer). 11 catalog elements remain. Group B is next per the roadmap.
- **Default severity icon sprites**. The Theme has slots for `IconInfo` / `IconWarning` / `IconError` but **no default sprites are shipped** — buyer assigns their own. The toast renders correctly without them (icon `gameObject.SetActive(false)`); shipping defaults requires Texture2D + Sprite asset generation in `Bootstrap Defaults`, deferred to a polish session.
- **`Build Group A Sample` style polish**. The generated prefabs are functionally complete but cosmetically minimal: flat colors, no rounded corners, default Unity button colors, no shadow, no padding finesse. Sufficient for a working demo, NOT representative of the final visual quality target ("Disney Getaway Blast" reference). Buyers ARE expected to restyle.
- **Pre-built prefabs in `Samples~/`**. Prefabs are generated on demand by the menu, not committed to the package. Rationale: prefab YAML drift and TMP/Unity GUID resolution issues across versions; the builder script is more maintainable than a binary asset to track.
- **Test Runner CI integration**. The 86 EditMode tests must be run manually until a CI pipeline is set up (out of scope for product-deliverable milestones).
- **Roadmap final sync**. Studio-internal roadmap will be synced to `v0.4.0-alpha` AFTER the tag is cut to avoid claiming a tag that doesn't exist yet.
- **`Samples~/GameWiring/` repair**. Discovered during v0.4.0 verification (2026-05-02): the legacy `GameWiring` sample is broken on import — VContainer is opt-in (not installed in PACHINKO host project, ~3 errors) and the 5 stub services (`StubEconomyService`, `StubPlayerDataService`, `StubProgressionService`, `StubShopDataProvider`, `StubAdsService`, `StubTimeService`) have not been updated since v0.1.0; ~30 interface members added during v0.3.0 G3 service binding work are unimplemented (CS0535). Importing the sample blocks the entire project from compiling. **Pre-existing damage, NOT a v0.4.0 regression** — was masked because `Samples~/` does not compile until imported. Buyer-facing P0. Path forward to be decided in a follow-up session: (a) update stubs + add VContainer dependency note in sample README, (b) refactor sample to be VContainer-free, or (c) remove sample from `package.json` `samples[]` until service contracts stabilize. Option (c) is the cleanest minimum; (a) is the highest fidelity. Tracked in `kitforge_mobile_ui_kit_roadmap.md` for prioritization.

### Done criteria — verified inside Unity (2026-05-02)

- [x] `Tools/Kitforge/UI Kit/Bootstrap Defaults` runs without error and creates `UIThemeConfig_Default.asset` + 10 `UIAnimPreset_*.asset` files.
- [x] `Tools/Kitforge/UI Kit/Build Group A Sample` runs without error and creates 4 prefabs + 1 scene under `Assets/Catalog_GroupA_Demo/`. Demo GameObject has all 5 references wired (Theme + 4 prefabs + UIServices + 2 parents).
- [x] All 4 generated prefabs validated structurally via YAML inspection: 36/36 catalog `Refs` fields and 4/4 animator `_card`/`_root` fields populated (`{fileID: <non-zero>}`). No missing-script entries.
- [x] `Catalog_GroupA_Demo.unity` Play + 12 `[ContextMenu]` entries: confirmed by user during verification.
- [x] **Theme reskin chain** structurally verified: `ThemedImage` and `ThemedText` components present on all themed elements per prefab YAML; `UIModuleBase.Initialize` / `UIToastBase.Initialize` walk children via `GetComponentsInChildren<IThemedElement>(true)` and call `ApplyTheme`. Runtime swap test deferred (pure visual confirmation, contract proven by component graph).
- [x] EditMode Test Runner: 86/86 passed (`TestResults.xml` confirms `total=86 passed=86 failed=0 inconclusive=0 skipped=0`).
- [x] Editor.asmdef compiles with the Catalog reference addition (verified by builder execution + diagnostic logs running successfully).
- [x] `git status` aligned with the v0.4.0-alpha deliverables list (no orphan files outside the catalog scope).

### Verification-only fixes applied during the closure pass

- `EditorSceneManager.NewScene(EmptyScene, Single)` was invalidating the asset references (Theme `ScriptableObject` + 4 prefab Components) loaded BEFORE it — references survived in memory but the scene serializer wrote `{fileID: 0}` to disk. Resolved by moving `LoadAssetAtPath` calls AFTER `NewScene`. Diagnostic logs proved both pre-Apply and post-Apply re-reads held a valid reference yet `SaveScene` persisted null until the load order was corrected. See `CatalogGroupABuilder` entry above.

### Animation system simplified — drop enum + library indirection (BREAKING)
- **Why**: 3-piece system (UIAnimStyle enum + UIAnimPresetLibrary SO + UIAnimPreset SOs) violated the kit's own pitch ("skin it once") — buyer needed 8 manual steps to get a fresh Theme to animate. Default Theme silently produced un-animated popups with zero feedback.
- **Removed**: `UIAnimStyle` enum (10 styles + Custom), `UIAnimPresetLibrary` SO (style->preset map with fallback), `UIAnimPresetLibraryEditor` (Editor inspector for the library).
- **Removed from `UIThemeConfig`**: `_defaultAnimStyle` field, `_animPresetLibrary` field, `DefaultAnimStyle` getter, `AnimPresetLibrary` getter.
- **Added to `UIThemeConfig`**: `_defaultAnimPreset` field (single `UIAnimPreset` ref), `DefaultAnimPreset` getter, tooltip clarifying "null = popups appear without animation".
- **`UIModuleBase` / `UIToastBase`**: `virtual UIAnimStyle? AnimStyleOverride => null` -> `virtual UIAnimPreset AnimPresetOverride => null`. Per-element override is now a direct preset ref.
- **All 4 popups + toast `ResolvePreset()` simplified**: `AnimPresetOverride ?? Theme?.DefaultAnimPreset`. Two levels of indirection collapsed into one.
- **`UIAnimPreset`**: removed `_style` identity field + `Style` getter (asset filename + folder communicate identity now).
- **`DefaultUIAnimPresetsCreator` rebuilt**: menu `Tools/Kitforge/UI Kit/Bootstrap Defaults`. Single click creates 10 presets at `Assets/Settings/UIAnimPresets/` AND a `UIThemeConfig_Default.asset` at `Assets/Settings/UI/` pre-wired to the Playful preset. Buyer's OOTB experience drops from 8 steps to 1.
- **Silent failure fix**: `UIModuleBase` and `UIToastBase` now expose `ResolveAnimPreset()` with a one-shot `Debug.LogWarning` if no preset is resolved (Theme null OR `DefaultAnimPreset` null OR override null). Points the buyer at the bootstrap menu by name. Replaces the duplicated `ResolvePreset()` private helper in 4 popups + toast.
- **README Quickstart rewritten**: step 1 is now the bootstrap menu, step 4 references the auto-generated `UIThemeConfig_Default` asset. Buyer never sees a null preset on first run.
- **Pending**: update `*PopupTests` if any reference the removed enum (`UIAnimStyle` not found in tests grep — should be safe but Unity Test Runner re-run required to confirm).

### Group A — element 4/4: TutorialPopup (scaffolded, code+tests, no spec/prefab yet)
- **`Runtime/Catalog/Popups/Tutorial/`**:
  - `TutorialStep` POCO (Title + Body[TextArea] + optional Sprite).
  - `TutorialPopupData` (List<TutorialStep>, StartIndex, ShowPrevious, ShowSkip, LoopBackToFirst, CloseOnBackdrop, TapToAdvance, custom labels Next/Previous/Skip/Done).
  - `TutorialPopup` (`UIModule<TData>`, `[RequireComponent(UIAnimTutorialPopup)]`, lazy `IUIAnimator` + `internal SetAnimatorForTests`, public `GoNext`/`GoPrevious`/`SkipTutorial`/`CompleteTutorial`/`CurrentIndex`/`StepCount`/`IsFirstStep`/`IsLastStep`, dynamic Next-label that mutates to `DoneLabel` on last step, ProgressLabel "i / N" auto-formatted, theme-null warning one-shot, `ClearAllEvents` on Bind + OnDestroy).
  - `UIAnimTutorialPopup` (clone of `UIAnimPausePopup` structurally — `SetUpdate(true)` defensive in show+hide for tutorials shown over a paused gameplay layer).
- **Decisions taken without consult — flag for review**:
  - Back press = Skip (mobile modal convention; Previous stays explicit).
  - GoNext on last step → `OnCompleted` + dismiss; if `LoopBackToFirst=true`, wraps to index 0 instead and fires `OnNext`.
  - `TapToAdvance` overrides `CloseOnBackdrop` on backdrop tap.
  - `GoPrevious` on first step is silently ignored (no event, no audio cue).
  - No `Time.timeScale` handling — Tutorial is gameplay-aware; if pause is needed, host wraps it (or composes inside Pause).
  - Single Next button mutates label vs separate Done button (consistent with mobile onboarding patterns).
  - Per-element animator (no shared code with Pause despite duplication — catalog rule: 1 animator per element).
- **EditMode tests**: `Tests/Editor/TutorialPopupTests.cs` (10 tests: Bind null defaults, Bind sets StartIndex, GoNext advances + StepChanged, GoNext on last → Completed+dismiss, GoNext on last with loop wraps, GoPrevious on first ignored, GoPrevious decrements, back press → Skip + dismiss, back press while dismissing ignored, Bind resets listeners). **All green: 10/10 (59/59 total).**
- **Pending**: spec (`Documentation~/Specs/Catalog/TutorialPopup.md`), prefab + Demo scene, CATALOG.md row link. Closes Group A → bumps minor to `0.4.0`.

### Group A — element 3/4: PausePopup (scaffolded, code+tests, no spec/prefab yet)
- **`Runtime/Catalog/Popups/Pause/`**: `PausePopupData` (7 buttons + 3 toggles inline + flags), `UIAnimPausePopup` (clone of Confirm with `SetUpdate(true)` for unscaled time), `PausePopup` (`UIModule<TData>`, captures/restores `Time.timeScale` around show/hide, Dismissing vs Shortcut button categories, inline toggles mutate `_data` without closing, public `Resume()`, `OnBackPressed→HandleResume`, theme-null warning, `OnDestroy` restores timeScale if `IsPaused`).
- **Decisions taken without consult — flag for review**:
  - Two button categories: **Dismissing** (Resume/Restart/Home/Quit) close, **Shortcut** (Settings/Shop/Help) raise event and keep popup open.
  - Inline toggles (Sound/Music/Vibration) mutate `_data.XxxOn` + emit `OnXxxChanged(bool)`, never dismiss.
  - `Time.timeScale` direct, no `ITimeService` (YAGNI until 2nd consumer).
  - Pause applied AFTER show-anim (callback of `PlayShow`); restore BEFORE hide-anim.
  - `UIAnimPausePopup` uses `SetUpdate(true)` defensively in hide-anim too.
  - `CloseOnBackdrop` default = `false` (catalog-consistent).
  - Public `Resume()` for external triggers (gameplay button, etc.).
- **EditMode tests**: `Tests/Editor/PausePopupTests.cs` (6 tests: Bind null defaults, back→Resume+Dismiss, back ignored if IsDismissing, OnShow pauses + Resume restores, restores original value not hardcoded 1f, Bind resets listeners). **All green: 6/6 (49/49 total).**
- **Pending**: spec (`Documentation~/Specs/Catalog/PausePopup.md`), prefab + Demo scene, CATALOG.md row link.

### Group A — element 2/4: NotificationToast (scaffolded, code+tests, no spec/prefab yet)
- **Group 0 extension** (`Runtime/Toast/UIToastBase.cs`): added `event Action<UIToastBase> DismissRequested`, `bool IsDismissing { get; protected set; }`, `protected UIThemeConfig Theme`, `protected UIServices Services`, `virtual Initialize(theme, services)`, `protected RaiseDismissRequested()`. Aligns Toast layer with Q1/Q2 decisions taken in element 1.
- **`Runtime/Catalog/Toasts/`**: `ToastSeverity` enum (Info/Success/Warning/Error), `NotificationToastData` DTO (Message + Severity + DurationOverride + TapToDismiss), `NotificationToast` (`UIToast<TData>`, `[RequireComponent(UIAnimNotificationToast)]`, lazy `IUIAnimator`, severity → tint+icon+audio cue mapping, idempotent `DismissNow`, `OnTapped`/`OnDismissed` events, `ClearAllEvents` on Bind), `UIAnimNotificationToast` (slide-in via `PositionOffset` + fade, no scale — toasts slide, don't bounce).
- **Severity → theme mapping** (decisions taken without spec — flag for review):
  - Info → `PrimaryColor` + no icon + `UIAudioCue.Notification`
  - Success → `SuccessColor` + `IconCheck` + `UIAudioCue.Success`
  - Warning → `AccentColor` + no icon + `UIAudioCue.Notification` (no dedicated `WarningColor` in Theme — YAGNI until 2nd consumer)
  - Error → `DangerColor` + no icon + `UIAudioCue.Error`
- **`ToastManager`**: added `[SerializeField] UIServices _services`, `Initialize(theme, services)` propagation per toast, `DismissRequested` subscribe/unsubscribe lifecycle. ⚠️ **Breaking for Group 0 buyers** — existing `ToastManager` references in scene need new `_services` slot wired (migration note).
- **EditMode tests**: `Tests/Editor/NotificationToastTests.cs` (5 tests: Bind null defaults, duration override fallback, duration override honored, idempotent DismissNow, Bind resets listeners). **All green: 5/5 (43/43 total).**
- **Pending**: spec (`Documentation~/Specs/Catalog/NotificationToast.md`), prefab + Demo scene, CATALOG.md row link. Closes at end of Group A → bumps minor to `0.4.0`.

### Group A — element 1/4: ConfirmPopup (closed, code+tests+spec)
- **Catalog asmdef** `KitforgeLabs.MobileUIKit.Catalog` (refs Runtime + DOTween.Modules + TMP).
- `Runtime/Catalog/Popups/Confirm/`: `ConfirmTone` (Neutral/Destructive/Positive), `ConfirmPopupData`, `ConfirmPopup` (`UIModule<TData>`), `UIAnimConfirmPopup` (`IUIAnimator` + DOTween).
- **Architectural decisions closed**:
  - Q1 — `UIModuleBase.Initialize(theme, services)` virtual injection. PopupManager/UIManager wire on instantiate. No more `GetComponentInParent`. Testable without scene.
  - Q2 — `UIModuleBase.DismissRequested` event + `RaiseDismissRequested()` protected. Popups no longer hold a manager ref. PopupManager subscribes on resolve, unsubscribes on destroy.
  - Q3 — `IUIAudioRouter` + `UIAudioCue` enum (None/PopupOpen/PopupClose/ButtonTap/Success/Error/Notification). Slot in `UIServices`. Popups call `Services?.Audio?.Play(cue)` null-safe.
- **Race-condition hardening**: `UIModuleBase.IsDismissing` (protected set) elevated to base. Guards double-click confirm/cancel, back press during hide, backdrop spam.
- **Event-leak fix**: `ConfirmPopup.Bind` resets `OnConfirmed`/`OnCancelled`/`OnDismissed` before re-bind.
- **Null-safe Bind**: `Bind(null)` → `new ConfirmPopupData()`. `OnShow` calls `Bind(null)` if `_data == null`.
- **Tunable**: `UIAnimPreset.HideScaleTo` (default `0.9f`) replaces hardcoded scale in animator.
- **TA polish**: `[Serializable] private struct Refs` with `[Tooltip]` per field. Animator tooltips clarify `_card` is the scaled rect.
- **`[RequireComponent(typeof(UIAnimConfirmPopup))]`** + lazy animator resolve (covers prefab + dynamic `AddComponent` paths).
- **EditMode tests**: `Tests/Editor/KitforgeLabs.MobileUIKit.Catalog.Tests.asmdef` + `ConfirmPopupTests` (5 tests: Bind null, back-press routing with/without cancel, back-press during dismiss, Bind resets listeners). **All green: 5/5 (38/38 total).**
- **Spec**: `Documentation~/Specs/Catalog/ConfirmPopup.md` (full micro-spec: DTO, services, events, animation, theme, edge cases, QA scenarios, file layout).
- **CATALOG.md**: ConfirmPopup row links to spec; single-button alert mode documented.
- **Pending (Editor manual)**: `ConfirmPopup` prefab + Demo scene entry under `Samples~/Catalog/`. Will close at end of Group A together with Toast/Pause/Tutorial → bumps minor to `0.4.0`.

## [0.3.0-alpha] — 2026-05-01

### Session — 2026-05-01 (cont. 6) — Phase 2 re-scoped + Group 0 foundation
GOAL: Re-scope Phase 2 to deliver the prefab catalog (15 mid-core mobile UI elements) the kit name promises. Build Group 0 foundation (F1-F8) before any visible element.
DONE:
- **Spec**: `Documentation~/Specs/CATALOG.md` (15 elements decomposed into Screens / Popups / Transient+HUD; 10 plug-and-play MUST + 8 MUSTN'T contracts; 5-group build order).
- **F5 Animation system**: `Runtime/Animation/` — `UIAnimStyle` enum (10 styles + Custom), `UIAnimEase` enum (17 easings, DOTween-mappable), `UIAnimChannel` enum, `UIAnimPreset` SO (per-channel duration/ease/overshoot + button-feedback tokens), `UIAnimPresetLibrary` SO (style → preset map with fallback), `IUIAnimator` interface (Runtime DOTween-free).
- **F6 Theme extended**: `UIThemeConfig` adds 10 sprite slots (panel/button/backdrop/divider/icons), 6 audio slots (button-click/popup-show/popup-hide/success/error/notification), `MinTouchTarget`, `DefaultAnimStyle`, `AnimPresetLibrary` ref.
- **F4 Theme exposure**: `PopupManager` now exposes `Theme` getter (mirrors `UIManager.Theme` pattern).
- **F3 Service binding**: `Runtime/Services/UIServices.cs` — single MonoBehaviour container with Inspector slots for all 6 services (Economy/PlayerData/Progression/ShopData/Ads/Time) + runtime setters for DI interop. `UIManager` exposes `Services` getter.
- **F1 Toast layer**: `Runtime/Toast/` — `UIToastBase`, `UIToast<TData>`, `ToastManager` (priority-less, auto-dismiss via Coroutine, `_maxConcurrent` cap with pending queue, `WaitForSecondsRealtime`).
- **F2 HUD layer**: `Runtime/HUD/UIHUDBase.cs` — abstract base for HUD elements with `OnEnable`/`OnDisable` Subscribe/Unsubscribe pattern + Refresh().
- **F7 SafeArea**: `Runtime/SafeArea/SafeAreaFitter.cs` — applies `Screen.safeArea` per-edge (configurable), polls `Update()` for orientation/resolution changes.
- **F8 Editor validator**: `Editor/Validation/UIKitValidator.cs` — menu item `Kitforge/UI Kit/Validate Active Scene` + `IPreprocessBuildWithReport` hook. Checks null fields and duplicate types in registries; aborts build on errors.
- `UIModuleBase`: virtual `AnimStyleOverride` property for per-element style override.
DECISIONS:
- 10 animation styles (Snappy/Bouncy/Playful/Punchy/Smooth/Elegant/Juicy/Soft/Mechanical/Cinematic) selected via Theme default + per-Module override; Disney Getaway Blast = Playful default.
- `UIAnimPreset` as ScriptableObject (buyer-creatable), library SO (`UIAnimPresetLibrary`) maps style→preset for one-step swap.
- Runtime asmdef stays DOTween-free; catalog asmdef will reference DOTween. `IUIAnimator` uses Action callbacks, not Tween return values.
- Service binding pattern locked = Option B (UIServices container, Inspector-driven, DI-optional).
- Validator built as Editor-side scan + pre-build hook (no runtime cost).
PENDING:
- Open Unity to generate `.meta` files for all new scripts.
- Run EditMode tests to confirm no regression on the 33 existing tests.
- Group 0 demo sample (`Samples~/Catalog_Group0_Foundation/`) + EditMode tests for new components (UIServices/ToastManager/SafeAreaFitter).
- Build the 10 default `UIAnimPreset` SO assets per styles table.
- `_tween` / `_tween-dev` agent update to consume `UIAnimPreset` (deferred — not a code task here).
REFS: `Runtime/Animation/*`, `Runtime/Toast/*`, `Runtime/HUD/*`, `Runtime/SafeArea/*`, `Runtime/Services/UIServices.cs`, `Runtime/Theme/UIThemeConfig.cs`, `Runtime/Core/PopupManager.cs`, `Runtime/Core/UIManager.cs`, `Runtime/Core/UIModuleBase.cs`, `Editor/Validation/UIKitValidator.cs`, `Documentation~/Specs/CATALOG.md`

### Session — 2026-05-01 (cont. 5) — Phase 1.5 deferred NOTEs cleanup
GOAL: Cerrar deuda diferida de Phase 1.5 sin abrir Phase 2.
DONE:
- `UIRouter.Initialize()`: nuevo método público idempotente. `Start()` ahora delega en `Initialize()`. Tests dejan de usar reflection sobre `Start` privado — llaman `_router.Initialize()` directo. Añadido `IsInitialized` getter.
- `UIRouterTests`: +1 test `Initialize_CalledTwice_IsIdempotent` (total UIRouter = 10, total Phase 1.5 = 33).
- README arch decision #10: documenta el patrón `protected override` para `BindUntyped` en derivaciones cross-assembly. Cierra el bug latente del sample que el `/_checker as dev` detectó.
DECISIONS:
- `Initialize` idempotente con flag `_isInitialized` — evita doble-disparo de `OnStateChanged` si Awake/Start corre y además llamamos manual.
- `Start()` se mantiene como `private void Start() => Initialize()` para no romper escenas existentes que dependen del ciclo Unity.
PENDING:
- `git push origin main --tags` para subir Phase 1 + Phase 1.5 al remoto.
- Definir alcance Phase 2 con `planner` skill.
REFS: `Runtime/Core/UIRouter.cs`, `Tests/EditMode/UIRouterTests.cs`, `README.md`

### Session — 2026-05-01 (cont. 4) — UIManager EditMode tests (Phase 1.5 close)
GOAL: Cubrir UIManager con EditMode tests siguiendo el mismo patrón que UIRouter/PopupManager. Cierre de Phase 1.5.
DONE:
- `Tests/EditMode/UIManagerTests.cs`: **12 tests** — Push (first/second/cache reuse/missing prefab), Pop (happy/empty/last), Replace (empty stack/swap/missing prefab no corruption), PopToRoot (multi/single).
- Setup pattern: GameObject inactivo durante `AddComponent<UIManager>()` + `SetField` reflection + `SetActive(true)` al final → evita que `Awake` corra con `_themeConfig == null` (que loggearía error y rompería los tests con `Unhandled log message`).
- `_themeConfig` inyectado como `ScriptableObject.CreateInstance<UIThemeConfig>()` y destruido en TearDown.
- LogAssert.Expect en los 2 tests de fallo de prefab (Push + Replace) — valida el contrato de error documentado en el README.
- **Total Phase 1.5 EditMode tests: 9 (UIRouter) + 11 (PopupManager) + 12 (UIManager) = 32.**
DECISIONS:
- NO se extrajo el test harness compartido (NOTE del checker dev). Los 3 archivos comparten `SetField` + el patrón de fakes pero la duplicación es de ~6 líneas por archivo. Extraer ahora sería over-engineering — la regla "rule of three" ya se cumple (3 archivos), pero los helpers son tan triviales que el coste cognitivo de un harness compartido (otra asmdef? otra clase abstracta? generic constraints?) supera al de copiar 6 líneas. Si llega un 4º archivo de tests con el mismo patrón, extraer entonces.
- `Replace_MissingPrefab_DoesNotPopExistingTop` valida explícitamente la decisión Phase 1 #3 del code-doctor (Replace<T> resuelve incoming antes de Pop). Es el test más importante del suite — protege contra regresión del bug original.
PENDING:
- Tag `v0.2.0-alpha` (espera validación real en Unity Test Runner).
- Refactor `UIRouter.Start` → `Initialize()` público (NOTE diferido).
- README arch decision #10 sobre `protected override` derive pattern (NOTE diferido).
- Validación real en Unity Test Runner sigue pendiente (MCP offline).
REFS: `Tests/EditMode/UIManagerTests.cs`

### Session — 2026-05-01 (cont. 3) — `/_checker as dev` follow-ups
GOAL: Aplicar 2 FIX NOW + 1 NOTE detectados por `/_checker as dev` sobre el primer pase de Phase 1.5.
DONE:
- **FIX NOW**: `Samples~/Quickstart/QuickstartBootstrap.cs` — `QuickstartScreen.BindUntyped` y `QuickstartPopup.BindUntyped` cambiados de `protected internal override` a `protected override`. Mismo bug latente que `Tests/EditMode`: cruzando assembly boundary, `protected internal` se ve como `protected` y CS0507 explota. No lo cazamos antes porque `Samples~/` con tilde no se compila hasta que el buyer importa el sample — habría reventado en máquina del cliente.
- **FIX NOW**: `PopupManagerTests` +2 tests — `IsShowing_TypeNotPresent_ReturnsFalse` (false case del API más usado) y `Show_TypeWithoutRegisteredPrefab_LogsErrorAndReturnsNull` (con `LogAssert.Expect`, valida el contrato de fallo más probable en producción cuando un dev olvida registrar un prefab). Total tests EditMode: 9+11 = **20**.
- **NOTE aplicado (breaking)**: rename `AppState.Playing` → `AppState.Gameplay`. Vocabulario alineado con `PopupPriority.Gameplay` que usa el resto del kit. 1 archivo Runtime + 1 archivo Tests actualizados (sólo 2 usos en todo el paquete).
- `package.json` bump `0.1.0-alpha` → `0.2.0-alpha` por el rename breaking.
DECISIONS:
- Bump pre-1.0 con `-alpha` mantiene la señal de inestabilidad. El tag `v0.1.0-alpha` se preserva como histórico — el siguiente tag será `v0.2.0-alpha` cuando cerremos Phase 1.5 con UIManager tests.
- `protected override` en samples confirmado como contrato del derive pattern. Pendiente documentar en README → Architecture decisions table como entrada nº 10 (no urgente, cuando toque pasar de alpha).
PENDING:
- UIManager EditMode tests — Phase 1.5 sigue abierta.
- Test harness compartido (extraer reflection helpers `SetField`/`InvokeStart` antes de duplicarlos en UIManagerTests) — NOTE del checker dev.
- Refactor `UIRouter.Start` → método público `Initialize()` para no depender de reflection en tests — NOTE del checker dev, deferrable.
- README entry nº 10 en Architecture decisions table sobre `protected override` derive pattern — NOTE.
- Validación real en Unity Test Runner sigue pendiente (MCP offline).
REFS: `Samples~/Quickstart/QuickstartBootstrap.cs`, `Tests/EditMode/PopupManagerTests.cs`, `Tests/EditMode/UIRouterTests.cs`, `Runtime/Routing/AppState.cs`, `package.json`

### Session — 2026-05-01 (cont. 2) — Phase 1.5 EditMode tests
GOAL: Cubrir UIRouter + PopupManager con EditMode tests (Phase 1.5) y arreglar smell residual en `UIRouter.RestrictPopupsTo`.
DONE:
- `UIRouter.cs`: arreglada firma de `RestrictPopupsTo` — estaba pegada a la llave anterior y sin modificador de acceso (privada de facto). Ahora `public void RestrictPopupsTo(...)` con formato correcto. Bug funcional: la API pública para restringir popups no se podía activar desde fuera.
- `Tests/EditMode/KitforgeLabs.MobileUIKit.Tests.EditMode.asmdef`: nueva asmdef Editor-only con `defineConstraints: ["UNITY_INCLUDE_TESTS"]`, referencias a `KitforgeLabs.MobileUIKit` + `nunit.framework.dll` + Test Runner.
- `UIRouterTests`: 9 tests — TransitionTo happy/idempotente/event payload, re-entrancy guard desde callback, IsValidPopup (null type, sin restricciones, con allow-list, null collection bloquea todo), ClearPopupRestrictions restaura estado abierto.
- `PopupManagerTests`: 9 tests — Show inicial, prioridad orden (Modal sobre Meta), MaxDepth=3 cap, drain de queue tras Dismiss, eviction por prioridad superior, eviction preserva Data y restaura al dismiss, DismissAll limpia activos+pending, DispatchBackPressed delega al topmost, DispatchBackPressed sin activos returns false.
DECISIONS:
- Fakes de popup vía `new GameObject().AddComponent<>()` con subclases internas en el test; `Instantiate` clona OK desde scene objects (no requiere prefab asset). Reflection para inyectar `_popupRoot` y `_popupPrefabs` (SerializeField private).
- EditMode (no PlayMode) — todos los flujos testeables son sincrónicos (no corrutinas).
- `_popupCache` no se purga en `DismissAll` (sólo en `OnDestroy`); test `DismissAll_ClearsActiveAndPending` valida que pending también se limpia re-mostrando el primer popup.
PENDING:
- Tests de `UIManager` (Push/Pop/Replace/PopToRoot) — diferidos: requieren screen prefabs y `UIThemeConfig` SO; mismo patrón que PopupManagerTests pero más boilerplate. Phase 1.5 sigue abierta hasta cubrir UIManager.
- Validación en Unity Test Runner — pendiente de ejecutar (Unity MCP offline en esta sesión). Compilación estática verde.
- NOTES diferidas técnicas (PriorityQueue<>, registry compartido, backdrop fade) — sin cambios.
REFS: `Runtime/Core/UIRouter.cs`, `Tests/EditMode/KitforgeLabs.MobileUIKit.Tests.EditMode.asmdef`, `Tests/EditMode/UIRouterTests.cs`, `Tests/EditMode/PopupManagerTests.cs`

### Session — 2026-05-01 (cont.) — PM checker FIX NOW
GOAL: Cerrar Phase 1 aplicando los 4 FIX NOW del PM checker antes de tag v0.1.0-alpha.
DONE:
- `package.json`: pitch description afilada (router + popup queue + theme), keywords ampliadas (`ugui`, `router`, `theme`), entry `Quickstart` añadida en `samples[]` (orden: Quickstart primero, GameWiring después).
- `README.md` reescrito desde cero: pitch line + Status + 8 Non-goals explícitos + Install (con tabla de prereqs DOTween Pro / UniTask / TMP / VContainer opt-in) + Quickstart 5 pasos + Phase 1 done-criteria checklist + Architecture decisions table de 9 entradas.
- `Samples~/Quickstart/` creado:
  - `QuickstartBootstrap.cs` con `[SerializeField]` UIManager + PopupManager y 4 `[ContextMenu]` (Push / Pop / Show / DismissAll).
  - `QuickstartScreen` y `QuickstartPopup` derivan de `UIModule<object>` (no de `UIModuleBase` directamente — `BindUntyped` es `internal abstract` y no es accesible desde otra asmdef).
  - `KitforgeLabs.MobileUIKit.Quickstart.asmdef` referencia solo `KitforgeLabs.MobileUIKit` (NO VContainer).
  - `README.md` con scene-setup paso a paso (Canvas + ScreenRoot/PopupRoot + UIThemeConfig + 1 Screen prefab + 1 Popup prefab).
DECISIONS:
- `UIModuleBase.BindUntyped` y `UIModule<TData>.BindUntyped`: `internal` → `protected internal`. Smell detectado al escribir el sample (la asmdef del Quickstart no podía heredar de `UIModuleBase` directamente). Cambio de 1 token, no breaking, desbloquea el patrón correcto. Sample refactorizado para derivar de `UIModuleBase` directamente — el patrón `UIModule<object>` solo aplica si quieres pasar payload tipado.
- Versión: bump `package.json` 0.1.0 → `0.1.0-alpha`. Renombrado el `[0.1.0]` histórico del CHANGELOG a `[0.0.1]` para alinear SemVer con el release real.
- Commit final + tag `v0.1.0-alpha` ejecutados localmente (sin push).
PENDING:
- EditMode tests (`UIRouter.TransitionTo`, `PopupManager` priority ordering) — Phase 1.5.
- NOTES diferidas de la sesión previa: `PriorityQueue<>` real para `_pendingQueue`, registry compartido `cache+FindPrefab`, backdrop fade.
- Push a remote diferido a decisión manual.
REFS: `package.json`, `README.md`, `Samples~/Quickstart/QuickstartBootstrap.cs`, `Samples~/Quickstart/KitforgeLabs.MobileUIKit.Quickstart.asmdef`, `Samples~/Quickstart/README.md`

### Session — 2026-05-01
GOAL: Aplicar 3 CRITICAL del code-doctor sobre PopupManager + UIManager y validar Phase 1 con checkers drift + as pm.
DONE:
- Hotfix UIRouter.IsValidPopup corrupto por merge fallido (commit 2175e6f, único commit de la sesión).
- code-doctor sobre PopupManager.cs + UIManager.cs: 3 CRITICAL + 2 MINOR + 1 SUGGESTION aplicados.
- PopupRecord unificado guarda Data → eviction preserva estado al re-encolar.
- UIManager.Replace<T> resuelve incoming antes de Pop (no corrompe stack si registry miss).
- OnDestroy en UIManager y PopupManager → OnHide() a módulos cacheados (libera tweens).
- MAX_DEPTH → MaxDepth; using System; añadido en PopupManager.
- PopupEntry + PopupRequest colapsados en PopupRecord único.
- /_checker drift y /_checker as pm ejecutados (veredictos 🔴 moderado / 🔴).
PENDING:
- Aplicar 4 FIX NOW del PM checker: pitch line en package.json, README completo (pitch + Non-goals + Quickstart + Done criteria + Architecture decisions), Samples~/Quickstart/ con QuickstartBootstrap.cs + asmdef + README.  ← NEXT
- EditMode tests (UIRouter.TransitionTo, PopupManager priority ordering) — Phase 1.5.
- Tag v0.1.0-alpha.
- Commit final batched de toda la sesión post-2175e6f.
- NOTES diferidas: PriorityQueue<> real para _pendingQueue, registry compartido cache+FindPrefab, backdrop fade.
DECISIONS:
- Política: 1 commit final por sesión (no per-fix). Excepción única ya consumida en el hotfix UIRouter.
- VContainer = opt-in vía Sample, NO dependencia del Runtime asmdef. Falta formalizar en README.
- ChangeLog del producto vive en el propio paquete UPM (portabilidad), no en KITforg_labs externo.
REFS: Runtime/Core/PopupManager.cs, Runtime/Core/UIManager.cs, Runtime/Core/UIRouter.cs, package.json, README.md

### Added
- Phase 0 scaffolding: package.json, folder layout (Runtime/Editor/Samples~/Documentation~).
- Samples~/GameWiring skeleton with VContainer LifetimeScope and 6 stub services.
- UIRouterStub that logs AppState transitions without invoking services.
- Phase 1: UIThemeConfig ScriptableObject + Editor inspector con color preview.
- Phase 1: UIModuleBase + UIModule<TData> con BindUntyped interno.
- Phase 1: UIManager (Push / Pop / Replace / PopToRoot, prefab cache, registry validator).
- Phase 1: PopupManager (priority queue Meta < Gameplay < Modal, eviction con preservación de Data, backdrop, sibling-order sync, MaxDepth=3).
- Phase 1: PopupPriority enum.
- Phase 1: UIRouter (AppState transitions con re-entrancy guard, popup allow-list con flag explícito, back-button dispatch).
- Phase 1: 6 service interfaces (IEconomyService, IPlayerDataService, IProgressionService, IShopDataProvider, IAdsService, ITimeService) + 4 DTOs.
- Phase 1: OnDestroy cleanup en UIManager y PopupManager.

### Changed
- PopupManager: PopupEntry + PopupRequest unificados en PopupRecord único.
- PopupManager: MAX_DEPTH → MaxDepth (UNITY_RULES const naming).
- UIManager.Replace<T>: valida prefab incoming antes de Pop — no corrompe stack si Push fallaría.
- **BREAKING**: `AppState.Playing` renombrado a `AppState.Gameplay` para alinear vocabulario con `PopupPriority.Gameplay`. `package.json` bumped `0.1.0-alpha` → `0.2.0-alpha`.

### Fixed
- UIRouter.IsValidPopup: bloque corrupto por merge fallido reparado (commit 2175e6f).
- PopupManager eviction: re-encolaba con Data=null perdiendo estado del popup desalojado.
- UIRouter.RestrictPopupsTo: firma sin modificador de acceso pegada a la llave anterior — la API pública para activar la allow-list no era invocable. Restaurada como `public void RestrictPopupsTo(IEnumerable<Type>)`.
- `Samples~/Quickstart/QuickstartBootstrap.cs`: `QuickstartScreen.BindUntyped` y `QuickstartPopup.BindUntyped` cambiados de `protected internal override` a `protected override`. Bug latente que sólo reventaba en máquina del buyer al importar el sample (Samples~/ con tilde no compila en CI del paquete).

### Tests
- Phase 1.5: `Tests/EditMode/` con `KitforgeLabs.MobileUIKit.Tests.EditMode.asmdef` (UNITY_INCLUDE_TESTS).
- `UIRouterTests`: 9 tests cubriendo TransitionTo (happy / idempotente / event payload / re-entrancy guard) y popup allow-list (null type, sin restricciones, con allow-list, null collection, ClearPopupRestrictions).
- `PopupManagerTests`: 11 tests cubriendo Show inicial, prioridad, MaxDepth=3, drain de queue, eviction con Data preserved, DismissAll, DispatchBackPressed, IsShowing false-case, Show con tipo sin prefab registrado (LogAssert).
- `UIManagerTests`: 12 tests cubriendo Push (first/second/cache reuse/missing prefab), Pop (happy/empty/last), Replace (empty stack/swap/missing prefab no corruption), PopToRoot (multi/single). Patrón GameObject inactivo durante AddComponent → field injection → SetActive(true) para evitar Awake con theme nulo.

## [0.1.0-alpha] - 2026-05-01

### Added
- Phase 1 closure: full Runtime (`UIManager`, `PopupManager`, `UIRouter`, `UIThemeConfig`, `UIModuleBase` / `UIModule<TData>`, 6 service interfaces + 4 DTOs).
- `Samples~/Quickstart` (zero-dependency) and `Samples~/GameWiring` (VContainer opt-in).
- `README.md` with pitch line, 8 Non-goals, Install + prereqs, 5-step Quickstart, Phase 1 done-criteria, Architecture decisions table.
- `package.json` with sharpened pitch description, expanded keywords (`ugui`, `router`, `theme`), `Quickstart` sample entry.

### Changed
- `UIModuleBase.BindUntyped` and `UIModule<TData>.BindUntyped`: `internal` → `protected internal`. Allows host assemblies (e.g. samples, buyer game code) to derive directly from `UIModuleBase` without forcing the `UIModule<object>` workaround. Non-breaking.

## [0.0.1] - 2026-05-01

- Initial repository skeleton (renamed from `[0.1.0]` to align SemVer with the real Phase 1 release as `[0.1.0-alpha]`).
